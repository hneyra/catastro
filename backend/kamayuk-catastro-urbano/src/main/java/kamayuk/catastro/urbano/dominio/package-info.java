/**
 * Urbanismo: la zona, sus parametros, la seccion de via y la habilitacion urbana.
 *
 * <p>Sin Spring y sin JPA (regla 7). Ninguna regla de calculo: aqui no se decide ninguna
 * compatibilidad ni ningun derecho de tramite — eso es de {@code rentas} (ADR-0024).
 */
@org.jspecify.annotations.NullMarked
package kamayuk.catastro.urbano.dominio;
