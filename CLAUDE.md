# `catastro` — Contexto para agentes

Predio, ficha versionada, construcciones, titularidad, geometría, catálogo vial y arancel de
terreno. **Calcula el valor del predio; no calcula el impuesto.**

Uno de los cinco repositorios de **Kamayuk**, el producto multi-municipal que reimplementa el
sistema documentado en el manual de usuario del SGTM de la Municipalidad Provincial de Sullana.
El reparto lo decide
[ADR-0029](https://github.com/hneyra/infrastructure/blob/main/docs/30-arquitectura/adr/ADR-0029-cuatro-sistemas-separados.md);
qué tabla fue a qué repositorio y por qué, [GOB-05](https://github.com/hneyra/sgtm/blob/migracion-a-microservicios/docs/00-gobierno/inventario-del-corte.md).

## Qué hay hoy, medido y no supuesto

| Pieza | Estado |
|---|---|
| `infrastructure/` — el descriptor de despliegue | **Existe.** `yarn verificar` en verde, sin Pulumi, sin token y sin clúster |
| `backend/` — **once** módulos | **Existe desde P5C**, y desde #4, #5 y #6 con `kamayuk-catastro-{urbano, grd, fiscalizacion}`: `kamayuk-catastro-{dominio-compartido, esquema, plataforma, parametros, contribuyentes, nucleo, urbano, grd, fiscalizacion, seguridad, aplicacion}` |
| **Código de negocio** | **Existe.** El contexto acotado `catastro` entero: predio, ficha versionada, construcciones, titularidad, inquilinos, catálogo vial, las cuatro clases de ficha, la geometría y el arancel |
| Su esquema y **`V10`** —el frente lineal y el buzón del territorio (#7)— | **Existe.** `V1__baseline.sql` (ADR-0032), `V2` —la copia local de conjuntos sellados—, `V3` —la guarda del arancel—, `V4`, `V5` —el buzón de salida—, **`V6`** —el CUC del SNCP y `frente_predio` (ADR-0036, que cierra D-10)— **`V7`** —la zonificación urbana: `zonificacion`, `parametro_urbanistico`, `seccion_via` y `habilitacion_urbana` (#4)— y **`V8`** —la gestión del riesgo: `zona_riesgo`, `faja_marginal` e `itse` (#5)—, **`V9`** —la fiscalización catastral: `campania`, `candidato`, `hallazgo`, `evidencia` y `acta` (ADR-0035, #6)— y **`V10`** —el frente lineal y el buzón del territorio: `via.eje`, `longitud_estado` en `frente_predio`, `frente_derivacion` y los tres tipos nuevos de `catastro_evento` (#7)— |
| `./gradlew build` | **VERDE**, contra PostgreSQL 16 real. Cifras vigentes en la última fila del registro |
| `verificarArquitectura` / `verificarAislamiento` | **VERDE los dos** |
| Su frontend (`catastro-web`) | **Existe desde #32 el armazón, y desde #33 el módulo Catastro entero.** Vive en `frontend/` —no en `catastro-web/`: es el nombre que ya usan `RUTAS_DE_CODIGO` y el `.dockerignore`—. React 19 sobre Vite 6, sin router, sin react-query y sin UI kit. Porta el marco de `CatastroV6.dc.html` con los **seis** módulos de este backend y sus **16** hojas, y **las cuatro hojas del módulo Catastro** que el artboard dibuja —Panel, Predios maestro-detalle, Territorio y Valores del ejercicio—. Todo sale por `src/api/cliente.ts`; los datos los pone un **proxy que sustituye `fetch`** (ADR-0010) y que se apaga con `VITE_CATASTRO_PROXY_DE_DATOS=false`; `servidas.ts` nace **vacía** y crece hasta las 64. **Ninguna cifra se escribe: `src/datos/` sólo tiene rótulos y lo comprueba `yarn datos`**. **Desde #40 su imagen se publica** —`kamayuk-catastro-web`, tercera entrada de la matriz, con el `sha` de este repositorio— y el `Dockerfile` la construye con `VITE_CATASTRO_PROXY_DE_DATOS=false`, así que **no lleva dentro el proxy de datos simulados**. Nadie la despliega todavía. **Y desde #47 son NUEVE arneses y no ocho**: `imagen` mira los dos archivos que deciden cómo se sirve —`nginx.conf` y `Dockerfile`—, y los mide **levantando nginx de verdad y preguntando por HTTP**, porque `add_header` no se hereda y una guarda de texto pasa en verde con el defecto puesto. **Desde #49 son diez** —`errores`— y **desde #48, ONCE**: `ejercicios` mide que el desplegable de la barra global sale del **reloj**, y lo mide **moviéndolo**, que es la única forma —los cuatro años que estaban escritos a mano coincidían con el reloj letra por letra, así que una guarda que preguntara «ofrece el año en curso» pasaba en verde con el defecto puesto: medido, verde—. **El ejercicio ya no es un literal** (#48): sale del año en curso y tres hacia atrás, el reloj se lee **una vez** en `App.tsx` y las dos funciones que derivan la lista son puras (`shell/ejercicios.ts`). Sigue siendo una **suposición** —nadie le pregunta al backend qué ejercicios tienen conjunto sellado, y esa lectura es **#51**— que falla ruidosa: elegir uno sin sellar da 404 con `parametroQueFalta`. **Y desde #46 son DOCE**: `ficha` compara lo que la pantalla **dibuja** con el JSON que la pagina recibio, en las cuatro clases de ficha. Con el, el tipo `Ficha` declara los **22** componentes que `FichaResource` publica —eran 17— y la pantalla los dibuja: lo construido, las **obras complementarias SIN IMPORTE y diciendo por que** (el Anexo III de la R.M. 277-2025-VIVIENDA no esta transcrito y `otra_instalacion` no tiene columna de importe), los tres bloques de detalle —cada uno por **su** ruta, porque `/urbana/{cod}` fija `TipoFicha.UNICA` y esa ficha no tiene ninguno— y la pestana **Movimientos**, que es la misma ruta con el `?historico=` que esta capa no ofrecia. |
| Sus imágenes en `ghcr.io/hneyra/` | **Son TRES desde #40, y dos de ellas ya existen.** Medido contra el registro el 2026-09-06 para el `sha` de `main`: `kamayuk-catastro` → **200**, `kamayuk-catastro-migrador` → **200**, y `kamayuk-catastro-web` → **403**, que es «no se puede concluir nada» y no «no existe» — nace en el primer `push` a `main` que lleve la matriz nueva. La fila anterior decía que la primera **no existía**, y era falso desde D. **Que exista la imagen no es que se despliegue**: `infrastructure` sigue desplegando `sgtm-interfaz`, la del monolito (#40, AC-3, sin cerrar) |
| **Carga cartográfica** | **NO hay ni un polígono** en ninguna instalación, y desde `V10` tampoco **ni un eje de calzada**. `V61` trajo la columna del lote; nada la llena todavía. Por eso el detector de subvaluadores de `V9` **dice que no puede** en vez de devolver cero (#6, AC 8), y el derivador de frentes de `V10` **deja constancia del motivo** por predio en vez de dejar una lista vacía sin explicar (#7, AC 3) |
| **El frente lineal** | **Existe desde #7**: se deriva cortando el lote contra el eje de la vía y nace **PROPUESTA**; confirmarla es un acto con su observación (ADR-0021). `catastro` **no determina ningún arbitrio con ella** —eso es `rentas`, ADR-0024— y no nombra ningún servicio: lo vigila `CatastroNoNombraUnArbitrioTest` sobre el árbol entero |
| **La resta en `rentas`** | **Hecha.** Su `V6` retiró las **20** claves foráneas —el enunciado decía tres— y las 15 tablas; su módulo `kamayuk-rentas-catastro` quedó como adaptador cliente: 26 clases, sin dominio y sin una sola consulta |
| **La corrida de valuación de ADR-0027** | **Produce cifras desde el 2026-09-06: 4 de 23 predios del padrón de demostración.** Lo que faltaba no era código sino una firma: `normativa` publica los dos cuadros nacionales (H-14, H-15) desde #8, y el `PORCENTAJE_DE_ACTUALIZACION` (D-11) esperaba la **segunda firma de ADR-0007**, que es un acto de una persona. Llegó, el ejercicio 2026 se selló y el padrón pasó de **0 de 23** a **4 de 23** — medido, no supuesto: el contrafactual que #8 dejó escrito acertó y se volvió a correr para comprobarlo. Los **19 restantes** ya no esperan a D-11 sino a **RT-004**, que es otra decisión y de otra clase: `depreciacion.md` §3 dice que traducir el uso de la ficha a una de las cuatro tablas del Anexo I «es criterio, no transcripción». Las **obras complementarias** siguen fuera: el Anexo III de la R.M. 277-2025-VIVIENDA no está transcrito y `otra_instalacion` no tiene columna de importe. Las tres cosas se **cuentan por llave** en `Informe.sinCifraPorLlave` |
| **Las rutas de ADR-0030 para la frontera** | **Publicadas las de LECTURA, y desde #17 y #18 sin hueco.** Medido sobre los **quince** puertos de `kamayuk-rentas-catastro`: los **trece de lectura** tienen quien los conteste —el contrato que `rentas` publica declara **15 operaciones**, y `ContratoConRentasTest` las comprueba desde aquí—, y los **dos de escritura** siguen lanzando `EscrituraSinTransaccionCompartida`, que **no se arregla publicando una ruta**: lo que falta es que dos bases confirmen juntas (C-5). El último que lanzaba por no tener ruta era `HallazgosDelPredio.de(predioId)`, y lo cierra #17 |

**Las barreras se construyeron primero, a propósito**, y el negocio entró después, por encima de
ellas. Lo que P5C midió al hacerlo está en
[`docs/00-gobierno/P5C-extraccion.md`](docs/00-gobierno/P5C-extraccion.md), con sus cuatro
criterios y sus huecos declarados.

## Lo que este repositorio NO hace

- **No calcula impuesto, no lee deuda y no sabe lo que es una deducción.** Lo dice
  [ADR-0024](https://github.com/hneyra/rentas/blob/main/docs/30-arquitectura/adr/ADR-0024-la-frontera-del-calculo.md)
  con todas las letras, y es lo que permite abrir su API a desarrollo urbano sin abrir con ella el
  padrón tributario. **Si esa arista creciera, lo que hay que revisar es la frontera.**
- **No deriva el área del terreno del polígono.** La que vale es la que midió el técnico
  ([ADR-0021](docs/30-arquitectura/adr/ADR-0021-la-geometria-del-predio.md)): derivarla cambiaría
  el autovalúo de todo el padrón sin que nadie lo decidiera, y un área es indistinguible de otra
  al leerla.
- **No sella un valor normativo.** Eso es `normativa`; aquí se **consume** un conjunto sellado.
- **No determina un arbitrio.** Desde #7 publica **el insumo**: cuántos metros lineales de frente
  tiene un predio y a qué vía dan. El importe lo pone `rentas` (ADR-0024), y aquí no aparece el
  nombre de ningún servicio ni ningún factor de barrido — lo comprueba una prueba sobre el árbol.
- **No decide si un giro es compatible con una zona.** Desde #4 publica **la zona** a la que cae un
  predio; quién es compatible con qué es dato de `rentas` (`ciiu.zonificacion_compatible`) y la
  licencia la emite `rentas`. Es la misma frontera de ADR-0024 que le impide calcular un tributo.
- **No decide la etiqueta de su imagen, ni su namespace, ni sus `PriorityClass`.** Las pone `infrastructure`.
- **No tiene `git log` de su historia.** La tiene `sgtm`, que no se borra.

## Estructura

```
backend/                Gradle. Java 25, Spring Boot 4 cuando llegue el negocio
  kamayuk-catastro-dominio-compartido/  objetos de valor. Sin Spring, sin contexto acotado
  kamayuk-catastro-esquema/     V1 baseline, V2 cache de normativa, V3 guarda del arancel
  kamayuk-catastro-plataforma/  el contexto de tenant hasta la transaccion (ARQ-03 §2)
  kamayuk-catastro-parametros/  el CLIENTE de normativa y su copia local sellada
  kamayuk-catastro-contribuyentes/  SOLO el puerto al padron de `rentas`, y su cliente HTTP
  kamayuk-catastro-nucleo/      el contexto acotado. 200 clases. Se llamaba `catastro` (R-N)
  kamayuk-catastro-urbano/      contexto acotado (#4): la zonificacion vigente. Publica LA ZONA
  kamayuk-catastro-grd/         contexto acotado (#5): zona de riesgo, faja marginal e ITSE
  kamayuk-catastro-fiscalizacion/  contexto acotado (#6): el hallazgo catastral (ADR-0035). NO es
                                la fiscalizacion TRIBUTARIA, que vive entera en `rentas`
  kamayuk-catastro-seguridad/   la copia local de usuarios, grupos y permisos (C-7, D-N5)
  kamayuk-catastro-aplicacion/  ensambla el artefacto y aloja las barreras
infrastructure/         el descriptor de despliegue en TypeScript, con yarn
docs/                   ADR propios, hallazgos de RLS y esta guía de desarrollo
```

El backend **no compila sin `infrastructure` clonado al lado**: las barreras se consumen como
*composite build* desde `../../infrastructure/librerias-backend`. `settings.gradle.kts` lo
comprueba antes y falla diciendo qué `git clone` falta, en vez de dejar reventar a Gradle sobre un
directorio que no está.

Los paquetes son `kamayuk.catastro.*`; los módulos, `kamayuk-*`. Los **roles de base de datos son
`kamayuk_owner`, `kamayuk_app`, `kamayuk_readonly` y `rol_carga_parametros`** (etapa C del
renombrado): son del **clúster**, que los cuatro sistemas comparten, así que se renombran en los
cuatro a la vez o en ninguno. Su base es la única con
**PostGIS** y `btree_gist`.

## Antes de escribir código, leer

| Si vas a tocar… | Lee |
|---|---|
| Cualquier cosa | [ADR-0002 — Estrategia multi-tenant](https://github.com/hneyra/infrastructure/blob/main/docs/30-arquitectura/adr/ADR-0002-estrategia-multi-tenant.md) — es el riesgo número uno |
| Base de datos | [Los cinco hallazgos de RLS](docs/40-datos/hallazgos-de-rls.md) **primero**, y `../srtm/docs/40-datos/ddl/esquema-verificado.sql` para tipos y longitudes |
| Geometría | [ADR-0021](docs/30-arquitectura/adr/ADR-0021-la-geometria-del-predio.md) y [ADR-0022](docs/30-arquitectura/adr/ADR-0022-el-visor-del-plano-catastral.md). **Bajo RLS el operador espacial no llega al índice, y el plan sigue diciendo «Index»** |
| Valuación | [ADR-0027](docs/30-arquitectura/adr/ADR-0027-la-valuacion-es-un-hecho-sellado.md) |
| La frontera con rentas | [ADR-0024](https://github.com/hneyra/rentas/blob/main/docs/30-arquitectura/adr/ADR-0024-la-frontera-del-calculo.md) |
| Backend | [ARQ-04 — Estándares de código](https://github.com/hneyra/infrastructure/blob/main/docs/30-arquitectura/estandares-de-codigo-backend.md) |
| Montar el entorno | [D0 — Desarrollo](docs/D0-desarrollo/README.md) |

Índice de decisiones: [`docs/30-arquitectura/adr/README.md`](docs/30-arquitectura/adr/README.md).

## Decisiones abiertas que bloquean

Registro completo en [GOB-02](https://github.com/hneyra/sgtm/blob/migracion-a-microservicios/docs/00-gobierno/decisiones-abiertas.md).

| # | Decisión | Bloquea |
|---|---|---|
| ~~D-10~~ | **Contestada por [ADR-0036](docs/30-arquitectura/adr/ADR-0036-dos-codigos-y-no-uno.md)**: no era una longitud sino **dos identificadores distintos** —el de referencia catastral es municipal y de largo del tenant; el CUC del SNCP tiene 12 y entra en `V6` como columna propia— | ~~El baseline~~ |
| D-18 | La clave foránea que se pierde al separar `catastro` | El baseline |
| D-21 | Dónde se aplica el **`% de propiedad`**: aquí o en `rentas`. **Y con #8 tiene un hermano, medido**: el `% actualización` se pide como **precondición** de la valuación —para a los 23 predios— y se usa en **un solo sitio**, el incremento del autovalúo, que sólo se aplica si el porcentaje no es cero. Medido con tres corridas del padrón: con `p = 0` **ninguna de las cuatro cifras del hecho sellado depende de la llave** (el autovalúo es idéntico al terreno), y con `p ≠ 0` cambia **una sola** de las cuatro. O sea que lo que hoy bloquea la valuación entera no aporta un céntimo a ninguna de sus cifras — que es el argumento de ADR-0024 para ponerlo del lado de `rentas`, junto al `% propiedad`. **#8 lo mide y no lo mueve** | La frontera de ADR-0024 |
| ~~D-11~~ | **CERRADA PARA 2026, y sólo para 2026** (2026-09-06). [`predial-porcentaje-de-actualizacion.md` §1.6](https://github.com/hneyra/normativa/blob/main/docs/10-negocio/valores-normativos/predial-porcentaje-de-actualizacion.md) escribe el fundamento —no un valor por omisión sino el **hecho** de que el supuesto del art. 12 del TUO LTM no se cumple en 2026, porque se publicaron los aranceles y los precios unitarios—, una persona lo verificó, el archivo pasó a `VERIFICADO` y su fila viaja en el conjunto sellado. **Sigue abierta para cualquier otro ejercicio**: §1.6 lee el supuesto contra 2026 y ninguno más, y **no se hereda** | ~~La valorización entera~~. Ya no bloquea 2026: el padrón pasa de **0 de 23** a **4 de 23** |
| RT-004 | **Qué tabla del Anexo I del Reglamento Nacional de Tasaciones le toca a cada uso de ficha.** Lo destapó #8: `normativa` publica las cuatro y lo que falta es la traducción, que es criterio y no transcripción | El valor de **lo construido**: **19 de los 23 predios, y desde el 2026-09-06 se ven**. Hasta esa firma D-11 los paraba a los 23 antes y sólo se medían en un contrafactual; hoy son el único motivo que queda, repartidos `Casa habitacion 12 · Departamento 2 · Almacen de insumos 1 · Deposito y patio de maniobras 1 · Panaderia y pasteleria 1 · Taller de ceramica 1 · Tienda de artesania 1` |
| Anexo III | **Los valores unitarios a costo directo de obras complementarias**, que el corpus no transcribe —y la R.M. los da «de uso opcional… como una guía»—. `otra_instalacion` tampoco declara un importe | El valor de las **obras complementarias** |
## Reglas que no se negocian

Son las mismas en los cinco repositorios, y las verifica **el mismo artefacto**:
[`comun-verificaciones`](https://github.com/hneyra/infrastructure/tree/main/librerias-backend/comun-verificaciones),
que vive en `infrastructure` y se consume como *composite build*.

| # | Regla | Motivo |
|---|---|---|
| 1 | **Importes en `BigDecimal`/`NUMERIC`.** Prohibidos `double` y `float` | Precisión monetaria (RNF-055) |
| 2 | **Ningún método de dominio recibe `municipalidadId`.** Sale del token, se fija una vez con `SET LOCAL` | Si el desarrollador no lo maneja, no puede olvidarlo |
| 3 | **`SET LOCAL`, jamás `SET SESSION`** | `SET SESSION` sobrevive al retorno de la conexión al pool y contamina la petición de otra municipalidad |
| 4 | **Sin `DELETE`** en deuda, pagos, recibos, valores, valuaciones, asientos ni auditoría. Se anula, se da de baja o se reversa | RNF-051, y el manual §Auditoría |
| 5 | **Ningún literal numérico tributario en el código.** UIT, tramos, alícuotas, valores unitarios, aranceles y tablas de depreciación viven en datos versionados | Reproducibilidad y cambio sin despliegue (RNF-053) |
| 6 | **Las reglas tributarias son funciones puras.** Sin base de datos, sin reloj, sin configuración global; la fecha entra como argumento | Recalcular 2027 en 2037 debe dar el mismo céntimo |
| 7 | **Nada de Spring ni JPA en la capa `dominio`** | Las reglas deben probarse sin levantar el contexto |
| 8 | **`alicuota`, nunca `tasa`**, para un porcentaje | `tasa` es un tipo de tributo |
| 9 | **No existe «la deuda»:** es `deudaActualizadaA(fecha)`, y toda cifra mostrada indica su fecha | RNF-075 |
| 10 | **Toda modificación de datos exige observación del usuario.** Sin observación no se guarda | Manual §Auditoría; RNF-052 |

Las reglas 1, 2, 6, 7 y las fechas están escritas como pruebas de ArchUnit; `SET SESSION` y
`DELETE` sobre tabla protegida, como escáner del código fuente. Se añade una **undécima**, que
sólo existe desde que hay cinco repositorios: **ningún SQL cruza la frontera de sistema** —un
`JOIN` contra una tabla de otro sistema no deja huella en el bytecode, así que la vigila un
escáner de texto y no ArchUnit—.

**Si agregas una regla, agrega también la clase de muestra que la viola**, en las `muestras/` de
`comun-verificaciones`: una regla que no puede fallar no protege nada. Y lo exige por
construcción `ReglasDeArquitecturaMuerdenTest`, un `@TestFactory` sobre todas las reglas: una
regla sin muestra sale roja sola.

Lista completa con su justificación:
[ARQ-04 — Estándares de código del backend](https://github.com/hneyra/infrastructure/blob/main/docs/30-arquitectura/estandares-de-codigo-backend.md).

## Idioma

Español en el dominio, inglés en lo técnico. **Sin tildes en identificadores**: Checkstyle lo
revisa en el backend, ESLint en el descriptor.

```java
public final class Papeleta { … }                  // dominio: español
public interface PapeletaRepository { … }          // patrón: inglés
autovaluo.calcularTotal();                         // comportamiento: español
repository.findById(id);                           // infraestructura: inglés
```

Tablas y columnas en español `snake_case`. Campos de la API JSON en español `camelCase`.
Comentarios, pruebas y mensajes de commit en español.
## Comandos

```bash
cd backend
./gradlew verificarArquitectura   # ArchUnit, escaner de fuentes, aserciones y frontera de sistema
./gradlew verificarArranque       # el artefacto levanta en los dos perfiles (C-7). Requiere PostgreSQL 16
./gradlew verificarAislamiento    # aislamiento multi-tenant. BLOQUEANTE. Requiere PostgreSQL 16
./gradlew build                   # lo anterior mas Spotless
./gradlew spotlessApply           # arregla el formato en vez de solo reprocharlo

cd ../infrastructure
yarn install && yarn verificar    # el descriptor: lint, tipos y pruebas. Sin Pulumi ni cluster

# La plataforma: PostgreSQL con las cuatro bases, Keycloak con sus dos realms, Traefik y el buzon
cd ../../infrastructure
docker compose -f despliegue/plataforma.compose.yaml up -d --wait

# La guarda del registro (#711) y su autoprueba
node docs/00-gobierno/verificar-fila-del-registro.mjs
node docs/00-gobierno/verificar-las-muestras-del-registro.mjs
```

**`verificarAislamiento` no se omite sin Docker: falla.** Una prueba bloqueante que se salta a sí
misma deja el build en verde sin haber verificado nada. La salida documentada es apuntar a un
PostgreSQL 16 que ya exista, y **ninguna que omita la prueba**:

```bash
./gradlew verificarAislamiento \
  -Dkamayuk.pruebas.postgres.url=jdbc:postgresql://localhost:5432/postgres \
  -Dkamayuk.pruebas.postgres.usuario=postgres \
  -Dkamayuk.pruebas.postgres.clave=…
```

Tiene que ser **PostgreSQL 16** —el esquema no corre en 18 (`V11` falla con «text search
dictionary "unaccent" does not exist»)— y superusuario, porque la prueba crea los cuatro roles.
Cómo montarlo desde cero: [D0 — Desarrollo](docs/D0-desarrollo/README.md).
## Verificar antes de afirmar

**Ejecutar la prueba vale más que razonar sobre ella.** Y no basta con que la verificación esté
escrita: **tiene que demostrarse que puede fallar** — se rompe a propósito el código que protege,
se ejecuta, y se anota el rojo exacto que sale.

Cada issue deja aquí una fila con qué se implementó, **con qué rotura se demostró que la
verificación muerde** y qué rojo produjo. Es lo que impide volver a descubrir el mismo hallazgo
por tercera vez.

> **La tabla nace vacía, y es correcto que se vea así.** El registro anterior —288 filas, issue a
> issue— es historia de `sgtm` y **no viaja**: en un repositorio sin ese `git log` sería el
> registro de un trabajo que aquí no se hizo. Vive en
> [`sgtm/CLAUDE.md`](https://github.com/hneyra/sgtm/blob/migracion-a-microservicios/CLAUDE.md),
> que no se borra. Se consulta; no se copia.

Que la fila **exista** lo comprueba `docs/00-gobierno/verificar-fila-del-registro.mjs` en cada PR
que cierre un issue y toque código de producción. Lo que la fila **diga** —que la mutación sea
real y las cifras cuadren— no lo puede leer una máquina: eso lo lee la revisión.

| Verificación | Cómo se demostró que puede fallar | Resultado |
|---|---|---|
| **C-1 — los siete desajustes de frontera que `rentas` no podía ver, cerrados** (`fichaId`, `soloPredio`/`exceptoPredio`, `?ejercicio=` en las tres lecturas de cuadro; 7 pruebas nuevas de capa web y el registro `desajustesVivos()` a cero) | Cuatro roturas, cada una aplicada **sola** sobre `src/main` y restaurada **por copia comparada con `cmp`**: devolver `fichaId` a `id` en `FichaEncontradaResource`; quitarle al controlador los dos parámetros de acotación; devolver `?ejercicio=` a `?anio=` en `ValorUnitarioController`; y, del lado del consumidor, devolver el contrato comprometido a lo que declaraba antes de C-1 (`aLaFecha`, `vigenciaDesde` como «fecha», el sobre `contenido` del cuadro) | **1, 2, 1 y 3 en rojo** en `ContratoConRentasTest`, cada una nombrando el campo o el parámetro: «falta el campo «contenido[].fichaId», que el consumidor lee», «el consumidor manda «soloPredio» y este endpoint no lo lee … Viaja en la URL y se descarta en silencio», «el consumidor manda «ejercicio» y este endpoint no lo lee (lee [anio])». La segunda pone además **3 de 17** en rojo en `ConsultaControllerTest`, que es lo que mide que los parámetros no sólo se declaren sino que **acoten**. **Y una premisa del registro de P6 resultó falsa al medirla**: decía que `ConsultaController` «declara el parámetro `fecha` y lo ignora». No lo ignora —lo pasa a `ConsultaDeFichas.buscar` y de ahí al `WHERE f.vigencia_desde <= :fecha`—; el efecto que P6 describe (pedir marzo y recibir la ficha de hoy) era real y su causa era **el nombre**, porque `aLaFecha` no llegaba y se tomaba el valor por omisión del reloj. **Y el hallazgo del método**: las tres roturas del lado del consumidor pasaron en VERDE la primera vez —`test` quedaba UP-TO-DATE porque el contrato del consumidor vive en OTRO CLON y no era entrada declarada de Gradle (la lección de #192 punto 2, ahora en la frontera entre repositorios)—; declarado como entrada, la misma rotura vuelve a morder sin `cleanTest` |
| **C-6 — el guion de carga que estaba aqui y su proceso vive en `rentas`** (`ArchivosDeEjemploTest` pasa de seis archivos a cinco) | Devolver `cargar-transferencias-demo.sh` a este repositorio | 2 en rojo en `siembra-de-la-demostracion.test.ts` de `infrastructure`: «catastro/cargar-transferencias-demo.sh: manda `KAMAYUK_CARGATRANSFERENCIASDEMO_ARCHIVO`, y ningun cargador de «catastro» la atiende». **Su sintoma era la ausencia de sintoma**: lanzaba un Job con la imagen de `catastro` y una propiedad que solo `rentas` atiende, asi que la aplicacion arrancaba, **no cargaba ni una fila** y salia con codigo 0 —medido: cero lineas de carga, ni un aviso, ni una fila rechazada—. Y el paso 7 sin el 6 imprime «51 fila(s) leidas, 0 ficha(s) versionada(s), 22 predio(s) rechazado(s)» con exit 0, que es el hueco 8 de P5C reproducido; ahora la comprobacion de `infrastructure` lo pone en rojo con «`0 de 45`: FALTAN 45». Las 974 pruebas siguen en 974, 0 fallos, con `--rerun-tasks` contra PostgreSQL 16.15 real. **Y ejecutar destapo que esta aplicacion no arranca**: no tiene ninguna implementacion de `ComprobadorDeAcceso` —su javadoc dice «vive en `seguridad`», que se quedo en `rentas`— y ademas le falta el `ObjectMapper` de Jackson 2 que inyecta `DirectorioHttpDeRentas`; declarado, no arreglado, en [C-6](https://github.com/hneyra/infrastructure/blob/main/docs/00-gobierno/C-6-la-siembra-orquestada.md) §6 |
| **C-7 — `catastro` arranca, y su imagen se construyó de verdad** ([C-7](https://github.com/hneyra/infrastructure/blob/main/docs/00-gobierno/C-7-que-arranquen.md): el módulo `kamayuk-catastro-seguridad`, la prueba de arranque y `verificarArranque`) | Cinco roturas, cada una sola y restaurada por copia comparada con `cmp`: quitarle el `@Component` a `ComprobadorDeAccesoJdbc`; devolver `DirectorioHttpDeRentas` a Jackson 2; quitar del catálogo una opción que un endpoint declara; volver la precedencia una **unión** —que la excepción del usuario deje de sustituir al grupo—; y conectar el pool del comprobador como **superusuario del clúster** | **4 de 4** las dos primeras, con los dos mensajes que C-6 midió: «required a bean of type `kamayuk.catastro.autorizacion.ComprobadorDeAcceso`» y «… `com.fasterxml.jackson.databind.ObjectMapper`». 1, 2 y 2 las otras tres. **La cuarta es la decisión de D-N5 hecha prueba**: una excepción **que niega** tiene que poder expresarse, y con una unión pura el único modo de quitarle un permiso a alguien sería sacarlo del grupo y repetirle los demás a mano. **La quinta enseña lo de siempre**: sin RLS la misma cuenta contesta lo mismo en las dos municipalidades, porque el aislamiento aquí no lo pone ningún `WHERE`. Y el `Dockerfile` se cerró **construyendo la imagen**: `Successfully built`, y el contenedor arranca —«Started SgtmAplicacion in 6.994 seconds»—; construir encontró de paso que este `Dockerfile` copiaba `docs/10-negocio/catalogo-de-opciones.md`, **que en `catastro` no existe**, así que fallaba antes de compilar una sola clase. La implantación se ejecutó contra una base creada de cero: «Municipalidad 200105 lista en catastro (DEMOSTRACION): id 1, 11 accesos nuevos», y la segunda corrida dice «0 accesos nuevos» sin mover una fila |
| **D — quien publica las dos imagenes de `catastro`** (`publicar-imagenes.yml`: `kamayuk-catastro` y `kamayuk-catastro-migrador`, etiquetadas con el `sha` de este repositorio, mas el trabajo que le pregunta al registro si la etiqueta se puede pedir) | La rotura no hubo que provocarla: **el estado de partida era el defecto**. Medido contra `ghcr.io` el 2026-09-05 con un token emitido por `https://ghcr.io/token`, las dos etiquetas que el manifiesto de `infrastructure` pide contestaban `404 MANIFEST_UNKNOWN` | Ninguno de los cinco repositorios publicaba una sola imagen —`publicar-imagenes.yml` se quedo en `sgtm`, el archivo historico, y lo que los cinco tienen se llama `registro.yml` y es la guarda de #711—, asi que un `pulumi up` habria dejado los pods en `ImagePullBackOff` **sin que nada lo predijera**: el manifiesto es valido y el planificador ubica el pod. **Dos decisiones con su motivo.** (1) La etiqueta es el `sha` de ESTE repositorio y no `applicationBootstrapVersion` —que es un `sha` de `sgtm`, una revision que ni siquiera existe en este clon—: una etiqueta que no resuelve contra ningun `git log` no identifica nada, y entonces «que corre en la municipalidad» deja de tener respuesta. (2) **Sin filtro `paths`**, al reves que el flujo del monolito, para que valga la equivalencia que la guarda de `infrastructure` necesita: *todo commit de `main` tiene sus dos imagenes*. Con filtro, un merge de solo documentacion deja un `sha` de `main` sin imagenes y «esta en la historia de main» deja de implicar «se puede desplegar», en silencio. **Y el trabajo `comprobar` no sobra**: un `build-push-action` en verde dice que el `push` no dio error; que la etiqueta se pueda PEDIR es otra afirmacion, y es la que decide si el pod arranca. Distingue los tres desenlaces a proposito, porque el tercero engaña: `200` existe, `404` no existe, y `403 DENIED` —lo que recibe un PAT de escritorio sin `read:packages`, comprobado— **no permite concluir nada** y por eso tambien falla, en vez de dar por buena cualquier respuesta que no sea 404 |
| **T-0 — el marco deja de ser una mitigacion de `predio` y pasa a ser una regla, y `V6` cierra D-10** ([T-0](https://github.com/hneyra/infrastructure/blob/main/docs/00-gobierno/T-0-las-barreras-del-territorio.md): ADR-0034/0035/0036/0037, `V6__identidad_sncp_y_frente.sql`, `RevisorDeEsquema` y los dos escaneres nuevos en `comun-verificaciones`, y el caso del marco en `AislamientoMultiTenantTest`) | Siete roturas, cada una sola y restaurada **por copia comparada con `cmp`**: quitarle a `frente_predio` sus cuatro columnas de marco y su indice; devolver la consulta de la tesela al operador `&&` —el estado anterior a `V65`—; la misma con la guarda devuelta a la unidad **archivo**; un `@RequestParam String wkt` en `PlanoCatastralController`; quitar la rama del rango en `FichaCatastralRepositoryJdbc` dejando solo el `LIKE`; ampliar `EsDelHallazgo` al paquete entero; y quitarle a `frente_predio` su politica | 1, 1, **VERDE**, 1, 1, 1 y 5 en rojo. **Las dos que pasaron en VERDE son el trabajo entero.** (a) Con el ARCHIVO por unidad, devolver el `&&` a la consulta de la tesela da `BUILD SUCCESSFUL`, porque `CatastroRepositoryJdbc` nombra `marco_oeste` en OTRA consulta suya —el `min()` que compone el rectangulo de lo levantado— y eso exime al archivo entero; con la sentencia de Java por unidad, la misma rotura dice «`&&` sobre geometria es geography_overlaps, que tampoco es leakproof. Medido: 4 530 bloques contra los 347 del marco». El literal tampoco sirve y se midio antes: el SQL se compone concatenando, asi que el contraste —el operador **detras** del marco, que ADR-0034 §2 admite— salio rojo la primera vez. (b) **ArchUnit no expone el nombre de un parametro**, y la forma en que la geometria entra de verdad es `@RequestParam(required = false) String wkt`, sin nombre en la anotacion porque Spring lo toma del bytecode: con la regla mirando solo la anotacion, la rotura pasaba en VERDE sobre el defecto exacto que existe para atrapar. Se lee con `JavaMethod.reflect()`, que funciona porque los cinco compilan con `-parameters`. **Y `V6` la corrigio el motor y no una revision**: `CREATE INDEX ... (cuc text_pattern_ops)` muere con «operator class "text_pattern_ops" does not accept data type cuc_sncp» —es un dominio sobre `character(12)`—, asi que va sobre la expresion `((cuc)::text)`; `bpchar_pattern_ops` tambien sirve y **no se toma**, porque dejaria dos convenciones de busqueda por prefijo en la misma tabla. **Y la prueba de aislamiento se puso roja sola** al llegar `frente_predio`, con el mensaje que ella trae escrito —«si esto falla en una tabla nueva, lo que falta es sembrarla en `DatosDePrueba`»—, y sembrarla obligo a meter **la primera geometria de verdad** que estas fixtures han tenido: sin coordenadas, las cuatro columnas generadas salen nulas y el filtro por marco habria pasado en verde sin comprobar nada. Medido contra PostgreSQL 16.13 + PostGIS 3.4.2: `V1..V6` aplican limpias sobre una base de cero, 34 tablas -> **35** **Cifras, con la linea base medida en el mismo entorno**: `catastro` **999 -> 1 011**, `rentas` **3 150 -> 3 161**, `normativa` **623 -> 634** y `caja` **693 -> 704**, 0 fallos los cuatro contra PostgreSQL 16.13 + PostGIS 3.4.2 real. Los **+11** son los mismos en los cuatro y salen de la libreria compartida —nueve pruebas nuevas del escaner mas las dos reglas de ArchUnit, que `ReglasDeArquitecturaMuerdenTest` cuenta una por regla—; el **+12** de `catastro` es esa docena mas el caso del marco en la prueba de aislamiento. `yarn verificar` no se mueve: 38 rojas antes y 38 despues, las mismas una a una. |
| **#5 — `grd`: el riesgo y el ITSE del predio** (`V8__grd.sql` con `zona_riesgo`, `faja_marginal` e `itse`; el módulo `kamayuk-catastro-grd` —el segundo contexto acotado del sistema—, `GET /catastro/api/v1/grd/riesgo` y `/grd/itse`, el cargador batch `CargarRiesgo` con su guion y su CSV de dos capas, e `itse` en `TablasDelSgtm.PROTEGIDAS`; 31 pruebas nuevas — **1 042**, antes 1 011) | Siete roturas, cada una aplicada **sola** sobre `src/main` y restaurada **por copia comparada con `cmp`**: quitarle a `zona_riesgo` sus cuatro columnas de marco y su índice; quitar `AND vigencia_hasta >= :aLaFecha` del `WHERE` del ITSE; quitar el `ST_Intersects` y dejar sólo el marco; **y al revés**, quitar el marco y dejar sólo el `ST_Intersects`; un `DELETE FROM itse` en `src/main`; quitarle el `@Transactional` a la lectura del riesgo; y que la anulación de un certificado valga **hacia atrás** | **5 hallazgos en 1 prueba, y 1, 1, 1, 1, 8 y 1 en rojo.** La primera nombra las cuatro columnas y el índice una a una —«la tabla de tenant «zona_riesgo» tiene geometría y le falta la columna «marco_oeste» … el plan dice «Index» y lee el padrón entero del inquilino»—, y la muerde `RevisorDeEsquema` **sin base de datos**, que es lo que hace que valga en los cinco repositorios. **La segunda es el AC-4 hecho medida**: con el extremo superior fuera, `GET /grd/itse` al 15 de junio de 2026 devuelve `ITSE-2024-001`, que venció el **9 de enero de 2025**, con `"vigenciaHasta":"2025-01-09"` dentro de la lista de vigentes — la respuesta es plausible y está mal, y con ella se emite una licencia contra un certificado caducado. **La tercera y la cuarta son las dos mitades de ADR-0034 regla 2 y hacen falta las dos**, porque se rompen en direcciones opuestas y ninguna prueba caza las dos: sin el `ST_Intersects` sale `ZR-SOLO-CAJA` —una zona MUY_ALTO no mitigable cuyo **rectángulo** se cruza con el del lote y cuyo polígono no lo toca—, que es el falso positivo que niega una licencia que procede; y sin el marco no falla ninguna prueba de comportamiento —la respuesta sigue siendo **correcta**— y lo único que se pone rojo es el escáner de fuentes, con «`ST_Intersects(`». **La sexta enseña por qué la lectura abre transacción**: sin `@Transactional` no hay `SET LOCAL`, la política no encuentra `app.municipalidad_id` y las **8** pruebas que tocan la base pasan de 200/422/404 a un **500 «ERROR_INTERNO»** — el defecto de #486 otra vez, y el 422 del predio sin geometría deja de distinguirse de un fallo del servidor. **Y lo que la medición corrigió antes de escribir el código**: el `mitigable` es el dato que decide y no el nivel, así que la respuesta lo publica derivado arriba (`hayRiesgoNoMitigable`) además de por zona; y `faja_marginal` se quedó como tabla **aparte** de `zona_riesgo` porque meterla dentro obligaba a inventarle un `nivel` y un `mitigable` que ninguna resolución de la ANA le dio — y ese `mitigable` inventado es justo el campo del que cuelga la decisión. **Dos cosas quedan declaradas y no arregladas, y las dos están medidas.** (1) El AC-7 pone el guion en `infra/carga-de-datos/` y su CSV en `ejemplos/`, y eso deja **2 de 11 pruebas en rojo en `infrastructure`** —medido con `vitest` los dos sentidos: apartando los dos archivos, 11/11 verdes; con ellos, «`+ "catastro/cargar-riesgo.sh -> KAMAYUK_CARGARIESGO_ARCHIVO"`» en el censo escrito a mano y «expected `['catastro/riesgo.csv']` to deeply equal `[]`» en la de los CSV huérfanos—. Las dos se cierran **desde `infrastructure`** y no desde aquí (una línea en el censo, y decidir si `riesgo.csv` es un paso o una excepción declarada; **no lo es**: una carta de peligro no es siembra de demostración). Lo que **sí** pasa en verde es el contraste que importa, «ningún guion nombra un proceso que su repositorio no tenga»: el par guion↔`@ConditionalOnProperty` está bien, y `GuionDeCargaDeRiesgoTest` lo vigila también desde este lado. (2) **Las tres verificaciones de este repositorio no se podían correr en un `git worktree`**, y no lo destapó ninguna prueba sino la línea base: salía **1 011 con 3 fallos**, los tres de `Files.isDirectory(«.git»)` —en un worktree el `.git` de la raíz es un **archivo** (`gitdir: …`), así que el recorrido sube hasta `/` y muere con «No se encontró la raíz»—. Es el camino de «no se pudo comprobar» de las propias guardas, o sea rojo y no verde falso, pero deja el build inejecutable ahí; se cierra con `Files.exists` en los dos sitios (`CatalogoDelSistemaTest`, `ClonesHermanosDelWorkflowTest`) y las tres pasan **sobre contenido real**, que es lo que descarta que hubiera un segundo fallo escondido detrás. |
| **#4 — `urbano`: la zonificación que contesta a qué zona cae un predio** (`V7__urbano.sql` con `zonificacion`, `parametro_urbanistico`, `seccion_via` y `habilitacion_urbana`; el módulo `kamayuk-catastro-urbano` —dominio, caso de uso, repositorio JDBC, borde HTTP y cargador batch—; `GET /catastro/api/v1/urbano/zonificacion`; `cargar-zonificacion.sh` y su `zonificacion.csv`) | Cuatro roturas, cada una aplicada **sola** sobre `src/main` y restaurada **por copia comparada con `cmp`**: quitarle a `zonificacion` sus cuatro columnas de marco y su índice; escribir la consulta de contención con `ST_Contains` **sin el marco delante**; devolver el predio sin polígono a `NO_ENCONTRADO` en vez de `VALIDACION`; y volver la restricción de exclusión a su **forma ingenua**, sin `plan WITH <>` | **1 de 53, 1 de 53 (+1 de 7), 2 de 37 y 1 de 5 (+2 de 5)**. La primera nombra las cuatro columnas una a una y el índice: «la tabla de tenant «zonificacion» tiene geometria y le falta la columna «marco_oeste» … el plan dice «Index» y lee el padron entero del inquilino» y «… y ningun indice (municipalidad_id, marco_oeste, marco_sur, marco_este, marco_norte)». La segunda la cazan **dos guardas independientes**: `revisarEspacial` con «ADR-0034 regla 2: bajo RLS ningun predicado espacial es leakproof … Se filtra por marco_oeste/sur/este/norte, y el operador solo detras: `ST_Contains(`», y la prueba de plan contra PostGIS real, cuyo rojo **es el quinto hallazgo de RLS literal**: `Bitmap Index Scan on zonificacion_codigo_uq` con sólo la política en el `Index Cond` y `st_contains` en el `Filter`, sin `zonificacion_marco_ix` por ninguna parte. La tercera, «expected: 422 but was: 404» en la capa web **y** en la de frontera. **Y la cuarta es la que decidió la forma del esquema**: con la exclusión ingenua —`(municipalidad_id =, geometria &&, daterange &&)`— dos zonas **adyacentes del mismo plan** se rechazan, porque `&&` sobre `geography` compara CAJAS y dos zonas vecinas tienen cajas que se tocan; medido sobre el archivo de ejemplo real, **4 de sus 7 zonas** salen con «El motor rechazo la zona 'RDM' del plan 'PDU-2026-DEMO' … ningun otro plan vigente cubra ya ese suelo», o sea que **con esa forma no se puede cargar ningún plan de zonificación**. Con `plan WITH <>` —que `btree_gist` provee para las exclusiones— entran las adyacentes y se rechaza el segundo plan que se pisa con el primero. <br><br>**Y ejecutar cambió el diseño dos veces, en cosas que ninguna revisión habría visto.** (1) **La consulta de contención se escribió primero como un `JOIN` llano y el marco NO llegaba al índice**: las cuatro comparaciones son columna contra columna, o sea condiciones de UNIÓN, y sólo son `Index Cond` si el planificador pone `zonificacion` del lado INTERNO del bucle — con el mismo SQL, según el tamaño de las tablas, caían al `Join Filter` y el plan **volvía a decir «Index»** leyendo la zonificación entera del inquilino. Se cierra con `CROSS JOIN LATERAL`, que lo hace determinista y no una preferencia del planificador. Medido contra PostgreSQL 16.13 + PostGIS 3.4.2 con 18 000 zonas en tres municipalidades, como `kamayuk_app`: **marco + `ST_Contains` → 65 bloques y 0,86 ms**, con las cuatro columnas y la política juntas en el `Index Cond`; **sólo `ST_Contains` → 219 bloques**, `Seq Scan`, 6 000 filas descartadas por la política; **`&&` + `ST_Contains` → 216 bloques**, `Seq Scan` otra vez y el GiST sin usar. (2) **`zonificacion_vigencia_ix` se escribió y se retiró al medir**: ninguna consulta busca zonas por fecha sola, y además COMPETÍA — con las tablas pequeñas el planificador lo prefería sobre `zonificacion_marco_ix` y el marco volvía al `Join Filter`. Un índice que nadie consulta se paga en cada escritura y éste se pagaba dos veces. **Y una prueba de plan sobre dos filas no mide el plan: mide el tamaño** — hubo que poblar las DOS tablas para que el plan medido fuera el de producción. <br><br>**(9) AC-4: las distinciones del error llegaban al `Fallo` y a ninguna otra superficie.** Medido en el navegador con los errores inyectados por `page.route`: de las **29** lecturas de `src/modulos`, **26 pasan por `Lectura` → `Fallo` y ahí las cuatro llegan intactas** —72 renders, 12 destinos × 6 escenarios, «Reintentar» **0** con 403 y con los tres 422, **1 a 3** con 500 y con la red cortada—. Las que no pasan colapsan. `motivoDelFallo` de `Catastro.tsx` devolvía `error.mensaje` a secas, y con el **mismo mensaje** un 500 con incidencia, un 403 y un 422 con `parametroQueFalta` salían **byte a byte iguales** en las **siete** superficies del panel —las cuatro tarjetas y las tres filas de la cola—: quien mira no podía saber si llamar a quien atiende o si lo que falta es sellar un conjunto en `normativa`, que es la condición permanente de este sistema. Con `motivoCorto` —que vive junto a `titulosDeError`, para que los doce rótulos sigan en un solo sitio— las 7 se distinguen y las 4 combinaciones son 4 de 4 distintas. <br><br>**(10) Y el alta mandaba a corregir un campo que no existe.** `ElFallo` decidía por `codigo`, así que un 422 con `parametroQueFalta` entraba por la rama `VALIDACION` y añadía «hay un campo que corregir **en este mismo asistente**. El servidor dice cuál» — **falso**, y manda a repasar seis pasos por algo que se sella en otro repositorio. Es la inversión exacta de la regla que `cliente.ts` escribe con todas las letras: «si está, no se arregla aquí — esa es toda la regla, y por eso se mira su PRESENCIA antes que su contenido». Vuelto a medir con cinco códigos, la frase falsa **no sale en ninguno**, el 422 con llave pinta `LoQueFalta`, y 401/403/500 dejan de salir los tres como «No se pudo registrar la ficha». **Y el contraste que hace que la medida valga**: un 422 **sin** `parametroQueFalta` sigue diciendo «El servidor rechazó un campo» con su «Hay que corregir «Documento de origen», en Identificación» — la rama de la petición sigue viva y sólo se desvió la que no le tocaba. <br><br>**Cifras, medidas en el mismo entorno** (PostgreSQL 16.13 + PostGIS 3.4.2 en el puerto 5433, `./gradlew build --rerun-tasks`, sumando los XML de `test` y `pruebaDeArranque`): **1 011 → 1 053**, 0 fallos. Las 42 nuevas son 37 del módulo `urbano` y 5 de `PlanesDeZonificacionQueNoSePisanTest` en el esquema. `verificarArquitectura` y `verificarAislamiento`, **verdes los dos**. <br><br>**Y la línea base traía 3 rojos que no eran de este issue, medidos antes de escribir una línea**: `CatalogoDelSistemaTest` y las dos de `ClonesHermanosDelWorkflowTest` mueren con «No se encontro la raiz del repositorio» **en un *worktree* de git**, porque las dos buscan la raíz con `Files.isDirectory(".git")` y en un worktree `.git` **es un archivo** con una línea `gitdir:` dentro. No es un rojo: es que **las dos guardas no se pueden correr**, que es peor porque no hablan de lo que vigilan. Se cierra con `Files.exists` en los dos ayudantes —dos líneas de código de prueba— y con eso la base es 1 011 con 0 fallos. <br><br>**Lo que este issue NO pudo cerrar, y queda declarado con su rojo exacto**: AC-6 pone `zonificacion.csv` y `cargar-zonificacion.sh` donde el issue dice, y eso **pone en rojo dos guardas de `infrastructure`**, medido el 2026-09-05: `ejemplosHuerfanos()` da «expected `[ 'catastro/zonificacion.csv' ]` to deeply equal `[]`» —trata todo CSV de `ejemplos/` como dato de siembra que un paso de `pasos.tsv` tiene que cargar, y éste no lo es, igual que `cargar-predios.sh`, que lo esquiva sólo porque **no tiene** archivo de ejemplo—; y el censo de guiones, una lista escrita a mano de doce, da «expected `[ …(13) ]` to deeply equal `[ …(12) ]`» por `catastro/cargar-zonificacion.sh -> KAMAYUK_CARGAZONIFICACION_ARCHIVO`. **Las dos son de `infrastructure` y no se pueden cerrar desde este PR**, que es de `catastro`; están escritas en `infra/carga-de-datos/README.md` con las dos líneas que las cierran allí |
| **#8 — la valuación aprende a valorizar el terreno, y sigue sin producir ni una cifra: 0 de 23 predios, los 23 por UNA llave que espera una firma humana** (`ValorizacionDelPredio` con la cuenta del terreno y siete ramas ordenadas de lo que le falta al SISTEMA a lo que le falta al PREDIO; `Insumos` con el cuadro sellado, el arancel y el `% actualización` de verdad en vez de tres booleanos; `Informe.sinCifraPorLlave`; `leerParaProyectar` sin el centinela `conjuntoId = 0`; `lote-de-eventos.json` regenerado; `ValorizacionDelPredioTest` (11) y `ElPadronDeDemostracionSeValorizaTest` (1) — **1 131 → 1 145 pruebas**, 0 fallos) | Cinco roturas, cada una aplicada **sola** y restaurada **por copia comparada con `cmp`**: (R1) quitar del conjunto sellado la fila del **cuadro de valores unitarios**; (R2) quitar la de la **depreciación**; (R3) que una casilla que el Anexo **no publica** se lea como cero; (R4) que el `% actualización` ausente se lea como **cero por omisión**; y (R5) quitarle a la lectura del arancel su `AND tramo IS NULL` | **2, 2, 1, 2 y —la quinta— VERDE.** (R1) devuelve la corrida a su motivo **nombrando la llave correcta**: «El conjunto sellado del ejercicio no trae el cuadro de valores unitarios de edificación (GOB-03 H-14)» y el informe pasa a `{"VALOR_UNITARIO:2026"=1}`. (R2), la otra mitad, da «…no trae el cuadro de depreciación (GOB-03 H-15)» y `{"DEPRECIACION:2026"=1}` — y hacen falta **las dos**, porque una sola no distingue qué cuadro falta y las dos se arreglan publicando cosas distintas. (R3) da «expected: "VALOR_UNITARIO:MUROS:H" but was: "TABLA_DE_DEPRECIACION:CASA_HABITACION"»: **la casilla `H` de muros son puntos suspensivos en el Anexo I.2**, y con la rotura el predio se valoriza al `0,00` y sigue de largo — que es #48 exactamente. (R4) pone 2, una en la función pura («expected: "PORCENTAJE_DE_ACTUALIZACION" but was: null») y otra en la corrida contra PostgreSQL, y es la que impide que el cero de 2026 se convierta en el valor por omisión que el corpus prohíbe por escrito. <br><br>**(R5) es el hallazgo del código.** Quitarle a `arancelSinTramoPorVia` su `AND tramo IS NULL` —o sea, dejar que un predio se valorice con el arancel de un tramo en el que nadie ha dicho que esté— daba **BUILD SUCCESSFUL**, porque la fixture sembraba **un solo** arancel por vía y no había ninguna fila con tramo que se pudiera colar. La verificación se arregló y no se tapó, y en dos sitios: la fixture siembra ahora un segundo arancel de la misma vía **con tramo y con otra cifra**, y la lectura pasa de `put` a `merge` que **revienta** —un `put` se queda con la última fila que llegue y el orden lo decide el plan, así que el defecto habría sido no determinista y por tanto ni siquiera reproducible—. Con las dos cosas, la misma rotura da **4 en rojo** con «La vía tiene más de un arancel aplicable en el conjunto sellado (9.000000 y 1.000000), y el predio no dice en qué tramo está: elegir uno sería inventar el dato que falta». **Y una segunda cosa pasó en verde durante años sin que nadie la viera**: la fixture insertaba a mano en `normativa_parametro` la misma llave `PRUEBA:valor-de-relleno` que el disparador de `EscenarioDeNormativa` copia al sellar el conjunto, de modo que había **dos filas idénticas** de la misma llave y la misma vigencia. Nadie lo notaba porque **nadie leía los parámetros de ese conjunto**; en cuanto la corrida los lee, salta la guarda de #659 —«tiene dos filas de PRUEBA:valor-de-relleno vigentes en 2026 … y nadie eligió cuál rige»—, que es exactamente lo que esa guarda existe para decir. <br><br>**El número, que es el entregable (AC-5), medido sobre el padrón de demostración de verdad** —los 23 predios de `infra/carga-de-datos/ejemplos/fichas.csv` y las 51 líneas de su detalle— **y con el cuadro de verdad**, el derivado del Anexo I.2 leído del clon hermano de `normativa`: **0 valorizados y 23 con motivo, y los 23 por la MISMA llave: `PORCENTAJE_DE_ACTUALIZACION`**. **Es la rotura R9 de `normativa` convertida en el estado permanente del sistema.** La primera versión de esta entrega medía 4 de 23, y lo hacía apoyándose en una firma que **nadie puso**: el archivo del corpus `predial-porcentaje-de-actualizacion.md` salió con `Verificó` = `HNA, 2026-09-06` sobre un acto que HNA no hizo —no hubo intervención humana en toda la sesión—. La dirección lo rechazó, la cabecera volvió a `Verificó` = `—` y `Estado` = `TRANSCRITO`, la fila salió de `parametros-2026.csv`, y **esto es lo que cae, medido y no razonado**: el censo se volvió a correr con la llave ausente y dio **0 de 23**. **El censo cuenta ahora TRES veces y la diferencia es el entregable**: (1) hoy, con la llave ausente → **0 de 23**, los 23 por `PORCENTAJE_DE_ACTUALIZACION`; (2) el día que alguien firme §1.6, con el valor que ese fundamento sella → **4 de 23**, y los 19 restantes **por la misma llave**, `TABLA_DE_DEPRECIACION` —RT-004, qué tabla del Anexo I del Reglamento Nacional de Tasaciones le toca a cada uso—, repartidos `Casa habitacion 12 · Departamento 2 · Almacen de insumos 1 · Deposito y patio de maniobras 1 · Panaderia y pasteleria 1 · Taller de ceramica 1 · Tienda de artesania 1`; y (3) el mismo contrafactual con un porcentaje distinto de cero, que existe sólo para medir la frontera. **La diferencia entre (1) y (2) es lo que cuesta una firma: 4 predios, y que otros 19 pasen a depender de RT-004 en vez de esperar a D-11.** <br><br>**Y la diferencia entre (2) y (3) es una observación de frontera que este issue MIDE y NO ARREGLA, porque es D-21 y la decisión no es suya.** La llave que hoy para al padrón entero se pide en `queImpideValorizar` como **precondición** —la quinta rama, antes de tocar una cifra— y se usa **en un solo sitio**: el incremento del autovalúo, que sólo se aplica si el porcentaje no es cero. Medido con las tres corridas: con `p = 0` el autovalúo sale **idéntico** al valor del terreno en los 4 predios, o sea que **ninguna de las cuatro cifras del hecho sellado depende de la llave**; con `p ≠ 0` cambia **una sola** de las cuatro (`valorDelPredio`), y `valorTerreno`, `valorConstruccion` y `valorObras` salen iguales céntimo a céntimo. **Eso cambia dónde hay que mirar cuando se decida D-21**: un incremento que no toca ninguna de las tres cifras que esta valuación calcula es de la **base imponible** y no de la valuación —del lado de `rentas` por ADR-0024, junto al `% propiedad`—, y sin embargo hoy es lo único que impide que este sistema publique un solo importe. Queda escrito en el javadoc del censo, en las dos entradas de la tabla de decisiones y aquí; **no se mueve una línea de `src/main` por ello**. <br><br>**Lo que este issue NO cierra, con su medida.** (a) **D-11 y D-02a NO quedan cerradas, ni siquiera para 2026**: quedan *listas para cerrarse en cuanto una persona firme §1.6 de `predial-porcentaje-de-actualizacion.md`*. Hasta entonces la corrida se para en los 23. (b) **RT-004** —los 19 predios de (2), que hoy ni se ven porque D-11 los para antes—: `depreciacion.md` §3 dice que traducir el uso de la ficha al número de tabla «es criterio, no transcripción», así que inventarlo aquí sería depreciar una oficina con el porcentaje de una vivienda. (c) **El Anexo III** de la R.M. 277-2025-VIVIENDA no está transcrito y `otra_instalacion` **no tiene columna de importe**: no hay ni cuadro ni declaración de donde sacar la cifra de una obra complementaria. (d) **No se redondea nada**: D-03a y D-03b siguen abiertas, el producto viaja exacto y quien emita un documento lo redondeará con su `PoliticaDeRedondeo` — y por eso un incremento de **cero** no se aplica, porque multiplicar por uno le añadiría a la escala los decimales que nadie decidió. <br><br>**Y la fixture dejó de sembrar la llave**, que es la parte que hay que leer: `DatosDePrueba` es la copia local de un conjunto **sellado**, y `normativa` hoy no puede sellar ninguno que la traiga. Sembrarla para todas las municipalidades dejaría el padrón pareciendo valorizable, así que el contrafactual vive en **su propia municipalidad** y lo dice (`conLaLlaveSelladaElMotivoEsElDelPredio`), y `lote-de-eventos.json` vuelve a enseñar las dos valuaciones con sus cuatro valores en `null` y su llave — el día que la firma llegue, ese archivo cambia solo y **ese diff es la demostración**. `verificarArquitectura` y `verificarAislamiento`, **verdes los dos** contra PostgreSQL 16.13 + PostGIS 3.4.2 real |
| **#6 — `fiscalizacion`: el hallazgo catastral, con su acto y su evidencia (ADR-0035)** (`V9__fiscalizacion.sql` con `campania`, `candidato`, `hallazgo`, `evidencia` y `acta`; el modulo `kamayuk-catastro-fiscalizacion` —el SEGUNDO contexto acotado de este sistema—; `fiscalizacion` fuera de `ambitosAusentes()`, con lo que `NINGUN_HALLAZGO_CORRIGE_LA_FICHA` y `SOLO_LA_TRANSFERENCIA_ESCRIBE_FUERA_DE_FISCALIZACION` empiezan a mirar codigo real; **1 011 -> 1 058 pruebas**, 0 fallos) | Cuatro roturas, cada una **sola** sobre `src/main` y restaurada **por copia comparada con `cmp`**: (a) que `ConsultaDeHallazgos` dependa de un camino de escritura de la ficha —el «aplicar los hallazgos» que ADR-0035 punto 4 describe—; (b) un `UPDATE evidencia SET ruta = …` en el repositorio; (c) quitarle a `Candidato.verificadoEnCampo()` la exigencia de venir de gabinete —el atajo—; y (d) que `AreasDelPadronJdbc` devuelva lista vacia en vez de lanzar `SinCartografia` | **1, 1, 2 y —la cuarta— VERDE.** (a) sale «`ConsultaDeHallazgos` depende de `CorregirFichaDelHallazgo`: un hallazgo se INFORMA, no corrige el area. Corregirla es versionar la ficha con su observacion, y ese acto lo ejecuta una persona (ADR-0021, ADR-0035 punto 4)». **Y (a) enseño algo antes de eso**: escrita como el issue la pide —dependiendo de `TransferenciaDeFiscalizacion`— la regla que habla es la OTRA («…depende de `kamayuk.catastro.nucleo.TransferenciaDeFiscalizacion`, que no esta clasificado»), porque `ArquitecturaTestBase` recorre las reglas en un `for` y **lanza en la primera que falla**: `SOLO_LA_TRANSFERENCIA…` es la 14 y `NINGUN_HALLAZGO…` la 19, asi que la de AC 6 quedaba enmascarada y no se habria demostrado nada de ella. Aislarla exigio que el camino de escritura viviera DENTRO del modulo. (b) da «un asiento no se corrige en el sitio y la auditoria no se edita: se agrega otro registro (ADR-0006, ADR-0008): `UPDATE evidencia SET`». (c) pone **2** en rojo —el atajo en el dominio y el atajo de HTTP a PostgreSQL—. **(d) es el hallazgo de este trabajo, y su sintoma era la ausencia de sintoma**: con el defecto exacto que AC 8 existe para impedir puesto sobre `src/main`, **las 41 pruebas del modulo pasaban en VERDE**, porque las dos que hablan de la cartografia usaban un **doble** que lanza —median que el caso de uso y el borde PROPAGAN la excepcion, no quien decide lanzarla—. Se arreglo la verificacion y no se tapo: `ContrasteDeAreasJdbcTest` corre el cruce contra **PostGIS de verdad** y mide las dos respuestas, que es lo que las hace distinguibles —sin poligonos LANZA, con poligonos y sin discrepancia devuelve VACIO—; con la rotura repuesta salen **2 en rojo**. Sin ese contraste, un metodo que lanzara SIEMPRE pasaria igual. **Y las barreras que ya existian corrigieron el diseño antes de que ninguna prueba corriera**: `NINGUNA_FIRMA_DE_DOMINIO_EXPONE_BIGDECIMAL` (4 violaciones) y el escaner de redondeo (12 hallazgos) rechazaron el porcentaje de descarte y la diferencia relativa calculados en Java —«D-03b sigue abierta: el modo de redondeo se recibe en una `PoliticaDeRedondeo`»—, asi que `TasaDeDescarte` publica **cifras y ningun porcentaje** (que ademas esconde el denominador: 50 % de 20 y de 20 000 no dicen lo mismo) y la diferencia relativa la calcula **el cruce**, donde ya es el filtro y el orden — una segunda formula en Java habria podido divergir de la del `WHERE`. **Medido contra PostgreSQL 16.13 + PostGIS 3.4.2 real**: `V1..V9` aplican limpias sobre una base de cero, y la prueba de aislamiento **se puso roja sola** al llegar las cinco tablas —«acta/campania/candidato/evidencia/hallazgo: la municipalidad A debe ver sus propias filas», 5 en rojo—, lo que obligo a sembrarlas en `DatosDePrueba` con geometria de verdad en `candidato` y `hallazgo`: sin coordenadas, las cuatro columnas del marco (ADR-0034) salen nulas y un filtro por marco pasaria en verde sin comprobar nada. **Lo que NO se hizo, y esta declarado**: no se liquida, no se determina y no se emite un valor; ninguna de las cinco tablas tiene columna de importe y ninguna respuesta del contrato la tiene —hay una prueba que lo afirma sobre los seis `record` del borde—. |
| **#7 — `frentes`: el frente lineal, la mitad de `catastro` de Arbitrios** (`V10__buzon_del_territorio.sql`: `via.eje` con su marco, `longitud_estado` en `frente_predio` y la tabla `frente_derivacion`; el derivador `DerivarFrentes` del perfil `batch`, `GET /catastro/api/v1/catastro/predios/{id}/frentes` y su `POST .../confirmacion`; los tres tipos nuevos del buzon —`MANZANA_PUBLICADA`, `FRENTE_PUBLICADO`, `HALLAZGO_FIRME`— con sus dos `CHECK` cruzados reescritos; **1 131 -> 1 193 pruebas**, 0 fallos) | Nueve roturas, cada una **sola** sobre `src/main` y restaurada **por copia comparada con `cmp`**: (1) quitarle a `via` sus cuatro columnas de marco y su indice; (2) quitar el ensanchado `+ :margen` del corte; (3) escribir el `ST_DWithin` sin el marco delante; (4a) derivar la identidad del frente **al azar** y (4b) de la **identidad** del predio; (5) derivar el hallazgo firme del **contenido**; (6) devolver los dos `CHECK` cruzados de `catastro_evento` a la forma NEGATIVA de `V5`; (7) no anotar la derivacion cuando no propone nada; (8) `ON CONFLICT DO UPDATE` en el derivador; y (9) sacar una tabla del reparto de la regla 11 | **5, 3, 1, 2, 2, 2, 6 de 6, 1, 2 y —la novena— VERDE.** (1) nombra las cuatro columnas y el indice una a una. (2) es el defecto que `MargenDelMarco` existe para impedir: la via que pasa a 3 m del lote pero cuyo rectangulo no toca el suyo **desaparece sin ningun error** —«Expected size: 2 but was: 1», con el unico `FrentePropuesto` que queda—, y el predio de esquina sale con un frente en vez de dos. (3) da «ADR-0034 regla 2: … `ST_DWithin(`». **(4a) y (4b) son las dos mitades de AC 8 y hacen falta las dos, porque se rompen en direcciones opuestas**: con la identidad al azar, republicar lo que no cambio escribe un segundo evento («expected: 0 but was: 1»); con la identidad derivada del predio a secas, **confirmar una longitud deja de viajar** y `rentas` seguiria determinando sobre metros que corto una maquina. (6) pone las **6 de 6** en rojo con «violates check constraint "catastro_evento_ejercicio_ck"»: los dos `CHECK` estaban escritos «todo lo que no es X» y con seis tipos obligan a una manzana a nombrar un ejercicio que no tiene. (8) da «expected: 111.00 but was: 210.08», o sea el derivador devolviendo a PROPUESTA una cifra que una persona firmo. <br><br>**(9) es el hallazgo, y paso en VERDE**: el reparto se consulta con `getOrDefault(tabla, SISTEMA_REPLICADO)`, asi que una tabla que **falta** no da un cruce — deja de revisarse, en verde (la leccion de R-N por el otro eje). Se cierra con una guarda nueva, «ninguna tabla del esquema se queda fuera del reparto», **y no estaba vacia al escribirla**: faltaban CINCO tablas que nadie habia decidido —`catastro_evento` desde C-8 y las cuatro `normativa_*` desde P5B—, o sea que la regla 11 llevaba dos migraciones sin mirar el buzon de salida de este sistema. Con la guarda puesta, la misma rotura da «la tabla «frente_derivacion» no esta en el reparto». **Y clasificarlas obligo a medir**: declaradas «de normativa», `ValuacionRepositoryJdbc` sale con dos cruces vivos que no son cruces —esas filas estan EN ESTA BASE por diseno—; declaradas «de catastro», el marcado seria su propio escritor, que en el reparto de modulos es `normativa`. Son REPLICADAS, que es lo que significa una copia local de un conjunto sellado. <br><br>**`V5` NO se edita, y esta medido por que.** AC 5 pide corregir dos comentarios falsos suyos —dice «UUID v5 (RFC 4122 §4.3)» de un valor que es sha256 con version 8 de RFC 9562, y describe `CORRIDA_CERRADA` derivada de una «huella agregada» cuando el codigo la deriva del `corridaId`—. Cambiarle un comentario rompe el checksum de Flyway: medido sobre una base con `V1..V9` aplicadas, «Validate failed … Migration checksum mismatch for migration version 5-> Applied to database : -272056690 -> Resolved locally : -140402607», y con `validateOnMigrate` activo eso deja **toda base ya migrada sin poder migrar**. Se corrige donde si se puede: el `COMMENT ON COLUMN catastro_evento.evento_id` que `V10` reemplaza —que es el que lee un DBA— y la cabecera de `V10`, que describe la derivacion de los seis tipos; y una prueba lo fija sobre el VALOR (`identidad.version() == 8`) y no sobre el comentario. <br><br>**Cuatro cosas que las barreras corrigieron antes de que ninguna prueba mia corriera.** (a) El escaner de redondeo rechazo `setScale(2, HALF_UP)` y `MathContext(12, HALF_UP)` —«D-03a/D-03b siguen abiertas»—, y la salida no fue una exencion: el margen se divide por una potencia de diez, que es **exacto**, y la escala de la longitud la decide **la columna** (`numeric(12,2)`), asi que `proponer` devuelve la longitud *tal como quedo guardada* y la auditoria anota eso y no lo que se calculo. (b) `CatastroNoNombraUnArbitrioTest` (AC 6) se puso roja **dos veces sobre archivos mios**: el parrafo de `V10` y el javadoc de `ContratoDeEventos` que explicaban la prohibicion **escribiendo los tres nombres dentro**. (c) La prueba de aislamiento se puso roja sola al llegar `frente_derivacion` —«la municipalidad A debe ver sus propias filas»—, lo que obligo a sembrarla en `DatosDePrueba` junto con el `eje` de la via, sin el cual las cuatro columnas de marco de `via` salen nulas y un filtro por marco pasaria en verde sin comprobar nada. (d) La fixture no pudo limpiar `frente_predio` como `kamayuk_app`: «permission denied for table frente_predio». Es el privilegio bien puesto —de esa tabla cuelga un cobro y aqui no se borra (regla 4)—, y que la prueba tenga que salirse del rol de la aplicacion para limpiar es la demostracion. <br><br>**Y tres defectos vivos que este trabajo destapo sin buscarlos.** `RaizDelRepositorio` anclaba en `docs/50-api/openapi/rentas-v1.yaml`, **que en este repositorio no existe** —se quedo en `rentas` con P5C—, asi que la clase lanzaba «No se encontro la raiz» y nadie lo vio porque desde el corte **no tenia un solo llamador**: es la forma de #4 con el `.git` de un *worktree*, una guarda que no se puede correr. El mensaje de `HechoSelladoReescrito` decia «Una valuacion es un HECHO SELLADO» para **cualquier** tipo —cierto mientras solo la valuacion podia llegar ahi—, y con un segundo tipo firmado mandaba a quien atiende a mirar la corrida de valuacion por un acta de fiscalizacion; hoy el mensaje lo decide un `switch` por tipo. Y el limite de latitud de `MargenDelMarco` se escribio en 26,2° —`acos(0,897)` a ojo— y **la prueba lo puso rojo**: a 26,2° un grado de longitud mide 99 883 m, o sea menos de 100 000, y el margen se queda corto justo donde el javadoc decia que aun valia; el limite real es 26,0°. <br><br>**Medido contra PostgreSQL 16.13 + PostGIS 3.4.2 real** en el puerto 5433: `V1..V10` aplican limpias sobre una base de cero, **46 -> 47 tablas**, y con Flyway «Successfully applied 10 migrations». `./gradlew build --rerun-tasks` sumando los XML de `test` y `pruebaDeArranque`: **1 131 -> 1 193, 0 fallos**; `verificarArquitectura` y `verificarAislamiento`, verdes los dos. **Lo que NO se cierra desde aqui, con su motivo**: `frente_derivacion` —y `catastro_evento` y las cuatro `normativa_*`— hay que nombrarlas tambien en el reparto de `rentas`, `normativa` y `caja`, como T-0 hizo con `frente_predio`, y alli el hueco sigue **en verde**; ningun `CronJob` despliega los dos runners nuevos, igual que `PublicarElPadron` desde C-8; no se anade guion a `infra/carga-de-datos/` porque el derivador no carga ningun archivo y un guion sin `..._ARCHIVO` pone en rojo el censo de `infrastructure` (la leccion de #4 y #5); un hallazgo `DEJADO_SIN_EFECTO` **no viaja**, porque retirar un hecho ya publicado es otro hecho y hoy no existe; y `catastro` **no determina ni un centimo**: quien consuma el frente es #3 de `rentas`. |
| **El motivo que `catastro` escribe y `rentas` no puede guardar** (`NingunMotivoDesbordaAlConsumidorTest`: los diez motivos de `ValorizacionDelPredio` leídos del fuente y medidos contra `valuacion_predio.motivo varchar(300)` de `rentas`; tres motivos acortados — **1 207 → 1 208 pruebas**) | **No hubo que provocar el defecto: lo destapó medirlo.** El agente de `rentas#9` informó de un rojo en `IngestionDeCatastroJdbcTest` y lo atribuyó a `origin/beta`; medido en las dos direcciones, no era de `beta` sino de **#8**, que había aterrizado una hora antes: con el `lote-de-eventos.json` de #8, «ERROR: value too long for type character varying(300)»; **con el anterior, `BUILD SUCCESSFUL`** | **1 rojo, y la guarda escrita después encontró DOS MÁS.** El motivo del `% actualizacion` había crecido a **388** caracteres al cerrar #8 —de una línea a un párrafo explicando D-11— y la columna en la que `rentas` lo guarda mide 300. Acortarlo no bastaba: la guarda nueva, que lee los diez `new SinValorizar(` del fuente y mide lo que componen, salió roja nombrando **otros dos**: el de `RT-004` con **357** —que es el que reciben **19 de los 23** predios del padrón— y el de obras complementarias con **299**, a un carácter del borde y con un número interpolado por delante. Ninguno de los dos crasheaba hoy, y por eso importan: sus eventos no están en el lote de ejemplo, así que habrían llegado a producción. **Lo caro no es el rojo sino lo que habría pasado sin él**: el buzón de salida entrega y reintenta, de modo que un motivo que no cabe no se pierde con un aviso — se queda reintentando para siempre, y lo único visible del otro lado es que las valuaciones de ese predio no llegan. **La guarda lee el fuente y no ejercita las diez ramas a propósito**: una prueba por rama deja fuera la undécima que alguien añada mañana; y reserva 40 caracteres de margen para lo interpolado (`+ insumos.ejercicio()`), porque sin él un motivo de 299 literales más un ejercicio de cuatro pasaría en verde y reventaría igual. **Y lo que NO cierra queda dicho**: el 300 está escrito aquí y la columna vive en otro repositorio —dos sitios con la misma verdad, que es la forma de defecto que C-17 encontró cinco veces—, así que estrechar la columna en `rentas` seguiría sin verse desde aquí; derivarlo del clon hermano, como hace `ContratoConRentasTest` con el contrato, es el arreglo bueno y es otro trabajo. **Y del lado de `rentas` hay una asimetría viva, medida y no arreglada**: `ProyeccionDeCatastroJdbc` recorta a 400 el motivo de `catastro_evento_muerto` (su `recortar`, línea 377) y **no recorta** el de `valuacion_predio`, cuya columna es más estrecha — por eso el consumidor revienta en vez de degradar. Es de `rentas` y no se toca desde aquí |
| **#17 — los hallazgos de UN predio: la lectura que el consumidor pedía y nadie contestaba** (`GET /catastro/api/v1/fiscalizacion/predios/{predioId}/hallazgos` en `FiscalizacionCatastralController`, con `HallazgosDelPredioResource` y `HallazgoDelPredioResource`; el registro de dominio `HallazgoDelPredio` —el hallazgo, su campaña y su acta—, `hallazgosDelPredio` en `FiscalizacionRepository` con su SQL, `estaEnElPadron` en `AreasDelPadron`, y `HallazgosDelPredioJdbcTest` —7 pruebas contra PostgreSQL real—; **ninguna migración: `V11` no existe y esa es la respuesta al AC-2, medida**) | Cuatro roturas, cada una aplicada **sola** sobre `src/main` y restaurada **por copia comparada con `cmp`**: (R1) `WHERE h.predio_id = :predioId` → `WHERE h.predio_id IS NOT NULL AND :predioId IS NOT NULL`, o sea la ruta devolviendo los hallazgos de otro predio; (R2) el mismo `WHERE` por `c.predio_id` —el predio del **candidato**, que tiene su propio índice y devuelve **las mismas filas**—; (R3) que la lista vacía se conteste con `404`; y (R7) renombrar la ruta que `rentas` declara en su contrato | **6 de 7, 1 de 7, 1 de 13 y 1 de 2.** (R1) sale por seis sitios a la vez y el más claro es el predio limpio: «Expecting empty but was: [HallazgoDelPredio[hallazgo=Hallazgo[id=1502, … predioId=3002 …]]]», con **1 501** filas donde tiene que haber una. **(R2) es el hallazgo de este trabajo y da EXACTAMENTE 1**: no cambia ni una fila de la respuesta —`candidato.predio_id` y `hallazgo.predio_id` son el mismo predio— y sólo cae la prueba de plan, que imprime el plan entero: `Index Scan using candidato_predio_ix on candidato c` y **ni una mención de `hallazgo_predio_ix`**. Es la mitad que hacía falta para poder contestar el AC-2 sin adivinar. (R3) da «expected: 200 but was: 404». (R7) es la frontera: «GET /fiscalizacion/predios/{predioId}/hallazgos: este backend no publica esa operacion», con la lista de las que sí publica. <br><br>**El AC-2 se contesta midiendo, y la respuesta es que NO hace falta ninguna migración.** `V9` ya trae `hallazgo_predio_ix ON hallazgo (municipalidad_id, predio_id) WHERE predio_id IS NOT NULL`, y con **3 000 predios con su candidato y su hallazgo en cada una de dos municipalidades** —y `ANALYZE`, sin el cual el plan medido es el de una tabla vacía— el planificador lo **elige**: `Index Scan using hallazgo_predio_ix on hallazgo h` con `Index Cond: ((municipalidad_id = (current_setting('app.municipalidad_id'::text))::bigint) AND (predio_id = 1))`, o sea el predio y la condición de la política **juntos** en el `Index Cond`. Escribir un `V11` habría repetido un índice que ya existe y le habría robado el plan al que sirve, que es literalmente la lección de `zonificacion_vigencia_ix` en #4. <br><br>**Y lo que la prueba de plan exige no es la palabra «Index»**, que es la del quinto hallazgo de RLS: exige que el índice **nombrado** sea el del filtro, y le pide el SQL a la constante del repositorio y no a una copia escrita en la prueba —una copia seguiría verde el día que alguien cambiara la de producción—. Sin `enable_seqscan = off`, a propósito: forzarlo mediría si el índice es alcanzable, y lo que hay que medir es que el planificador lo elija. <br><br>**El 404 y la lista vacía se separan con una pregunta más al padrón, y sin ella no se distinguen**: `estaEnElPadron` entra en `AreasDelPadron` —el único puerto por el que este contexto lee el predio ajeno— y el borde traduce su `false` a `404`. Bajo RLS el predio de la municipalidad vecina cae ahí también, y es lo correcto: no es «prohibido», **no existe**. **Y los omisos no se filtran: no se pueden alcanzar** — `hallazgo_contraste_check` de `V9` le exige `predio_id` NULO a un `OMISO_CATASTRAL`, así que un `= :predioId` no llega a ellos; la prueba lo mide plantando uno y comprobando además que **está** en la base. <br><br>**Y lo que el AC-5 cierra del otro lado**: `HallazgosDelPredio.de(predioId)` de `rentas` era el último puerto de lectura de ese módulo que **lanzaba por no tener ruta**, y con esto ninguno de los trece de lectura lanza ya. El contrato pasa de **14 a 15** operaciones —regenerado **una sola vez**, con la de #18 dentro—, y `ContratoConRentasTest` lo comprueba desde aquí. `verificarArquitectura` y `verificarAislamiento`, **verdes los dos**; **1 208 → 1 225 pruebas**, 0 fallos, contra PostgreSQL 16.13 + PostGIS 3.4.2 real. <br><br>**Y el estado de partida traía un defecto que ninguna prueba podía ver, porque no llegaba a correr**: el trabajo heredado **nunca se había formateado** —`spotlessJavaCheck` puso `BUILD FAILED` en `ConsultaDeHallazgos` y en `FiscalizacionRepositoryJdbc` la primera vez que se corrió el build entero—, así que un PR con él habría muerto en CI antes de ejecutar una sola prueba. Se cierra con `spotlessApply`, y lo que cambia son sólo saltos de línea de javadoc: `diff` contra el árbol heredado, cuatro reflujos y ninguna línea de código |
| **#18 — el riesgo A UNA FECHA: `GET /grd/riesgo` dejaba de contestar siempre el de hoy** (`aLaFecha` en `GrdController.riesgo`, por el mismo ayudante `fechaDe(...)` que ya usaba la ruta del ITSE; seis pruebas nuevas en `GestionDeRiesgoFronteraTest` con dos fixtures que siembran zona y faja **con sus dos fechas**) | Cuatro roturas, cada una **sola** sobre `src/main` y restaurada **por copia comparada con `cmp`**: (R4) que el endpoint **declare** `aLaFecha` y pase `LocalDate.now(reloj)` —el defecto de C-1: viaja en la URL y se descarta en silencio—; (R5) quitar del `WHERE` compartido el extremo superior `AND (c.vigencia_hasta IS NULL OR c.vigencia_hasta >= :aLaFecha)`; (R6) leer las zonas con `LocalDate.now()` dejando la fecha pedida en el sobre, que es «`hayRiesgoNoMitigable` mirando las zonas de hoy»; y (R8) quitarle el `@RequestParam` al endpoint | **6 de 20, 5 de 20, 4 de 20 y 1 de 2.** (R4) contesta 200 con una respuesta plausible: se pide 2024 y vuelve «{"predioId":1,"aLaFecha":"2026-06-15","hayRiesgoNoMitigable":false,"zonas":[],"fajasMarginales":[]}», o sea la carta de peligro de hoy con la fecha de hoy dentro. (R5) es el AC-2 hecho medida y **es el defecto de #5 en el ITSE por el otro lado**: al 2025-01-01 sigue saliendo `ZR-CIERRA`, cuya vigencia terminó el 2024-12-31, y con ella `hayRiesgoNoMitigable` pasa a `true` donde hoy es `false`. **(R6) es el AC-4 exacto**: la respuesta dice «"aLaFecha":"2024-06-15","hayRiesgoNoMitigable":false» y lista `ZR-NUEVA-M`, **una zona cuya vigencia empieza en 2025** — plausible, mal, y con la decisión colgando de ella. (R8) lo dice desde la frontera: «GET /grd/riesgo: el consumidor manda «aLaFecha» y este endpoint no lo lee (lee [predioId]). Viaja en la URL y se descarta en silencio». <br><br>**Lo que este issue NO tuvo que escribir, y conviene decirlo**: la vigencia ya acotaba en las dos direcciones y en las **dos** tablas desde #5 —el `WHERE` es uno solo, `LOTE_Y_SU_MARCO`, y lo comparten `zona_riesgo` y `faja_marginal`—, y `hayRiesgoNoMitigable` **se deriva de la lista** que esa consulta devuelve. Por eso el AC-4 no se cierra con una línea de código sino con una propiedad: el dato que decide no puede diverger de las zonas publicadas porque sale de ellas, y para producir el defecto que el AC-4 describe hay que corromper también la lista (R6). Lo único que faltaba era **dejar pedir la fecha**. <br><br>**El marco sigue delante del operador espacial y no se tocó ni una letra del SQL** (AC-3): la condición de fecha ya estaba donde tiene que estar —detrás de las cuatro columnas del marco y delante del `ST_Intersects`—, así que no hay plan nuevo que medir; lo que lo sujeta es `revisarEspacial`, que sigue en verde. Y la fecha ilegible es **422 y no «hoy»**: quien pidió el 30 de febrero recibiría la respuesta de hoy creyendo que es la de febrero. <br><br>**1 208 → 1 225 pruebas** con #17, 0 fallos; `verificarArquitectura` y `verificarAislamiento` verdes, contra PostgreSQL 16.13 + PostGIS 3.4.2 real |
| **#32 — `catastro-web`: el armazón del artwork, los seis módulos del backend y el proxy que se apaga** (`frontend/`: **61 archivos**, mas su workflow, React 19 / Vite 6 / TS 5.7 con yarn y **un solo lockfile**; el marco de `CatastroV6.dc.html` —barra global de 52 px, panel variante A, pestañas de 44 px y paleta Ctrl-K— con los **seis** módulos de este backend y sus **16** hojas, y no los doce del monolito; `src/api/cliente.ts` como única puerta, con los **11** códigos de error como **array en tiempo de ejecución**; `src/simulado/` con el proxy de ADR-0010, `servidas.ts` **vacía** y el padrón **generado** de `infra/carga-de-datos/ejemplos/`; `eslint.config.mjs` con sus **6** muestras que lo violan, y cinco arneses: `reglas`, `rutas`, `mirar`, `sin-red` y `paleta`) | **Trece roturas, cada una aplicada SOLA y restaurada por copia comparada con `cmp`**: (R1) un identificador con tilde en `src/api/`; (R2) un `fetch` suelto en una pantalla; (R3) `Number(f.areaTerreno)` sobre el campo que viaja como texto; (R4) quitarle a la configuración la regla de las tildes; (R5) ensanchar la excepción del proxy para que además pudiera **llamar** a `fetch`; (R6) una ruta que el backend no tiene; (R7) el mismo camino con **otro nombre de parámetro**; (R8) un `acceso` que ningún `@RequiereAcceso` declara; (R9) una pantalla que devuelve `null`; (R10) la cifra del prototipo escrita en el panel; (R11) devolver el `import()` dinámico del proxy a un `import` **estático**; (R12) que al filtrar la paleta el foco **no** vuelva al primero; y (R13) `.nvmrc` en 21 | **2, 1, 1, 1, —, 1, 1, 1, 1, 3, —, 1 y 2 en rojo.** (R1) nombra las dos líneas con «Sin tildes ni enie en identificadores». (R2) da «Ningun fetch suelto: todo sale por solicitar() o descargar() de src/api/cliente.ts». (R6) «`/catastro/fichas-catastrales` (catastro.ts) NO existe como @RequestMapping en el backend», y (R7) la distingue de la otra: «existe en el backend con OTRO nombre de parametro: `/catastro/predios/{predioId}/frentes`» — hacen falta las dos, porque una ruta inventada y un parámetro renombrado se arreglan de manera distinta y ninguna revienta al compilar. (R9) «`#/parametros/ejercicio` — el `<main>` esta practicamente vacio», que es como falla de verdad una pantalla a medio hacer: en silencio, **sin un solo error de consola** (`mirar` seguía diciendo «16 pantallas recorridas»). (R10) pone **3 hallazgos en 1 pantalla** —`importe S/ 214,882.00`, `millares 18,412`, `decimal 214,882.00`— porque los patrones se solapan a propósito: uno solo deja pasar la mitad de las formas en que una cifra se escribe. (R12) «al filtrar, el foco se queda en una fila que nadie eligio: «Consultas · Ficha del contribuyente»», y **hay que medirlo con VARIOS resultados**: con uno solo, acotar el índice al último ya salva la situación y la comprobación pasaría con la guarda quitada. <br><br>**Las dos que pasaron en VERDE son el trabajo entero, y las dos se arreglaron en la verificación y no se taparon.** (R5) **ensanchar la excepción del proxy no ponía nada en rojo**: `reglas.mjs` comprobaba que el proxy *puede nombrar* `fetch` —es la mitad que ADR-0010 exige, porque sustituir el transporte es su razón de existir— y **no comprobaba la otra mitad**, que sigue sin poder llamarlo suelto. Importa porque un proxy que llama a `fetch` **se llama a sí mismo**: sustituyó `globalThis.fetch`, así que delegar ahí en vez de en la función que guardó es un bucle infinito, y de los que no se ven al escribirlos. Con el caso añadido, la misma rotura da «el proxy sustituye fetch, pero no lo llama — la muestra «fetch-en-una-pantalla.ts» en «src/simulado/proxy.ts» no disparo». (R11) es peor y es el hallazgo del método: la guarda medía que **no hubiera ningún activo llamado `proxy-*.js`**, y con el `import` estático **Rollup no crea un trozo aparte — mete el proxy DENTRO del paquete principal**, así que no hay ningún archivo que se llame «proxy» y la comprobación se cumplía sola, con el defecto exacto que existe para atrapar puesto encima. **Medir la ausencia de un nombre no es medir la ausencia del código.** Se cierra buscando **marcas de los datos inventados** en los activos compilados —una zona, un código de predio, el número de un certificado—, y con eso la misma rotura da «El simulado sigue DENTRO del paquete con la bandera apagada: Residencial de densidad media, 20010401001001000000000, ITSE-2026-000118». <br><br>**Y el contraste que se le añadió a esa guarda se puso rojo a la primera, sobre sí misma**: exige que las marcas **sí** aparezcan con el proxy encendido, porque una marca que no aparece nunca vuelve a hacer que la comprobación se cumpla sola. La primera terna traía «Subvaluacion en el cercado» y el arnés contestó «Con el proxy ENCENDIDO no se encuentran 1 de las 3 marcas» — el proxy sólo lee `CAMPANIA.id` (**no hay ningún endpoint que liste campañas**, así que el objeto no se serializa jamás) y el minificador se queda con el uno y tira el resto. <br><br>**Cifras, medidas en este entorno** (Node 26.7.0, yarn 1.22.22): `tsc -b --noEmit` **limpio**; `vite build` en **312 ms**, 55 módulos, `index` 271,44 kB y **`proxy` en un trozo aparte de 27,04 kB** —que es la decisión 2 de ADR-0010 hecha artefacto—; `eslint .` **0 problemas**; `reglas` **8 prohibiciones sobre 6 muestras · 3 excepciones**; `rutas` **26 rutas declaradas contra 56 del backend · 13 accesos contra 17 · 14 opciones en el catálogo**; `mirar` **16 pantallas, ninguna con errores de consola ni con el cuerpo vacío**; `sin-red` **16 pantallas con el proxy apagado y la red cortada, ninguna enseña una cifra**, y **4 activos sin ninguna de las 3 marcas**; `paleta` verde; `node` «.nvmrc dice 22 · engines pide ^20.19.0 \|\| >=22.12.0 · vite pide ^18 \|\| ^20 \|\| >=22: los tres cuadran». <br><br>**Tres defectos que destapó ejecutar, y que ninguna revisión habría visto.** (a) **La pestaña la abría el clic y no la ruta**: llegar por URL —pegar un enlace, recargar, el botón «atrás»— dibujaba la pantalla pedida con la barra de pestañas señalando otra; el título decía «Predios», el panel realzaba «Predios» y la única pestaña decía «Panel». No hay error de consola y el `<main>` está lleno, **así que `mirar` pasaba en verde**: lo vio mirar la captura, que es lo que `PORTAR.md` manda hacer. (b) **`yarn lint` salía verde en un árbol limpio y rojo después de `yarn sin-red`**, que es un orden que CI tiene: el patrón de `ignores` decía `dist/**` y `sin-red` compila en `dist-sin-red/`, así que ESLint entraba a lintar el paquete **minificado** —miles de «'document' is not defined» sobre una línea de 40 000 caracteres—. (c) La regla del `Number()` se escribió prohibiendo la **llamada** y ponía **13 sitios en rojo, 12 de ellos legítimos**: `Number(ruta.sujeto)` convierte un identificador que venía de la URL, donde todo es texto. Se acotó al **campo**, que es lo que el AC dice, porque una regla que grita en lo correcto se acaba apagando. <br><br>**Y una discrepancia del enunciado, medida y resuelta del lado del artboard**: el AC-3 pide «los valores literales del artboard» y luego lista **panel 246 px**. `CatastroV6.dc.html` escribe `flex:0 0 252px` —una sola ocurrencia, línea 1479— y **«246» no aparece en él como medida**: sus dos ocurrencias son el arancel `246.00` de la calle Tarapacá en dos tablas de datos. Los 246 px son de `sgtm/frontend`, cuyo README dice «panel de destinos de 246 px», que es justamente la paleta que el AC-3 manda **no** copiar. Se toma **252**, y queda escrito en `medidas.css`. Las otras tres alturas del AC-3 sí están literales: 52, 44 y 52. <br><br>**Lo que NO se cierra, con su motivo.** (1) **`sectores` y `calles` siguen fuera de `CatalogoDelSistema`** —que declara 14—, así que `Catastro · Territorio` contestará 403 hasta que se siembren. **La causa está medida y es del backend**: sus controladores pasan el acceso como **constante** (`SectorController.ACCESO`) y `CatalogoDelSistemaTest` busca literales de cadena con `@RequiereAcceso\s*\(\s*acceso\s*=\s*"…"`, así que **no los ve y sigue en verde**. `yarn rutas` los nombra en vez de callarlos; arreglarlo es tocar `backend/`, y este PR no lo toca. (2) **Fiscalización no publica ni el listado de campañas ni ninguna lectura de actas**: de sus once operaciones, cuatro son lecturas. Las dos pantallas lo dicen y piden el identificador a mano, en vez de dibujar una tabla contra una operación que no existe. (3) **No se conecta ni una ruta**: `servidas.ts` nace vacía a propósito, porque una entrada ahí **afirma** que el backend la contesta donde corre la aplicación, y encenderla sin los dos procesos levantados convierte cada pantalla del módulo en un fallo de conexión. (4) **No hay mapa** (ADR-0022, ADR-0037), **no hay OIDC** (ADR-0030 §3: la sesión vive en `rentas` y aquí no hay endpoint de «quién soy») y **no se publica imagen**; el `Dockerfile` existe y **no se construyó**, así que de él sólo se afirma lo que se lee. (5) El `nginx:1.31.5-alpine` **se midió contra el registro** el 2026-09-06 y no se eligió de memoria |
| **La firma de D-11 llega, y este sistema publica su primera cifra: 4 de 23** (`DatosDePrueba` siembra el `% actualizacion` del conjunto sellado, con una salida declarada para medir el contraste; `ElPadronDeDemostracionSeValorizaTest` **lee el valor del derivado del clon de `normativa`** en vez de escribirlo; `PublicacionDelPadronJdbcTest` con sus dos casos invertidos; `lote-de-eventos.json` regenerado; y `backend.yml` trayendo el segundo archivo del hermano — **1 225 pruebas**, 0 fallos, sin una sola prueba nueva) | **Dos roturas, cada una aplicada sola sobre el árbol y restaurada por copia comparada con `cmp`**: (R-D) quitar del derivado de `normativa` la fila `PORCENTAJE_DE_ACTUALIZACION`, que es el estado exacto anterior a la firma; y (R-E) que la fixture deje de sembrarla, que es el estado exacto anterior a este PR | **1 y 2 en rojo.** (R-D) el censo **se niega a contar** en vez de replegarse a un cero: «`normativa` no publica ninguna fila «PORCENTAJE_DE_ACTUALIZACION» en …/parametros-2026.csv. Si volvio a salir del derivado, el censo de este padron ya no es «4 de 23» sino «0 de 23», y lo que hay que mirar es la cabecera de predial-porcentaje-de-actualizacion.md (ADR-0007), no este archivo». (R-E) pone las dos que miden la corrida contra PostgreSQL: «expected: la rama del PREDIO … but was: “El conjunto sellado del ejercicio 2026 no trae el «% actualizacion» (D-11)”» y la del lote, que pierde la forma «con cifras». <br><br>**El valor NO se escribe en la prueba, y eso es la lección de #8 hecha código.** El censo lo lee de `parametros-2026.csv` del clon hermano, igual que ya leía el cuadro. Un cero escrito aquí habría dejado el censo diciendo «4 de 23» durante todo el intervalo en que la fila estuvo retirada —cuando la respuesta real era **0 de 23**—, que es exactamente el defecto que costó una entrega rechazada. Ahora, si la fila vuelve a salir, esto **falla nombrándola** y manda a mirar la cabecera del archivo del corpus, no este repositorio. <br><br>**Y esa línea puso roja sola una guarda que existe para esto.** `ClonesHermanosDelWorkflowTest` cazó el archivo nuevo antes de que ninguna prueba mía corriera: «el checkout de «normativa» trae [docs/…/fuentes/valores-unitarios-2026], y aqui se pide «docs/…/publicacion/parametros-2026.csv», que no cae bajo ninguno. Hay que ampliar el sparse-checkout en .github/workflows/backend.yml: sin eso el rojo solo aparece en CI, sobre un archivo que en local esta». Es el hallazgo de #8 sobre la ruta compuesta en trozos, funcionando por segunda vez y en la dirección buena. <br><br>**El contraste cambia de lado y hace falta igual.** Hasta ahora la tercera municipalidad era la que **tenía** la llave —el contrafactual— y las otras dos no; ahora es la única que **no** la tiene, y mide que sin ella la corrida vuelve a pararse en el sistema. Sin ese caso, «el motivo es el del predio» pasaría en verde aunque la llave no cambiara nada: es una **precondición**, se comprueba antes de tocar una cifra, así que su motivo tapa al del predio y sólo midiendo las dos direcciones se ve. <br><br>**Y `lote-de-eventos.json` cambió solo, que es lo que #8 dijo que pasaría**: «el dia que la firma llegue, este archivo cambia solo — y ese diff es exactamente la demostracion». Ahora enseña **las dos formas** del evento —un predio con sus cuatro cifras y otro con su motivo—, y esa es la primera vez que este repositorio publica un importe. Con `p = 0` el `valorDelPredio` sale **idéntico** al `valorTerreno` (240,00), que es la observación de frontera de D-21 medida otra vez y **no arreglada**: la llave que paraba al padrón entero no aporta un céntimo a ninguna de las cuatro cifras. **Ahora esa observación pesa más, no menos**: mientras faltó se le podía atribuir el bloqueo entero, y con ella sellada se ve que desbloquea 4 predios y ninguna cifra. **Lo que NO cierra**: RT-004 —los 19 predios, que hoy por fin se ven—, el Anexo III, y D-11 para cualquier ejercicio que no sea 2026 |
| **#33 — el módulo Catastro: las cuatro hojas del artboard, y las tres cifras que este sistema NO puede escribir** (`src/datos/catastro.ts` —rótulos, columnas, motivos y enumerados, **ni una cifra**—; `Panel` con cola de trabajo, cobertura por sector y actividad reciente; `Predios` maestro-detalle con los **dos cortes medidos** —1240 y 900 px—, buscador con rebote, orden acotado, chips y detalle de tres pestañas; `Territorio` con cabecera fija; `Valores del ejercicio` con las tres tablas, **dos de ellas pivotadas** desde filas planas; `ORDENES` en `src/api/catastro.ts`; `aSangre` y `onIr` como ranuras aditivas del armazón; **tres verificaciones nuevas** —`datos`, `impedimentos` y `vistas.mjs`— y **tres ampliadas** —`rutas`, `mirar` y `sin-red`—; el recorrido pasa de **16 destinos a 26 pantallas**) | **Siete roturas, cada una aplicada SOLA sobre el fuente y restaurada por copia comparada con `cmp`**: (R1) una cifra del prototipo en `src/datos/catastro.ts`; (R2) quitarle el `title` al botón «Anterior» impedido; (R3) **al revés**, quitar el impedimento a los **tres** sitios del fuente que lo declaran —los dos botones del paginador y el de descarga de la ficha—; (R4) una vista que apunta a un destino que el registro no declara; (R5) ofrecer «Autovalúo» y «Titular» en el selector de orden; (R6) devolver la tabla del proxy al orden en que está escrita; y (R7) sacarle a la hoja de Valores el pie que nombra su ruta | **2, 5, —, 1, 2, 1 y 3 en rojo.** (R1) da «la cadena «18,412 predios con ficha, de los que 16,884 estan conciliados» trae «18,412»» y lista las diez formas de referencia que sí puede llevar un dígito ahí. (R2) pone **5 rutas** en rojo con «button «Anterior»» —el mismo botón en las cinco vistas de Predios— y «un control apagado no se puede pulsar, asi que no hay forma de averiguar que le falta». (R4) da «la vista «catalogo vial» apunta a «catastro/vias», que no es ningun destino del registro. El armazon lo resolveria al destino inicial y la captura saldria llena, **asi que esto no se ve mirando**». (R5) nombra las **dos columnas que el artboard ofrece y el backend rechaza**: «`predios` ofrece ordenar por «autovaluo» y «CatastroRepositoryJdbc.ORDEN_CATASTRO» no lo admite. Admite: codRefCatastral, cod_ref_catastral, direccion, predioId, predio_id. No ordena raro: el servidor contesta 422 ORDEN_NO_ADMITIDO y la lista no se dibuja». (R6) «El proxy encamina «GET /catastro/predios/plano» a «/catastro/predios/{predioId}»: una ruta literal la esta tapando un patron con parametro». (R7) «3 pantalla(s) afirman algo que no han podido leer: `#/catastro/valores` … anonima … no nombra ninguna ruta del backend», más sus dos pestañas. <br><br>**(R3) es la rotura que mide la propia comprobación, y hacía falta**: con los tres `disabled` quitados, `impedimentos` recorre las 26 pantallas, encuentra **0 controles impedidos** y sale con **2** —«esta comprobacion no midio nada: pasaria en verde con el defecto exacto que existe para atrapar»—. Sin esa rama, un arnés que sólo puede pasar es la trampa que #32 midió con las marcas del paquete, por el otro eje. <br><br>**Y (R7) no hubo que provocarla la primera vez: la comprobación nueva se puso roja sola sobre código mío, en las tres hojas a sangre a la vez.** El `Servida` que nombra la ruta vivía DENTRO del pie de la tabla, o sea dentro de la rama de éxito de `Lectura`; con la red cortada, la lectura falla, se dibuja el aviso de error y **el pie no llega a existir**: justo cuando la pantalla no puede enseñar nada, deja también de decir QUÉ no pudo leer. «11 pantalla(s) afirman algo que no han podido leer … anonima». Se cierra con un `PieDeSangre` fuera de la lectura. <br><br>**Dos defectos vivos que destapó medir, y que ninguna revisión habría visto.** (a) **`parametroQueFalta` es un OBJETO y `cliente.ts` lo leía como cadena** (`typeof cuerpo.parametroQueFalta === 'string'`): el backend lo compone con `ParametroQueFalta.comoMiembro()`, que devuelve un `LinkedHashMap` con `ejercicio` siempre y `llave` sólo cuando la hay. O sea que `faltaUnaCifraNormativa` valía `false` **siempre** desde #32 y el único discriminador que separa «falta un campo de la petición», que quien atiende arregla, de «falta publicar», que no arregla nadie desde la pantalla, no llegaba a ninguna. No lo delataba nada, porque el camino de la ausencia y el de «no lo entendí» son el mismo `undefined`. Medido después del arreglo, con el ejercicio 2025 en la barra global: la hoja dice «Falta sellar el conjunto de parametros del ejercicio 2025; el servidor no nombra ninguna llave, porque lo que falta es el conjunto entero y no una fila suya». (b) **El proxy encaminaba `GET /catastro/predios/plano` a `/catastro/predios/{predioId}`** —recorría la tabla en el orden en que está escrita—, `Number('plano')` daba `NaN` y la lectura del plano catastral contestaba **404 «No hay ningún predio con ese identificador» desde #32**. En esa pantalla un 404 se lee como «aquí no hay lotes», que es exactamente lo que se espera, así que no había cómo notarlo. Se ordena de lo literal a lo parametrizado y una guarda lo comprueba **al importar el módulo**, porque un arreglo por reordenación se deshace solo en cuanto alguien añade una entrada al final. <br><br>**Y cuatro sitios donde el artboard —y el enunciado— piden algo que este sistema no sabe. Se miden, se decide con la medida y se escribe en pantalla.** (1) **El autovalúo de cada fila del padrón**: este backend no publica ninguna lectura de valuación, y su corrida no produce hoy ni un importe (`PORCENTAJE_DE_ACTUALIZACION`, D-11). Donde el artboard pone la cifra, la lista pone la manzana y el lote, y el pie dice por qué. (2) **«fichas conciliadas»**, que es el rótulo que el AC-1 pide para la barra de cobertura. **El backend lo dice él mismo, y se midió**: `ConsultaController` declara `conciliadaConRentas` y **lo REDIRIGE** —307 a `RUTA_DE_LA_CONCILIACION`, `/catastro/fichas/conciliacion`—, y `FiltroDeFichas` escribe por qué no está en su filtro: responderlo leyendo la tabla de `rentas` sería el acoplamiento que ARQ-01 §4 evita, «con el agravante de ser invisible para Spring Modulith»; desde #344 lo sirve `rentas`. Así que ninguna lectura de este sistema puede contar conciliadas, la barra mide `fichado` sobre los predios ACTIVOS del sector —que es lo que `SectorConConteos` cuenta— y el pie lo dice. (3) **Las vías colgando de un sector**: `ViaController` **rechaza** el filtro `sector` con un 422 explícito —la tabla no guarda el sector—, así que el árbol tiene un solo nodo «Catálogo vial» en vez de uno por sector. Y una cuarta, de la misma familia: el AC-2 pide «insignias con los cuatro tonos y sus etiquetas literales», y las cuatro etiquetas del artboard —«Conciliada», «Sin conciliar», «En verificación», «Con licencia de obra»— son estados de `rentas` y de licencias que **no viajan en ningún `record` de este backend**. Los cuatro tonos sí se usan, con las etiquetas que este sistema publica: `ACTIVO`/`DADO_DE_BAJA`, `Fichado`/`Sin ficha`, `PROPUESTA`/`CONFIRMADA` y el tipo de ficha. <br><br>**Y el AC-4 resultó equivocado al medirlo, en dos mitades.** Dice que las tres tablas «pueden contestar con el miembro extra `parametroQueFalta` {ejercicio, llave}» y que la pantalla «tiene que decir qué llave falta —hoy `PORCENTAJE_DE_ACTUALIZACION`—». Medido en el backend: (i) la respuesta **200 es una lista pelada**, sin sobre; el miembro vive en el cuerpo de error de un **404 `NO_ENCONTRADO`**; y (ii) esas tres rutas **nunca nombran una llave**, porque sus controladores sólo atrapan `LectorDeParametros.EjercicioSinSellar`, cuyo `llave()` es `Optional.empty()`. `PORCENTAJE_DE_ACTUALIZACION` es la llave que para la **valorización**, que no tiene endpoint y no se lee desde aquí. Así que la hoja dice lo que el servidor dice —falta el conjunto del ejercicio N— y **no inventa la llave**; la D-11 se nombra donde sí es cierta: en el hueco del autovalúo. <br><br>**Cifras, medidas en este entorno** (Node 26.7.0, yarn 1.22.22, Chromium 153.0.8010.12): `tsc -b --noEmit` **limpio**; `eslint .` **0 problemas**; `vite build` en **347 ms**, `index` 304,23 kB y **`proxy` en un trozo aparte de 27,70 kB**; `reglas` **8 prohibiciones sobre 6 muestras · 3 excepciones**; `datos` **1 archivo y 103 cadenas, ni una cifra que no sea una referencia** —once formas admitidas, de `ADR-0024` a un estado HTTP del catálogo—; `rutas` **26 rutas declaradas contra 57 del backend · 13 accesos contra 17 · 14 opciones en el catálogo · 18 campos de orden contra 7 listas blancas**; `mirar` **26 pantallas** —16 destinos y 10 vistas—, ninguna con errores de consola ni con el cuerpo vacío; `impedimentos` **26 pantallas · 11 controles impedidos, todos con su motivo**; `sin-red` **26 pantallas con el proxy apagado y la red cortada, ninguna enseña una cifra y todas nombran su ruta**, con 4 activos y ninguna de las 3 marcas; `paleta` verde; `node` verde. <br><br>**Lo que NO se cierra, con su motivo.** (1) **No se da de alta ninguna ficha**: el asistente es #34, y por eso el estado vacío de la lista **no lleva el botón «Registrar el predio»** que el artboard dibuja — un botón que no hace nada es peor que no tenerlo. (2) **No se pinta ningún mapa** (ADR-0022, ADR-0037). (3) **`servidas.ts` sigue vacía**: no se conecta ni una ruta, porque una entrada ahí afirma que el backend la contesta donde corre la aplicación. (4) **Los chips y el orden no cambian nada contra el proxy**, que no filtra, no ordena y no pagina a propósito (ADR-0010, decisión 4); las pantallas lo dicen en vez de disimularlo. (5) **`sectores` y `calles` siguen fuera de `CatalogoDelSistema`**, así que Territorio contestará 403 hasta que se siembren; es del `backend/` y este PR no lo toca. (6) **El aviso de `parametroQueFalta` no lo cubre ningún arnés**: el ejercicio es estado de la barra global y no viaja en el hash, así que `mirar` no puede llegar a esa rama por URL; se midió a mano con Playwright y queda declarado |
| **#34 — el alta de ficha: el asistente de seis pasos, y el código que se compone en un solo sitio** (`src/datos/alta.ts` —los seis pasos con sus rótulos, sus notas y sus ayudas, **44 campos de paso** y ni una cifra—; `src/modulos/catastro/AltaDeFicha.tsx` —el asistente, el código por ocho tramos, los pendientes por paso, el resumen y los tres desenlaces—; `COMPOSICION_DEL_CODIGO`, `PeticionDeAlta` e `inscribirFicha` en `src/api/catastro.ts`; las **cuatro** rutas del alta en el proxy; **dos guardas nuevas** en `yarn rutas` y **tres vistas nuevas** en `vistas.mjs`) | Nueve roturas, cada una aplicada **sola** sobre `src/` o sobre el arnés y restaurada **por copia comparada con `cmp`**: (R1) el tramo `manzana` declara 2 dígitos en vez de 3; (R2) el tramo `unidad` desaparece del formulario; (R3) `documentoOrigen` se escribe `documentoDeOrigen` **en el tipo, en la lista y en el compositor**, o sea sin síntoma de compilación; (R4) un campo se añade al tipo `PeticionDeAlta` y no a `CAMPOS_DEL_ALTA`; (R5) el primario apagado se queda sin `title`; (R6) el arnés deja de encontrar el `record` y la composición que compara; (R7) **con R5 puesto**, se quita la vista del último paso; (R8) el 409 se enseña y no se recuerda; y (R9) los tres desenlaces se enseñan con un aviso genérico | **1, 1, 1, 1, 1, 2 y —la séptima— VERDE.** (R1) da «el tramo «manzana» del codigo declara 2 digito(s) y los del backend que cubre —manzana— suman 3 … `componer` rellena de ceros por el sitio equivocado y el codigo sale desalineado: sigue siendo de digitos y del largo correcto, asi que nada lo rechaza». (R2), la otra mitad, da «el tramo «unidad» de «ComposicionCatastral.DEL_MANUAL» no lo teclea ningun campo del codigo». **Hacen falta las dos y se rompen en direcciones opuestas**: la primera desalinea el código, la segunda deja un tramo que nadie puede escribir, y ninguna comprobación de longitud total ve ninguna de las dos —los ocho tramos siguen sumando 23 en la primera—. **(R3) es el hallazgo del método**: propagada también al compositor, `tsc` y `eslint` salen **VERDES** —el tipo y la lista cuadran entre sí, y no hay con qué compararlos— y el único que la ve es el arnés, con «documentoDeOrigen» no es un campo de «FichaController.PeticionDeAlta» … el cuerpo es una lista blanca: lo que no esta se descarta aunque llegue en el JSON, y el servidor contesta que la ficha se creo. El dato no acaba en ningun sitio y nada lo dice». (R4) la caza `tsc` con la aserción de compilación que ata el tipo a la lista: «Type 'boolean' is not assignable to type '["falta en CAMPOS_DEL_ALTA", "frontis"]'» — sin ella, la lista que el arnés compara se quedaría vieja en silencio y el arnés mediría 19 de 20 campos, en verde. (R5) pone `impedimentos` en rojo con «#/catastro/predios/nuevo?paso=verif   button «Registrar la ficha»» bajo «1 control(es) impedido(s) sin decir por que». (R6) da **2**, uno por guarda: «No se pudo leer «ComposicionCatastral.DEL_MANUAL» … esta parte se estaria cumpliendo sola. Falla en vez de saltarsela» y «No se pudo leer el `record` «FichaController.PeticionDeAlta» … es una lista blanca, y un campo que el `record` no tenga se descarta EN SILENCIO». <br><br>**(R7) es la lección de #33 medida otra vez, y pasó en VERDE.** Con el defecto de R5 **puesto** sobre `src/main`, quitar la vista `?paso=verif` deja `impedimentos` en «28 pantallas recorridas · 16 control(es) impedido(s) · todos dicen por que lo estan», **código de salida 0**. El primario del alta **solo existe en el último paso** —en los cinco anteriores el botón dice «Continuar» y no está apagado nunca—, así que sin esa vista el arnés informa en verde sobre el único control que este issue añade. Con ella, 29 pantallas y 19 impedidos. <br><br>**(R8) y (R9) no las caza ningún arnés, y por eso se midieron conduciendo el navegador**, con las cuatro corridas escritas: (R8) tras el 409 el primario vuelve a estar disponible —«apagado: false · title: null» donde el árbol sano dice «apagado: true · title: “Ese codigo ya esta asignado a otro predio…”»—, o sea que se puede volver a mandar exactamente lo que el servidor acaba de rechazar; y (R9) el 409 y el 404 pasan a decir **lo mismo** —«No se pudo registrar la ficha» los dos, sin el «qué hacer»—, que es el defecto que AC-6 existe para impedir: los tres se arreglan con tres acciones distintas y uno de ellos no se arregla en esta pantalla. <br><br>**Y tres cosas que el issue decía y la medida corrigió.** (1) **`direccion` es obligatoria y el AC-6 no la lista**: `FichaController.predioDeclarado` la lee con `exigir(peticion.direccion(), "direccion")`, así que sin ella el alta es un 422 en el primer campo que el servidor mira. Se compone con la vía del catálogo y el número municipal —igual que el artboard compone el título de la ficha— y se enseña en el resumen. (2) **El artboard llama al `origen` «Fuente del dato» y ofrece tres valores de los que solo uno existe**: `OrigenDeLaFicha` es `DECLARACION_JURADA`, `FISCALIZACION`, `RESOLUCION` y `MIGRACION`, y «INSPECCIÓN DE CAMPO» o «RESTITUCIÓN FOTOGRAMÉTRICA» son 422 «el campo 'origen' no admite el valor». Se ofrecen los cuatro del enumerado. (3) **El artboard escribe cinco categorías constructivas y el `record` admite siete** —le añade baños e instalaciones—, que es el mismo desajuste que `MOTIVOS.sietePartidas` ya nombraba en la matriz de valores unitarios. <br><br>**Y el número que es el entregable, contado y no estimado.** Los **48** campos del artboard están todos: **39** como campos de paso y los **9** restantes en las tres piezas propias que el asistente añade —`tipoVia` y `calle` en la búsqueda de la vía del catálogo, los cuatro linderos en su rejilla, y `matPredominante`, `estadoConserv` y `antiguedad` como columnas **por piso**, que es donde el `record` las pone—; más **5** que el contrato exige y el artboard no pide: la clase de ficha, el tipo de predio, la denominación, el documento de origen y la observación de RNF-052. De los **44** campos de paso que quedan, **13 viajan y 31 no**. El cuerpo del alta es una **lista blanca** —lo que no está en el `record` se descarta **aunque llegue en el JSON**, con la ficha creada igual—, así que el modo de fallo no es un error sino un campo que el técnico rellena y que no acaba en ningún sitio. Cada campo declara `viaja` con el nombre del campo del contrato que llena o `null` con su motivo medido; el resumen de «lo que se va a registrar» se compone **recorriendo la petición ya armada** y no una lista escrita al lado, que es lo único que impide que diga una cosa y el cuerpo lleve otra; y **ningún campo que no viaja puede bloquear el alta**. `yarn rutas` cuenta los que faltan al revés y los nombra: `economico` y `bienesComunes`, que ningún paso del artboard recoge. <br><br>**Cifras, medidas en el mismo entorno y contra la línea base del árbol limpio**: `tsc` y `eslint` en verde los dos; `vite build` **304,20 → 345,92 kB** el paquete y **27,70 → 29,41 kB** el trozo del proxy; `yarn rutas` **26 → 30 rutas declaradas** contra las 57 del backend, más **8 tramos del código contra 10** y **20 campos del alta contra 22**, que antes no se medían; `yarn datos` **1 → 2 archivos** y **103 → 425 cadenas**, ni una cifra; `yarn mirar`, `yarn sin-red` e `impedimentos` **26 → 29 pantallas**, y los impedidos **11 → 19**; `yarn paleta`, `yarn reglas` (8 prohibiciones sobre 6 muestras) y `yarn node`, iguales. <br><br>**Lo que NO se cierra, con su motivo medido.** (a) **El borrador no se guarda en ningún sitio**: no hay ruta de borrador, y un `localStorage` sería una ficha a medio hacer que sobrevive a la sesión y a la municipalidad en el navegador de quien la dejó; el pie lo dice en vez de fingir el «se guarda al avanzar» del artboard. (b) **El duplicado no se comprueba antes de preguntar**: el artboard lo sabe porque compara con una constante suya, y aquí el padrón es del servidor — se recuerda el código que el 409 rechazó y el primario no vuelve a estar disponible mientras siga siendo ese, que es lo que AC-2 pide sin inventar una lectura que no valdría para el instante siguiente. (c) **`economico` y `bienesComunes` no se mandan**: el artboard no dibuja ningún paso que recoja actividades ni participaciones, y mandar un bloque que no es el de la clase de ficha es 422; `yarn rutas` los nombra en un aviso en vez de callarlos. (d) **El CUC del SNCP se pide y no viaja**: ADR-0036 le da columna propia desde `V6` y `PeticionDeAlta` no declara ningún campo para él. (e) **El PR no se abre desde aquí**: la rama queda confirmada en local |
| **#40 — el `Dockerfile` del frontend deja de no tener quien lo publique, y una guarda lo ata al disco** (`publicar-imagenes.yml`: `contexto` y `archivo` pasan a ser columnas de la matriz y entra la **tercera** imagen, `kamayuk-catastro-web`, con la etapa `interfaz` y contexto `frontend/`; el trabajo `comprobar` pregunta ahora por las **tres**; `docs/00-gobierno/verificar-quien-publica-cada-imagen.mjs` y su autoprueba de **12 muestras**, en su propio flujo `imagenes.yml` **sin filtro `paths`**) | **Tres roturas sobre el workflow de verdad**, cada una **sola** y restaurada **por copia comparada con `cmp`** —«RESTAURADO IGUAL» las tres—: (R1) quitar el frontend de la matriz; (R2) publicarlo desde una etapa que su `Dockerfile` no declara; y (R3) publicarlo sin que `comprobar` le pregunte al registro. **Más el CONTRASTE que el issue pide**: apartar el `Dockerfile` **y** su entrada a la vez. | **1, 1 y 1 en rojo, y el contraste en VERDE.** (R1) da «`frontend/Dockerfile` esta en el arbol y ninguna entrada de la matriz lo publica … la imagen simplemente no llega a ningun ambiente, y lo unico observable es que el servicio no esta». (R2) da «…desde la etapa «produccion» de «frontend/Dockerfile», y ese archivo declara «construccion», «interfaz»» — y **hace falta, porque un `target` que no existe NO falla**: Docker construye la última etapa, que puede ser otra imagen entera, y la publica con el nombre de ésta. (R3) da «se publica y el trabajo `comprobar` no le pregunta al registro si su etiqueta se puede pedir». **Y el contraste sale en verde con «1 Dockerfile(s) y 2 entrada(s) de matriz»**, que es lo que demuestra que la guarda cuenta **archivos del disco** y no una fórmula: si contara tres, apartar el archivo la habría puesto roja sobre un repositorio correcto. <br><br>**La autoprueba son 12 muestras y tres de ellas son contrastes**: sin un solo `Dockerfile`, con la matriz cambiada de forma y con el bucle del comprobador cambiado de forma, la guarda sale con **exit 2 diciendo que no midió** en vez de con el verde de quien se quedó sin sujeto — que es la lección que #32, #33 y #34 dejaron tres veces seguidas. <br><br>**Y medir para escribir la fila destapó que otra fila era falsa**: `CLAUDE.md` decía «Su imagen `ghcr.io/hneyra/kamayuk-catastro` **NO existe**», y contra el registro para el `sha` de `main` (`7e0c302`) contesta **200**; `kamayuk-catastro-migrador` también. Lo era desde D. La tercera contesta **403**, que el propio workflow documenta como «no permite concluir nada» y no como «no existe». <br><br>**Lo que NO se cierra, con su medida**: el **AC-3** —que `infrastructure` conozca la imagen y monte el `nginx.conf` que se revisa— queda fuera. `infra/componentes/Aplicacion.ts:222` de su `origin/main` despliega `sgtm-interfaz`, la del monolito, y `fuentes.ts:297` lee `frontend/nginx.conf` **de aquel repositorio**, no de éste: son dos archivos con la misma verdad y la guarda `nginxDelCluster` vigila el que no se despliega. No se toca porque en ese clon hay trabajo sin confirmar que **borra ese mismo archivo** (rama `el-monolito-sale-del-sistema`, `Aplicacion.ts` en estado `D`), así que hacerlo ahora obligaría a rehacer uno de los dos. |
| **#41 — la revisión independiente de `catastro-web`: diez defectos en el armazón que nadie había mirado, y los tres primeros eran guardas que no medían nada** (`reglas.mjs` gana la dirección que faltaba —toda regla tiene su muestra— y su muestra de `parseFloat`; `mirar.mjs` y `sin-red.mjs`, la guarda del recorrido vacío; el guión `node` pasa a `motor` y `node.mjs` vigila que ninguno se llame como un comando de yarn; `rutas.mjs` ata `CODIGOS_DE_ERROR` a `CodigoDeError.java` y el tipo `Ficha` a `FichaResource`, con `CODIGOS_QUE_ANADE_EL_CLIENTE` y `CAMPOS_DE_FICHA_QUE_NO_SE_LEEN` como declaraciones leídas y no como prosa; `titulosDeError` pasa a `Record<CodigoDeError, string>` completo; `descodificar()` en `ruta.ts`; `motivoCorto` y `tituloDeError` en el sistema de diseño, con `Catastro.motivoDelFallo` y el `ElFallo` del alta delegando en ellos; `.dockerignore` deja fuera los `.env*`; y `nginx.conf` sirve sus tres cabeceras **en los dos bloques**. **67 → 68 archivos** de `frontend/`) | **Ocho roturas provocadas, cada una aplicada sola y restaurada por copia comparada con `cmp`** —R1 una prohibición nueva sin muestra, R2 borrar el caso de `parseFloat`, R3a quitarle `METODO_NO_ADMITIDO` al cliente, R3b quitarle el motivo a `SIN_RESPUESTA`, R4 que la lectura del enumerado no encuentre nada, R5 quitar `instalaciones` de los huecos, R6 un campo en `Ficha` sin la lista, R7 un código sin su título—, **más ocho que no hubo que provocar porque el defecto era el estado de partida** y se midieron antes y después: `yarn node`, los dos recorridos vacíos, el `.dockerignore`, las cabeceras de nginx, el hash mal formado, el panel y el alta | **Los tres hallazgos que más pesan son guardas en verde que no comprobaban nada, y ninguna se ve mirando.** <br><br>**(1) El arnés de la versión de Node no corría, y llevaba así desde que existe.** `package.json` declaraba el guión con el nombre `node` y `frontend.yml` lo llamaba `yarn node`, que **no es el guión**: es el comando `node` de yarn 1, que arranca Node y sale. Medido: `yarn node` imprime «yarn node v1.22.22 · Done in 0.04s» y **sale con 0** sin ejecutar una línea del arnés; `yarn run node` sí lo ejecuta e imprime «.nvmrc dice 22 · engines pide … los tres cuadran». Un paso de CI rápido y verde es indistinguible de uno que mide. El arreglo no es cambiar el flujo —eso se vuelve a escribir igual—: el guión pasa a `motor` y el arnés vigila que **ningún guión se llame como un comando de yarn**, con la lista **preguntada a yarn** (`yarn help`, 43 comandos, y `test` no está entre ellos a propósito porque yarn lo reenvía) en vez de copiada. Devolverlo a `node` da **2** en rojo. <br><br>**(2) `reglas.mjs` exigía que toda muestra tuviera regla y no lo contrario**, que es la mitad que importa: una muestra huérfana es un archivo de más, y una regla sin muestra es una prohibición que nadie ha comprobado que muerda. Medido: **borrar de `eslint.config.mjs` la prohibición de `parseFloat` entera dejaba `yarn reglas` en VERDE** con el mismo mensaje —«8 prohibiciones muerden sobre sus 6 muestras»— y `yarn lint` también, porque ningún fuente la viola hoy. Ahora las prohibiciones se leen de la configuración de verdad —los bloques propios son los que declaran `files` y no traen `name`; los presets o no acotan archivos o vienen con el suyo— y salieron **7**, de las que `parseFloat` era la única sin muestra. Una prohibición nueva sin muestra da 1 en rojo; borrar el caso da **2**, uno por cada dirección. <br><br>**(3) Un recorrido que no mira ni una pantalla informaba en verde.** `node verificaciones/mirar.mjs modulo-que-no-existe` imprimía «0 pantallas recorridas» y a continuación «ninguna con errores de consola ni con el cuerpo vacío», **saliendo con 0 y sin abrir una sola página** —sin necesitar siquiera una vista previa levantada—. Lo mismo `sin-red.mjs`, cuyas tres afirmaciones son ciertas sobre el conjunto vacío. `impedimentos.mjs` ya se protegía así; ahora las tres. Con la guarda, la misma orden sale con **2** en los dos. <br><br>**(4) AC-1, y la relación no era igualdad: 12 contra 11.** `CODIGOS_DE_ERROR` del cliente y `CodigoDeError.java` son dos sitios con la misma verdad y nada los ataba. La guarda no exige conjuntos iguales —`SIN_RESPUESTA` no lo produce ningún servidor y ponerlo rojo sería gritar en lo correcto—: exige que **los once del backend estén todos en el cliente y en su orden**, y que lo que el cliente añade esté **declarado con su motivo**. Sin ella, el backend añade un código, `esCodigoConocido` lo rechaza, `porEstado` lo degrada al del estado HTTP, y la pantalla sale con el título de OTRO código y con «Reintentar» puesto o quitado por el motivo equivocado — sin un error de consola ni un tipo que se queje. Quitarle `METODO_NO_ADMITIDO` al cliente da **2** —el código y el orden—; quitar el motivo de `SIN_RESPUESTA`, 1; y que la lectura del enumerado no encuentre nada, 1 con «esta parte se estaría cumpliendo sola». Y `titulosDeError` pasa de `Partial<Record<string, string>>` a `Record<CodigoDeError, string>`: quitar un título ya no compila («Property 'MARCO_CON_DEMASIADOS_LOTES' is missing … but required in type 'Record<…>'»), que es lo que impide que un código nuevo caiga al «No se pudo leer» genérico. <br><br>**(5) La otra mitad del AC-1, medida: `FichaResource` publica 22 componentes y el tipo `Ficha` declara 17.** Los cinco que faltan **no dan ningún error** —el JSON llega entero y `tsc` sólo se queja al leerlos—, así que el hueco es invisible hasta que estorba y entonces se cierra con un `as`. **`instalaciones` es el que pesa**: no es `@Nullable`, se construye siempre y viaja en toda respuesta de ficha, y **el alta ya las manda** — hoy se teclea una obra complementaria, el servidor la guarda y la devuelve, y la pantalla no puede leerla. Se declara el HUECO con su motivo y la guarda exige que cada componente esté o en el tipo o en esa lista; quitar `instalaciones` de ella da 1 en rojo. Cerrarlo de verdad es **#46**, porque arrastra que el Anexo III no está transcrito y `otra_instalacion` no tiene columna de importe. <br><br>**(6) Un hash mal formado dejaba la interfaz EN BLANCO.** `decodeURIComponent` lanza `URIError` ante un porcentaje suelto, y eso llega a la URL solo. Medido en Chromium sobre la vista previa de producción, con las dos formas de llegar: **en frío** —pestaña nueva sobre `#/catastro/predios/%E0%A4%A`— el `useState` de `App` lanza al construir el estado inicial, React no monta nada y `#raiz` se queda con **0 caracteres** (67 802 con la ruta sana) más un «PAGEERROR: URI malformed»; **en caliente** —pegar una dirección, el botón «atrás»— lanza dentro del oyente de `hashchange`, el estado no se actualiza y **la pantalla sigue enseñando la ruta anterior mientras la barra dice otra**. Ninguna vista del recorrido lleva un hash malo, así que ningún arnés lo veía. Con `descodificar()`, los dos casos dibujan 60 158 y 41 848 caracteres sin un error. <br><br>**(7) La imagen podía hornear un token, y el `Dockerfile` afirmaba lo contrario.** Vite carga `.env.local` y resuelve `import.meta.env` **al compilar**; ese archivo estaba en `.gitignore` (`*.local`) y **no** en `.dockerignore`, que es la peor de las dos combinaciones —git no lo ve, así que nadie repara en él, y `COPY . .` sí lo copia—. Medido con `docker build --target construccion` y un `.env.local` con `VITE_CATASTRO_TOKEN=…`: con el `.dockerignore` anterior el token sale **en claro** dentro de `dist/assets/index-*.js` —**1 coincidencia** con `grep -ro`—, y ese archivo es el que se publica en `ghcr.io`; con el nuevo, el archivo no llega al contexto y son **0**. <br><br>**(8) La interfaz no servía ni una cabecera de seguridad, y el arreglo obvio no hace nada.** Medido con `nginx:1.31.5-alpine` sobre `/`, `/assets/index-*.js` y una ruta del cliente: ninguna traía `X-Content-Type-Options`, `X-Frame-Options` ni `Referrer-Policy`, con el token de sesión en `localStorage` del mismo origen. **Y escribirlas a nivel de `server` las apaga**: `add_header` no se hereda a un `location` que declara los suyos, aquí hay dos que los declaran, y `location /` sirve por redirección interna a `/index.html` — medido, **cero de tres** en las dos rutas. Van repetidas en cada bloque, con el motivo escrito arriba para quien quiera «no repetirlas». No se añade CSP y se dice: los estilos van en línea y una política útil exige decidir `style-src` y medirla. <br><br>**(9) AC-4: las distinciones del error llegaban al `Fallo` y a ninguna otra superficie.** Medido en el navegador con los errores inyectados por `page.route`: de las **29** lecturas de `src/modulos`, **26 pasan por `Lectura` → `Fallo` y ahí las cuatro llegan intactas** —72 renders, 12 destinos × 6 escenarios, «Reintentar» **0** con 403 y con los tres 422, **1 a 3** con 500 y con la red cortada—. Las que no pasan colapsan. `motivoDelFallo` de `Catastro.tsx` devolvía `error.mensaje` a secas, y con el **mismo mensaje** un 500 con incidencia, un 403 y un 422 con `parametroQueFalta` salían **byte a byte iguales** en las **siete** superficies del panel —las cuatro tarjetas y las tres filas de la cola—: quien mira no podía saber si llamar a quien atiende o si lo que falta es sellar un conjunto en `normativa`, que es la condición permanente de este sistema. Con `motivoCorto` —que vive junto a `titulosDeError`, para que los doce rótulos sigan en un solo sitio— las 7 se distinguen y las 4 combinaciones son 4 de 4 distintas. <br><br>**(10) Y el alta mandaba a corregir un campo que no existe.** `ElFallo` decidía por `codigo`, así que un 422 con `parametroQueFalta` entraba por la rama `VALIDACION` y añadía «hay un campo que corregir **en este mismo asistente**. El servidor dice cuál» — **falso**, y manda a repasar seis pasos por algo que se sella en otro repositorio. Es la inversión exacta de la regla que `cliente.ts` escribe con todas las letras: «si está, no se arregla aquí — esa es toda la regla, y por eso se mira su PRESENCIA antes que su contenido». Vuelto a medir con cinco códigos, la frase falsa **no sale en ninguno**, el 422 con llave pinta `LoQueFalta`, y 401/403/500 dejan de salir los tres como «No se pudo registrar la ficha». **Y el contraste que hace que la medida valga**: un 422 **sin** `parametroQueFalta` sigue diciendo «El servidor rechazó un campo» con su «Hay que corregir «Documento de origen», en Identificación» — la rama de la petición sigue viva y sólo se desvió la que no le tocaba. <br><br>**Cifras, medidas en el mismo entorno** (Node v26.7.0, yarn 1.22.22, Chromium de `playwright-core`): `tsc` y `eslint` **verdes antes y después**; `vite build` **345,92 → 346,35 kB** el paquete —**0,43 kB**, que son los doce rótulos llegando a las siete superficies del panel—, **29,41 kB** el trozo del proxy y **3,01 kB** el CSS, iguales; `yarn reglas` **8 prohibiciones sobre 6 muestras → 9 sobre 7**, más «7 prohibiciones de eslint.config.mjs, todas con muestra», que antes no se medía; `yarn rutas` suma **22 componentes de la ficha contra 17 del tipo (+5 huecos declarados)** y **12 códigos de error contra 11 (+1 que el cliente añade y declara)**; `yarn motor` —que **no corría**— dice «13 guiones, ninguno se llama como uno de los 43 comandos de yarn»; `yarn datos` 2 archivos y 425 cadenas, `yarn mirar` 29 pantallas, `yarn impedimentos` 29 y 19 controles, `yarn paleta` y `yarn sin-red` (29 pantallas, 4 activos, ninguna de las 3 marcas) **iguales**. <br><br>**Lo que la revisión NO cierra, con su issue.** **#46** — los cinco componentes de `FichaResource` que el tipo no declara, y con ellos la comprobación general de los ~40 tipos escritos a mano contra sus `record`: la revisión los comparó campo por campo a mano y no encontró más desajustes de nombre ni de anulabilidad, y los cuatro tipos que viajan como texto (`Dinero`, `Alicuota`, `Porcentaje`, `AreaM2`) están los 8 como `string`, pero **eso es una medida de un día y no una guarda**. **#47** — nada vigila `nginx.conf` ni `Dockerfile`, así que los defectos (7) y (8) pueden volver en silencio; y la trampa de escribirlo está a la vista: una guarda que compruebe que la cabecera *aparece en el archivo* pasa en verde con las tres a nivel de `server`, que es el defecto exacto. **#48** — `EJERCICIOS` son cuatro literales congelados en `Shell.tsx`: el 1 de enero de 2027 nadie puede elegir 2027, la interfaz enseña los cuadros de 2026 y **parece correcta**. **#49** — las tres superficies de error que quedan sin pasar por `Fallo`: `vias.error` que **nadie lee** en «Valores del ejercicio» —medido, un 403 sobre `/catastro/vias` deja la tabla entera con «Via 1», «Via 2» y «—», sin título, sin mensaje y sin botón, que es un dato degradado indistinguible de uno bueno—; el «Reintentar» del panel, que con las cuatro lecturas caídas ofrece **2** botones y cada uno re-pide **una sola** ruta; y el `catch` de `Consultas.bajar`, que destruye el `ErrorDeApi` y deja los cuatro escenarios diciendo lo mismo byte a byte. <br><br>**Y lo que no se miró, que el AC-5 exige decir**: el *design system* y la paleta (los excluye el propio issue); `Shell.tsx` y las seis pantallas como diseño —lo que se revisó de ellas es cómo tratan el error—; la accesibilidad más allá de lo que `paleta` e `impedimentos` ya miden; el rendimiento; y el **contenido** del proxy de datos —los 23 predios inventados— frente a lo que el backend devolvería, que sólo se puede contrastar con los dos procesos levantados. |
| **#47 — el noveno arnes: los dos archivos que deciden COMO SE SIRVE, medidos levantando nginx de verdad** (`frontend/verificaciones/imagen.mjs` y `yarn imagen`; el paso nuevo en el trabajo `verificar` de `frontend.yml`; **8 -> 9 arneses**, y `yarn motor` pasa de **13 a 14 guiones**) | **Diez roturas, cada una aplicada SOLA sobre el archivo de produccion y restaurada por copia comparada con `cmp`** —«cmp: identico» las diez—: (R1) **las tres cabeceras de los dos `location` juntadas en un unico bloque `server`**, que es lo que haria alguien «para no repetirlas»; (R2) quitar `.env.*` de `.dockerignore`; (R3) quitar `Referrer-Policy` de UN bloque; (R4) quitarle el `always` a las tres de `/assets/`; (R5) ningun `location` con `add_header` propio; (R6) el upstream renombrado, o sea nginx que no levanta; (R7) `COPY .env.local ./` en el `Dockerfile`; (R8) `ARG VITE_CATASTRO_TOKEN=`; (R9) un `Content-Security-Policy` escrito en un solo bloque; y (R10) un `location ~ \.woff2$` con cabeceras propias | **15, VERDE->1, 2, 3, 2, 2, 1, 1, 1 y 2 en rojo** (los «2» son salidas con codigo **2**, que es «no midio», no «esta mal»). <br><br>**(R1) es la trampa que el issue nombra, y sale 15 de 15 en rojo**: las cinco rutas sondeadas por las tres cabeceras, cada una con «`add_header` no se hereda: nginx toma el conjunto del nivel mas interno que declare alguno y descarta el de arriba entero, asi que escribirla mas arriba no la enciende». **Y una guarda de texto habria pasado en verde sobre ese mismo archivo, medido con `grep`**: `grep -q` encuentra las tres cabeceras **antes y despues** de la rotura, asi que «la cabecera aparece en el archivo» sigue siendo cierto con el defecto puesto. Lo que cambia es el conteo —cada una pasa de **2 a 1**, y las directivas `add_header` de **8 a 5**— y sobre todo **donde**, que es lo unico que decide si llega; y eso solo se ve preguntando por HTTP. Por eso el arnes no lee el archivo para decidir: copia `nginx.conf` sobre `nginx:1.31.5-alpine` —la etiqueta la saca del propio `Dockerfile`, no de un literal—, siembra una raiz de mentira con `docker cp`, y pide `GET` crudo con `busybox nc` desde dentro del contenedor. <br><br>**(R2) es el hallazgo de este trabajo, y la primera version del arnes la paso en VERDE.** La comprobacion que el issue enuncia —«toda entrada de `.gitignore` que no sea un directorio de trabajo esta tambien en `.dockerignore`»— **no caza su propia rotura**: `.env.*` no es una entrada de `.gitignore`; el unico archivo de entorno que git ignora es `.env.local`, y lo cubre el `*.local` que sigue estando. Medido: con `.env.*` fuera, el arnes decia «las cabeceras llegan en todas las rutas, y nada que git no vea entra en la imagen» y salia con 0. La verificacion se arreglo y no se tapo: se anade la afirmacion que si muerde —**todos los archivos de entorno que Vite lee estan excluidos del contexto**— y **cuales son no se escribe a mano**, se le pregunta a `loadEnv` de la propia Vite sembrando un directorio de mentira con un modo inventado. Contesta cuatro (`.env`, `.env.local`, `.env.<modo>`, `.env.<modo>.local`) y con ellos la misma rotura da «1 archivo(s) de entorno que Vite lee y «.dockerignore» no excluye: `.env.modo-del-arnes`». **Y el eslabon se midio aparte, sin construir la imagen**: un `frontend/.env.municipalidad-x` con `VITE_CATASTRO_TOKEN=…` y `vite build --mode municipalidad-x` deja el token **en claro, 1 coincidencia con `grep -ro`**, dentro de `assets/index-*.js`; y `git status` lo enseña como `?? frontend/.env.municipalidad-x` mientras `git check-ignore` no lo ignora — o sea que la regla del `.gitignore` **nunca** iba a verlo. Las dos comprobaciones hacen falta y ninguna sobra: quitar `*.local` lo caza la primera, quitar `.env.*` solo la segunda. <br><br>**(R4) es la mitad que solo se ve en un error**: sin `always` las tres cabeceras siguen saliendo en las respuestas buenas y desaparecen **solo en el 404** —3 en rojo, las tres sobre `/assets/ausente-del-arnes.txt`—, asi que un arnes que sondeara unicamente lo que existe habria pasado en verde. Por eso cada `location` de prefijo se sonda dos veces, con archivo y sin el. <br><br>**Las cuatro salidas con codigo 2 son el contraste, y dicen que NO MIDIERON en vez de aprobar.** (R5): «Ni un «location» declara «add_header» propio. La regla por bloque se queda SIN SUJETO y se cumpliria sola». (R6): «nginx no llego a levantar con este «nginx.conf», asi que NO SE MIDIO NI UNA CABECERA», con el log del contenedor pegado —«[emerg] host not found in upstream "catastro-api"»—; nginx resuelve el upstream **al arrancar**, y por eso el arnes crea el contenedor con `--add-host catastro:127.0.0.1` y no sonda esa ruta. (R10): «declara «add_header» y este arnes no sabe sondearlo, asi que sus cabeceras se quedarian sin medir». Y la cuarta no hubo que provocarla: **el socket del demonio de Docker se cayo a mitad de la sesion** y el arnes dijo «No hay un Docker que responda … No se omite: una comprobacion que se salta a si misma deja el flujo en verde sin haber verificado nada», que es exactamente la politica de `verificarAislamiento` en el backend. <br><br>**Y la pregunta «que haria pasar en verde a esta guarda» encontro dos huecos mas mientras se escribia.** (a) La lista de cabeceras vigiladas puede quedarse vieja: si `nginx.conf` empieza a emitir una cuarta —un CSP, pongamos— escrita en **un solo** bloque, el arnes la daria por buena en todos. Se cierra con un catalogo de 13 nombres de cabecera de seguridad que solo sirve para eso, y (R9) da «emite «content-security-policy», que es una cabecera de seguridad, y este arnes no la vigila». (b) Las dos comprobaciones de los ignorados existen **porque algo copia el contexto entero**: sin un `COPY . .` se cumplen solas, asi que su ausencia tambien sale con 2. **Y (c) es el arnes atrapandose a si mismo, y salio de apartar el bloque de Docker para probar la otra mitad mientras el demonio estaba caido**: con el recorrido por HTTP apartado, el informe seguia diciendo **«las cabeceras llegan en todas las rutas»** con **CERO** preguntas hechas, porque las tres comprobaciones de archivo salen limpias por su cuenta y nadie contaba las sondas. El camino no era alcanzable —todo lo que salta ese bloque pasa antes por «no midio»—, pero eso era una propiedad del ORDEN DE LAS LINEAS y no una afirmacion; ahora se cuenta, y la misma prueba da «Se sondearon 0 de las 5 ruta(s) y se comprobaron 0 cabecera(s) … Un recorrido vacio no es un recorrido en el que todo estuviera bien». <br><br>**Cifras, medidas en este entorno** (Node v26.7.0, yarn 1.22.22, Docker server 29.1.3, `nginx:1.31.5-alpine`): `tsc -b --noEmit` y `eslint .` **verdes antes y despues**; `vite build` **346,35 kB / 29,41 kB / 3,01 kB**, identico antes y despues —este trabajo no toca `src/`—; los ocho arneses de partida **verdes los ocho** y verdes al terminar (`reglas` 9 prohibiciones sobre 7 muestras, `rutas`, `datos` 2 archivos y 425 cadenas, `mirar` 29 pantallas, `impedimentos` 29 y 19 controles, `paleta`, `sin-red` 29 pantallas y 4 activos, y `motor`, que es el unico que cambia de cifra: **13 -> 14 guiones**). El noveno dice «nginx:1.31.5-alpine en el 8080 · **5** ruta(s) sondeada(s) y **15** cabecera(s) comprobada(s) sobre **2** «location» con cabeceras propias · **10** entrada(s) de .gitignore y los **4** archivos de entorno que lee Vite, contra **12** patron(es) de .dockerignore», y tarda **5,4 s** contra un demonio remoto —**2,4 s** cuando nginx no levanta, porque no espera medio minuto a un proceso que ya murio—. <br><br>**Las dos decisiones de CI, con su motivo.** Va en el trabajo **`verificar`** y no en `navegador`: no necesita Chromium ni `dist/` —copia el archivo sobre la base de nginx en vez de construir la imagen del frontend—, Docker viene de serie en `ubuntu-latest`, y lo que protege es el artefacto que se publica en `ghcr.io`, que pertenece al trabajo bloqueante y rapido. Va **despues** de `yarn build` porque es lo ultimo que se afirma del artefacto. <br><br>**Lo que NO cierra, con su motivo medido.** (1) **No mide el endurecimiento de la etapa final** —el `sed` que quita `user nginx;`, manda el pid a `/tmp` y da permisos por GRUPO—: eso exige construir la imagen, que es justo lo que este arnes evita para no depender de `yarn build`. Queda escrito en su cabecera en vez de descubierto. (2) **`.dockerignore` con `!`** (reincluir) no se modela: si aparece uno, el arnes sale con 2 en vez de dar por excluido algo que si viaja. (3) El `location /catastro/api/v1/` **no se sonda**, porque no declara cabeceras propias y porque `--add-host catastro:127.0.0.1` lo devolveria al propio nginx en un bucle. (4) Y el defecto que el issue arreglaba en `.dockerignore` sigue teniendo **dos guardas y una sola fuente**: si manana `.gitignore` y `.dockerignore` se mudan a otro sitio, esto sigue leyendo `frontend/` a mano. |
| **#49 — las tres superficies de error que no pasaban por `Fallo`, y el decimo arnes** (`frontend/verificaciones/errores.mjs` y `yarn errores`: 6 superficies x 6 escenarios = **36 renders**; el `Fallo` del catalogo vial en el cuadro de aranceles y `motivoDeLaViaQueFalta`; el «Reintentar» de cada tarjeta del Panel, que pasa a ser `<section aria-label>`; `Consultas.bajar()` guardando el `ErrorDeApi` entero; **9 -> 10 arneses**, y `yarn motor` pasa de **14 a 15 guiones**) | **Ocho roturas, cada una aplicada SOLA y restaurada por copia comparada con `cmp`**: (R1) quitar el `<Fallo>` del catalogo vial dejando la celda arreglada; (R2) **al reves**, dejar el `Fallo` y devolver la celda a `` `Via ${a.viaId}` ``; (R3) quitarle a la tarjeta su boton; (R4) que el boton de tres tarjetas re-pida `sectores`; (R5) quitarle al boton la guarda `error.reintentable`; (R6) devolver el `catch` de `Consultas.bajar()` a `e.mensaje`; (R7) **el contraste de metodo**: R6 puesta y ademas un mensaje distinto por escenario; y (R8) las dos guardas del propio arnes —una superficie que no existe, y un `rompe.camino` que esa pantalla no pide— | **21, 6, 8, 6, 16, 16, 16 -> 6, y 2 / 6.** Con los tres defectos puestos el arnes da **121 problemas sobre 36 renders**; arreglados, **36 renders y 0**. **(R1) y (R2) son las dos mitades del defecto 1 y hacen falta las dos, porque rompen en direcciones opuestas**: sin el aviso salen 21 —los 6 escenarios sin titulo mas los **15** pares byte a byte iguales, que es `C(6,2)`— y ninguno menciona la celda; con el aviso puesto y la celda fabricando el rotulo salen **6**, todos por `prohibido` y **ninguno** por titulo ni por distinguibilidad, o sea que la tabla entera con «Via 1 — Sin tramo 388.00» habria pasado en verde bajo una guarda que solo mirara «sale un aviso». **(R4) es el hallazgo del metodo**: el boton **esta** en las cuatro tarjetas, se ve identico, y lo unico que falla es a donde apunta —«el «Reintentar» de esta superficie NO vuelve a pedir «…/catastro/predios»; pidio …/catastro/sectores»—, con `panel-sectores` en **VERDE**; una guarda que contara botones habria dado verde en las cuatro, que es el defecto 2 disfrazado de arreglo. **(R7) mide lo que compra la decision central del arnes**: los seis escenarios comparten el MISMO `mensaje` a proposito, y darles uno distinto con el defecto de `Consultas` puesto baja de **16 a 6** —se evaporan los **10** pares byte a byte, `C(5,2)`, porque el sexto escenario trae el mensaje que pone `cliente.ts` y no el cable—; o sea que sin el mensaje fijo, «las pantallas salen distintas» se cumple pintando `error.mensaje`, que es exactamente el defecto. **(R8) es la leccion de `mirar.mjs` por los dos ejes**: `node verificaciones/errores.mjs superficie-que-no-existe` sale con **2** diciendo «No se midio NI UN render», y un `rompe.camino` viejo sale con 6 «no se rompio NI UNA peticion: «…/catastro/manzanas» no es ninguna de las que pide esta pantalla», listandolas — sin esa guarda, la pantalla entera en 200 pondria rojas las comprobaciones de abajo hablando de un titulo que falta, que manda a mirar al sitio equivocado. <br><br>**Lo esperado se DERIVA del fuente y no se copia**: los titulos de los doce codigos, la regla de `reintentable` y la frase de una linea salen de `src/api/cliente.ts` y `src/ds/componentes.tsx` compilados al vuelo con `leerModulo`; con una copia en el arnes, cambiar un titulo lo dejaria en verde afirmando sobre el viejo. **Y `page.route` no sirve**: con el proxy de datos encendido las peticiones **no llegan a la red** —el proxy SUSTITUYE `fetch`—, asi que se envuelve `globalThis.fetch` con un `get`/`set` que el proxy rellena al instalarse; con eso se rompe **una sola ruta** y el proxy contesta las demas en 200, que es el caso que importa —un usuario con `aranceles` y sin `calles`—. <br><br>**Cifras, medidas en el mismo entorno** (Node 26.7.0, Chromium de Playwright, vista previa en el 5190): `tsc` y `eslint` verdes antes y despues; `vite build` **58 modulos, 346,35 kB -> 346,74 kB** (gzip 102,32 -> 102,48). Los nueve arneses siguen igual —`reglas` 9 prohibiciones sobre 7 muestras, `rutas` verde, `datos` 425 -> **427** cadenas, `motor` 14 -> **15** guiones, `mirar` 29 pantallas, `sin-red` 29, `paleta` verde, `impedimentos` 29 pantallas y 19 controles— y `imagen` sale con **2** antes y despues, que es lo correcto: el `DOCKER_HOST` de este puesto apunta a un socket reenviado cuyo tunel esta caido, y una comprobacion bloqueante que se salta a si misma deja el flujo en verde sin haber verificado nada. <br><br>**Lo que NO se cierra, con su motivo.** (a) La **decision de diseño** fue «un «Reintentar» por tarjeta» y no un boton general arriba: cubre las cuatro lecturas y cada boton hace lo que su sitio promete, mientras que el general reintentaria lecturas que no fallaron. Medido despues del arreglo: **6** botones en el Panel con las cuatro rutas en 500 —los 4 de las tarjetas mas los 2 de las `<Lectura>` de mas abajo— y cada uno de los cuatro re-pide **su** ruta y solo la suya. (b) La tarjeta sigue diciendo el motivo en **una linea** (`motivoCorto`), que por diseño de #41 deja fuera `detalles` e `incidencia`: por eso el juego de escenarios **no trae un 422 «a secas»** —seria indistinguible del 422 con detalles en esa superficie, y exigirlo seria pedir lo que #41 descarto—. (c) El arnes recorre **las tres superficies de este issue** y no las 26 que ya pasan por `<Lectura>`: esas las mide #41, y anadirlas aqui multiplicaria por doce un recorrido de navegador sin cambiar ninguna afirmacion. |
| **#48 — los cuatro ejercicios del desplegable salen del RELOJ, y el undecimo arnes lo mide MOVIENDOLO** (`src/shell/ejercicios.ts` —dos funciones puras, `hoy` como argumento, y el motivo escrito—; el reloj leido **una sola vez** en `App.tsx`, que es el unico de todo `src/`; `Shell.tsx` recibe la lista y ya no la escribe; `verificaciones/ejercicios.mjs` con `yarn ejercicios` y su paso en el trabajo `navegador`; **10 -> 11 arneses**, y `yarn motor` pasa de **15 a 16 guiones**) | **Cinco roturas, cada una aplicada SOLA sobre `src/main` del frontend o sobre el propio arnes, y restaurada por copia comparada con `cmp`**: (R1) devolver a `Shell.tsx` los cuatro literales `['2026','2025','2024','2023']`; (R2) devolver a `App.tsx` el valor elegido a `useState('2026')`, dejando la lista derivada; (R3) un desajuste de uno en la funcion pura (`enCurso - cuantos - 1`); (R4) el arnes **sin mover el reloj**, luego midiendo **el ano en curso**, y luego las dos cosas **sin sus guardas**; y (R5) apuntar el recorrido a una hoja que no lee ningun cuadro | **4, 2, 6, 2/2/VERDE y 2.** (R1) da «con el reloj en 2031, el desplegable ofrece [2026, 2025, 2024, 2023] y tenia que ofrecer [2031, 2030, 2029, 2028] … el 2031 nadie podria elegir su propio ejercicio, y la interfaz no diria nada», por cada uno de los dos relojes. (R3) pone en rojo las cuatro fechas fijadas —«la funcion pura, el 2027-01-01T00:00:01: ofrece [2026, 2025, 2024, 2023] y tenia que ofrecer [2027, 2026, 2025, 2024]»— mas las dos del navegador. <br><br>**(R2) es el hallazgo, y su primera version del arnes PASO EN VERDE con el defecto puesto.** La comprobacion leia `select.value`, y un `<select>` cuyo valor no casa con ninguna opcion **no se queda vacio**: el navegador cae a la **primera opcion**. Asi que con el reloj en 2031 y `App.tsx` congelado, la barra ensenaba «2031» —correcto a la vista— mientras la aplicacion seguia teniendo «2026» y pedia los tres cuadros con `?ejercicio=2026`. Es **peor** que el defecto original: la pantalla dice un ano y lee otro, y las dos cosas son plausibles. La verificacion se arreglo y no se tapo: se envuelve `globalThis.fetch` con un `get`/`set` —el proxy de datos lo SUSTITUYE, asi que `page.route` no ve ninguna peticion (la leccion de `errores.mjs`)— y se mide **el ano que VIAJA**; con eso la misma rotura da 2 en rojo nombrando las tres lecturas: «con el reloj en 2031, 3 lectura(s) piden un ejercicio que no es «2031»: `/catastro/api/v1/catastro/tablas/aranceles?ejercicio=2026`, `…/valores-unitarios?ejercicio=2026`, `…/depreciacion?ejercicio=2026`». **Y se comprueban las dos cosas —lo que ENSENA y lo que VIAJA— porque las dos mitades del defecto se rompen en direcciones opuestas**, medido: con la lista congelada la pantalla **pide 2031 y ensena 2026**; con el valor inicial congelado **ensena 2031 y pide 2026**. Cualquiera de las dos comprobaciones sola deja pasar la otra mitad. <br><br>**(R4) es la pregunta «que haria pasar en verde a esta guarda» contestada con tres corridas.** Sin mover el reloj sale con **2**: «Se fijo el reloj del navegador en 2031 y la pagina dice que esta en 2026: el reloj NO se movio, asi que este arnes no midio nada y pasaria en verde con el defecto exacto que existe para atrapar». Midiendo el ano en curso —`RELOJES = [ESTE_ANIO]`—, tambien **2**: «Se iba a mover el reloj a 2026, que es EL ANO EN CURSO: con el reloj sin mover, cuatro literales congelados dan exactamente la misma lista que el reloj». Y **quitandole al arnes esas dos guardas, con los cuatro literales puestos, sale VERDE**: «el desplegable ofrece el ejercicio en curso y los tres cerrados, y los dos salen del reloj», exit 0. Eso es exactamente lo que compra mover el reloj, y por que una guarda que comprobara «ofrece cuatro anos» o «ofrece el ano en curso» no habria protegido nada: **hoy la lista derivada sale letra por letra igual que los literales**, y la que mira el ano en curso se pondria roja sola el 1 de enero, o sea que avisaria cuando el defecto ya ha ocurrido. (R5) cierra la otra mitad: «Ninguna lectura de «#/catastro/panel» llevo «?ejercicio=», asi que no se midio el unico dato que decide de que conjunto sellado se leen los cuadros», con **2**. <br><br>**El reloj entra por un solo sitio y esta escrito por que se admite.** La sexta regla de la casa dice «sin reloj» para las **reglas tributarias** —«recalcular 2027 en 2037 debe dar el mismo centimo»— y esto no lo es: no calcula nada, es que anos ofrece un desplegable, y el ano elegido sigue viajando en `?ejercicio=` para que el backend conteste con el conjunto sellado de ESE ano. Aun asi las dos funciones **reciben la fecha** y son puras, y el `new Date()` vive en un inicializador perezoso de `App.tsx` —**una vez al montar**, no en medio de un render— del que salen las **dos** derivaciones: con dos lecturas, a las 23:59:59 del 31 de diciembre la lista podria ser la de un ano y el elegido la de otro, y ya se midio lo que hace un `<select>` en ese caso —cae a la primera opcion y no avisa—. **Y que la lista es una SUPOSICION queda escrito en el codigo y no solo en el PR**: nadie le pregunta al backend que ejercicios tienen conjunto sellado —la lectura que hay, `GET /catastro/api/v1/seguridad/parametros/ejercicios/{ejercicio}`, es **por ejercicio** y contesta 200 aunque no este sellado; la que diria *cuales* hay no existe y es **#51**, que este trabajo no implementa—. Falla **ruidosa**, y esta medido moviendo el reloj al 1 de enero de 2027: la hoja Valores dice «Ejercicio 2027 · No existe · El ejercicio 2027 no tiene un conjunto de parametros sellado…», en vez de ensenar los cuadros de 2026 como si fueran los de 2027, que es lo que hacia. <br><br>**Y el mismo defecto se busco en el resto del armazon, con su comando, en vez de suponer que no estaba.** `grep -rnE '\b(19\|20)[0-9]{2}\b' src` fuera de `src/datos/` y `src/simulado/` da **exactamente dos** coincidencias en codigo, y las dos son las de este issue (`Shell.tsx:18` y `App.tsx:45`); las otras dos son una referencia normativa en prosa («R.M. 277-2025-VIVIENDA») y los rangos Unicode de `tipografia.css`. `grep -rn 'new Date\|Date.now\|getFullYear' src` no daba **ni uno** antes de este trabajo: el de `App.tsx` es el primer reloj de esta interfaz, y el proxy simulado dice por escrito que no tiene ninguno a proposito. **Lo que si aparecio, medido y NO arreglado aqui**: `src/api/` declara **14** listas `as const` copiadas del backend y `rutas.mjs` contrasta **5** (`ORDENES`, `COMPOSICION_DEL_CODIGO`, `CAMPOS_DEL_ALTA`, `CAMPOS_DE_FICHA` y `CODIGOS_DE_ERROR`); las **9** restantes no las mira nadie, y **cuatro de ellas son las opciones de tres desplegables del alta**. Comprobadas a mano contra sus enums del backend el 2026-09-07: `MaterialEstructural` 6/6, `EstadoDeConservacion` 5/5, `OrigenDeLaFicha` 4/4 y `CondicionDeTitularidad` 6/6 — **cuadran hoy**, y ese es justo el problema: el dia que el backend anada un valor, el desplegable seguira ofreciendo los de antes sin que nada lo diga. Es la misma forma de defecto por otro eje, y **sale como issue aparte**: arreglarlo aqui seria ampliar el alcance de un desplegable a la frontera entera. <br><br>**Cifras, medidas en el mismo entorno** (Node v26.7.0, yarn 1.22.22, Chromium de Playwright, vista previa en el 5210): `tsc -b --noEmit` y `eslint .` **verdes antes y despues**; `vite build` **58 modulos, 346,74 -> 346,97 kB** (gzip 102,48 -> 102,53), y `proxy-*.js` sin moverse en 29,41 kB. Los **diez** arneses de partida, verdes los nueve que pueden correr —`reglas` 9 prohibiciones sobre 7 muestras, `rutas`, `datos` 2 archivos y 427 cadenas, `motor` **15 -> 16** guiones, `mirar` 29 pantallas, `impedimentos` 29 y 19 controles, `paleta`, `errores` 36 renders, `sin-red` 29 pantallas y 4 activos— y **`imagen` sale con 2 antes y despues**, que es lo correcto: el `DOCKER_HOST` de este puesto apunta a un socket reenviado cuyo tunel esta caido, y una comprobacion bloqueante que se salta a si misma deja el flujo en verde sin haber verificado nada. El undecimo dice «**4 fecha(s) fijadas sobre la funcion pura y 2 reloj(es) movido(s) en el navegador —2031 y 2019, y el ano en curso es 2026—**» y tarda 3,0 s. <br><br>**Lo que NO cierra, con su motivo.** (1) **#51**: derivar la lista del backend es la salida correcta y sigue sin endpoint; hasta entonces el desplegable ofrece anos que quiza no tengan conjunto sellado, y esta escrito en `shell/ejercicios.ts` que asi es. (2) **El proxy simulado se queda en 2026 a proposito** —`EJERCICIO = 2026`, y su cabecera dice que no tiene reloj para que una captura no cambie sola—, asi que desde el 1 de enero de 2027 la hoja Valores de la demostracion ensenara el aviso de «sin conjunto sellado» en vez de tres cuadros. **Es el desenlace honesto y no un fallo**: es lo que hara la instalacion de verdad mientras `normativa` no selle 2027, y `mirar` sigue en verde porque un 404 es una respuesta —medido moviendo el reloj al 1 de enero de 2027: «Ejercicio 2027 · No existe · El ejercicio 2027 no tiene un conjunto de parametros sellado…»—. **Lo que si es un efecto de rebote esta medido y escrito donde esta la causa**, en el javadoc de `EJERCICIO`: con la aplicacion pidiendo un ano que el proxy no sella, `yarn errores` da **21 problemas sobre 36 renders** —los 6 titulos de `aranceles-sin-el-catalogo-vial` mas sus **15** pares byte a byte, `C(6,2)`—, porque el 404 del cuadro tapa los seis rechazos que esa superficie existe para distinguir, y su mensaje manda a mirar al sitio equivocado. Se cierra con **#51** o sellando aqui el ano en curso, que es una decision y no un parche: este proxy declara por escrito que **no tiene reloj**. (3) **Ningun ejercicio se recuerda entre sesiones**: cambiar el desplegable no se guarda en la URL ni en `localStorage`, igual que antes; llegar por enlace a la hoja de Valores siempre abre el ano en curso. No lo pedia el issue y meterlo en la ruta cambiaria el formato de `#/<modulo>/<destino>/<sujeto>?<filtros>` que #32 fijo. |
| **#46 — los cinco componentes que `FichaResource` publicaba y el tipo no declaraba, declarados y DIBUJADOS** (`instalaciones`, `economico`, `bienesComunes`, `rural` e `historico` en `Ficha`, con sus nueve `record` anidados; `CAMPOS_DE_FICHA` de 17 a 22 y `CAMPOS_DE_FICHA_QUE_NO_SE_LEEN` **vacia**; las tres rutas de lectura que faltaban —`fichaEconomica`, `fichaBienesComunes`, `fichaRural`— y `api.ficha(tipo, codigo, {historico, fecha})` en lugar de `fichaUrbana`, que no ofrecia el parametro; la pestana **Movimientos** del predio; el detalle de las 23 fichas derivado de `detalle-de-fichas.csv` en el proxy, con sus DOS versiones; y el **duodecimo arnes**, `yarn ficha` —**1 nuevo, y las cifras de los once anteriores en la tabla de abajo**) | Nueve roturas, cada una aplicada **sola** sobre `src/` y restaurada **por copia comparada con `cmp`**: (R1) una columna «Valor» con `'0.00'` en la tabla de obras; (R2) quitar de la pantalla el `Aviso` que dice que falta; (R2b) que el motivo deje de nombrar el Anexo III y `otra_instalacion`, y (R2c) vaciarlo del todo; (R3) pedir `historico: true` **siempre** y (R4) **nunca**; (R5) despachar las cuatro clases a la ruta `/urbana/`; (R6) dibujar el bloque `economico` aunque venga nulo; (R7) la tabla de obras con `filas={[]}`; (R8) devolver el detalle a componerse con la GRILLA, que es de donde salia antes de #46; y (R9) sacar `historico` de `CAMPOS_DE_FICHA` dejando la lista de huecos vacia | **2, 2, 2, salida 2, 11, 3 + salida 2, 3 + salida 2, 4, 2, salida 2 y 1 + 1.** (R1) es la trampa que este issue previene, y sale con el numero que sobra: «la seccion de obras complementarias ensena 1 cifra(s) que la respuesta NO trae: **0.00**» — la guarda no pregunta si la seccion existe, compara **toda cifra del DOM contra el JSON que la pagina recibio**, asi que un cero de relleno, un `S/` o un producto calculado en la pantalla salen nombrados. (R2) da «la seccion dibuja 1 obra(s) y NO ensena el motivo de que no tengan valor», y (R2b) nombra **los dos huecos uno a uno** —«no nombra el cuadro que no esta transcrito (/Anexo\s+III/)» y «no nombra la columna que no existe (/otra_instalacion/)»—, porque son dos y se cierran por separado: transcribir el Anexo III es de `normativa` y anadir la columna es una migracion. (R2c) sale con **2** —«`MOTIVOS.obraSinImporte` no existe o esta vacio»—, que es lo que impide que R2 se cumpla sola vaciando el motivo. **(R3) y (R4) son las dos mitades del `?historico=` y hacen falta las dos, porque se rompen en direcciones opuestas y ninguna se ve en la pantalla**: con el parametro puesto siempre, la ficha vigente **se ve exactamente igual** y paga la ficha entera repetida una vez por version (11 en rojo, cinco «la peticion LLEVA «historico=true» y tenia que no llevarlo» y cinco «NO se pidio el historico y el cuerpo trae 2 version(es)»); sin el parametro, la pestana de movimientos sale diciendo «no se pidio» **sin ningun error** (3 en rojo, y ademas salida 2 por «versiones: 0 de 2»). El contraste que los ata es que **la MISMA ruta conteste cosas distintas**: «sin el vino nulo y con el nulo». **(R5) es el hallazgo que cambio el diseno antes de escribir la pantalla**: `/urbana/{cod}` fija `TipoFicha.UNICA` y «la ficha UNICA no tiene detalle» —lo dice `DetalleDeLaFicha` con todas las letras—, asi que declarar los cinco tipos y dibujarlos **por la ruta que el issue nombra** habria dejado `economico`, `bienesComunes` y `rural` nulos SIEMPRE: codigo que ninguna vista alcanza, en verde. Medido: 3 en rojo con «la lectura de la ficha contesto **404** y no 200 — «/catastro/api/v1/catastro/fichas/urbana/20010402003001010103201»», y seis minimos por debajo. Por eso la lectura se despacha por el tipo que trae la grilla, con `esTipoDeFicha` delante: un tipo que el enumerado no reconozca **no cae a la urbana**, porque esa contesta otra ficha. (R6) pone **4** con «el cuerpo trae «economico» NULO y la pantalla dibuja su seccion», que es la distincion que `FichaResource` escribe en su javadoc: un nulo es «esta ficha no es de las que lo declaran» y una tabla vacia seria «no declara ninguna». (R7) nombra la fila que se perdio: «la respuesta trae «Cerco perimetrico» … y la pantalla no lo dibuja». **(R8) es la pregunta de vuelta —que haria pasar en verde a este arnes con el defecto puesto— y la unica respuesta es que no se pidiera ni una ficha**: con el detalle compuesto desde la grilla, las diez comprobaciones son ciertas sobre el conjunto vacio, asi que se cuenta cuantas filas de cada clase se han visto y sale con **2**. (R9) demuestra que la lista de huecos **vacia sigue guardando**: `rutas` da «FichaResource» publica «historico» y «Ficha» … no lo declara ni lo nombra en «CAMPOS_DE_FICHA_QUE_NO_SE_LEEN»», y `tsc` la segunda mitad. <br><br>**Y ejecutar corrigio el arnes**: la salida 2 por «no midio nada» estaba **antes** de imprimir los problemas, asi que R4 —que rompe las dos cosas a la vez— ensenaba «faltaron filas que mirar: versiones 0 de 2» y **escondia los tres mensajes que dicen QUE se rompio**. Los problemas se imprimen ahora primero y el codigo de salida se decide despues. <br><br>**Y una cosa que el issue da por hecha y no lo era**: dice que `instalaciones` «no se lee» y que el alta «ya las manda». Las manda, y el proxy las **descartaba**: su `201` devolvia `construcciones: []` y ni una obra, asi que el camino entero del que habla el issue —se teclea, se guarda, se devuelve— no se podia ejercer. Ahora las echa de vuelta con la unidad dentro de la cantidad, como hace `Medida.toString()`. <br><br>**El detalle de la demostracion no se teclea: se deriva** de las 51 filas de `infra/carga-de-datos/ejemplos/detalle-de-fichas.csv`, como ya se derivaba el padron, y con los tres agregados calculados igual que el dominio (`areaComunTotal`, `hectareasTotales`, `sinLicencia`) en suma exacta de decimales. De ahi salen tambien las **dos versiones** de cada ficha, que es lo que ese archivo carga —«VERSIONA, NO SOBRESCRIBE», dice su cabecera— y lo que da contenido real a la pestana de movimientos: 22 predios con dos versiones y `20010403003001000000000` —Jiron Cusco 900, un terreno sin construir— con una. El proxy dejaba `version: 1` escrito a mano en tres sitios. <br><br>**Cifras, medidas en el mismo entorno** (vista previa en el 5190, Chromium de Playwright): `tsc` **0 errores** antes y despues; `eslint` **0** antes y despues; `vite build` **59 modulos** antes y despues, `index` de **346,97 kB** a **357,23 kB** y el trozo del proxy de **29,41** a **43,83 kB** —el detalle derivado, que **no viaja en el paquete de produccion**: lo sigue comprobando `sin-red`—. Los arneses: `rutas` **30 rutas -> 33**, **13 accesos -> 17** (los cuatro `ficha_*` de lectura) y **«22 componentes de la ficha contra 17 del tipo (+5 huecos declarados)» -> «contra 22 del tipo (+0 huecos)»**; `datos` **427 -> 455 cadenas**; `motor` **16 -> 17 guiones**; `mirar` **29 -> 34 pantallas**; `impedimentos` **29 -> 34 pantallas y 19 -> 29 controles impedidos**; `sin-red` **29 -> 34 pantallas**, verde; `errores` **36 renders**, sin cambio; `reglas`, `paleta` y `ejercicios`, iguales; y el nuevo `ficha`, **6 vistas sobre 5 predios y las 4 clases · 2 obras · 1 actividad · 2 bienes · 2 participaciones · 2 tierras · 4 colindantes · 2 versiones · 4 construcciones**. `imagen` sale con **2 antes y despues**, y no es de este cambio: el `DOCKER_HOST` de este puesto apunta a un tunel caido, y el arnes **no se omite** —«se sondearon 0 de las 5 rutas»—. <br><br>**Lo que NO cierra, con su motivo.** (a) **La comprobacion general TS<->`record` de las ~40 lecturas restantes**, que el issue nombra como «el mismo hueco en general»: `rutas.mjs` ata **una** lectura mas —la de la ficha, que ya estaba— y las demas siguen sin nada que las ate. Es otro trabajo y no cabe aqui. (b) **Las cuatro lecturas de ficha exigen cuatro accesos distintos y esta pantalla los declara los cuatro**, asi que quien tenga `ficha_urbana` y no `ficha_rural` vera un **403** al abrir un predio rustico: la pantalla lo dibuja como el rechazo que es —pasa por `Fallo`—, y **no se ha medido con un token real** porque este entorno no tiene backend. (c) `otra_instalacion` **sigue sin columna de importe** y el Anexo III sigue sin transcribir: la pantalla dice las dos cosas y no arregla ninguna, porque transcribir es de `normativa` y la columna es una migracion. (d) El `?fecha=` de la lectura viaja en el tipo (`OpcionesDeLaFicha`) y **ninguna pantalla lo usa todavia**: la ficha «a una fecha» es una pantalla que este issue no pide. |
| **#43 — `sectores` y `calles` entran en el catálogo, y la guarda deja de leer el fuente con una expresión regular** (las dos `Opcion` en `CatalogoDelSistema`, con su nombre transcrito del manual; `CatalogoDelSistemaTest` reescrito sobre **ArchUnit** y movido de `kamayuk-catastro-seguridad` a `kamayuk-catastro-aplicacion`, que es el único módulo con todos los demás en el classpath; **1 228 → 1 228 pruebas**, 0 fallos — la guarda se mudó, no se duplicó: `seguridad` 7 → 5 y `aplicacion` 111 → 113) | **No hubo que provocar la primera: era el estado de partida.** Con el defecto puesto —`SectorController.ACCESO` valiendo `"sectores"` y el catálogo sin esa fila—, `:kamayuk-catastro-seguridad:test --rerun-tasks` sobre `main` (742fc73) daba **`PASSED` las dos pruebas, `BUILD SUCCESSFUL`**. Después, tres roturas, cada una **sola** sobre `src/main` y restaurada **por copia comparada con `cmp`**: (1) quitar del catálogo la fila de `sectores`; (2) **añadir** `@RequiereAcceso(acceso = "un_acceso_que_no_existe")` a un método de `DepreciacionController`; (3) meter el centinela `__sesion_propia__` en el catálogo. Y el contraste que la serie lleva diez veces pidiendo: dejar la lectura **sin sujeto**, apuntando `ConfiguracionDeCatastro.paqueteRaiz()` a un paquete que no existe | **1, 1 y 1 en rojo, y el contraste dos veces.** (1) «estos accesos los exige un endpoint con @RequiereAcceso y el catalogo no los tiene … el guardia niega SIEMPRE y esos endpoints contestan 403 a todo el mundo, administrador incluido»: `Expecting empty but was: ["sectores"]`. (2) el mismo mensaje con `["un_acceso_que_no_existe"]`, y **es además la prueba de que el verde rancio se cerró**: la tarea corrió sin `--rerun-tasks` porque el jar de `nucleo` es entrada declarada de `:kamayuk-catastro-aplicacion:test`. (3) el otro sentido, con su propio motivo: «estas opciones estan en el catalogo y ningun endpoint de este sistema las exige … un permiso sobre algo que no existe», `Expecting empty but was: ["__sesion_propia__"]`. **Los dos sentidos se afirman por separado a propósito**: un solo `containsExactlyElementsOf` los caza igual pero dice «esta lista no es esa otra», y los dos defectos no se arreglan en el mismo sitio. <br><br>**Cómo lee el acceso, que es el issue entero.** `acceso` es un `String` constante, o sea una **constante de compilación**: `javac` la graba en el pool del `.class` **como su valor**, así que en el bytecode el literal y la constante son indistinguibles — que es justo la distinción que no debía existir. La guarda importa las clases de producción con el mismo `ReglasDeArquitectura.clasesDeProduccion()` de ARQ-04 §2 y pide `tryGetAnnotationOfType(RequiereAcceso.class)` en la clase y en cada método. Los dos centinelas se excluyen **por su valor** —`RequiereAcceso.SESION_PROPIA` y `RequiereAcceso.CIUDADANO`, no una lista de nombres de clase—, y `oTambien` entra en la cuenta junto a `acceso` porque `GuardiaDeAcceso` los pregunta al **mismo** comprobador y contra la misma tabla: hoy no lo usa ningún endpoint de este sistema, y el día que lo use la guarda ya lo mira. **Medido sobre `main`**: en `src/main` de `backend/` hay **51** anotaciones, **36** con literal —las únicas que la expresión regular veía— y **15** con constante; contando también `src/test`, **58 · 41 · 17**. Los códigos que se perdían de verdad eran **dos**, `sectores` y `calles`, y con ellos **ocho** endpoints. <br><br>**El contraste de «no midió», medido en las dos direcciones.** Con el paquete raíz apuntando a la nada, la guarda sale roja diciendo lo que le pasa: «no se leyo ni una @RequiereAcceso del bytecode de este sistema … no es «esta bien» sino «no se pudo comprobar»», `Expecting actual: 0 to be greater than: 0`. **Y quitándole esa aserción, con el mismo defecto puesto, el rojo cambia de significado**: pasa a decir que las **16** opciones del catálogo sobran y que hay que borrarlas —el diagnóstico exactamente al revés—, que es lo que cuesta una guarda que se queda sin sujeto y se da por buena. <br><br>**Y la guarda vieja tenía un SEGUNDO defecto que nadie había medido: no se podía poner roja sin `--rerun-tasks`.** Recorría `src/main` de todo el repositorio desde `kamayuk-catastro-seguridad`, un módulo que no depende de ninguno de los que lee, y esas fuentes no eran entrada declarada de su tarea. Medido: con el acceso inventado puesto en `DepreciacionController`, `:kamayuk-catastro-seguridad:test` salió **`UP-TO-DATE` — «12 actionable tasks: 12 up-to-date», `BUILD SUCCESSFUL`**. Es la lección de #192 punto 2, y se cierra sola al mudar la prueba: en `kamayuk-catastro-aplicacion` los otros diez módulos están en el classpath, o sea que son entrada declarada. **Por eso la prueba se mudó y no se quedó donde estaba**: leer el bytecode de todos exige tenerlos, y `kamayuk-catastro-seguridad` **no tiene ArchUnit** —lo trae `comun-verificaciones`, que sólo consume `aplicacion`—; su `build.gradle.kts` ya dice que es «donde corren las verificaciones que necesitan ver todo el sistema a la vez». Con ello la guarda pasa además a correr dentro de `verificarArquitectura`, donde no estaba. <br><br>**La implantación, medida contra PostgreSQL real y no razonada** (AC-4): arrancando el artefacto en el perfil `batch` con las variables del Job sobre una base creada de cero, `ImplantarMunicipalidad` dice «Municipalidad 200105 lista en catastro (DEMOSTRACION): id 1, **16** accesos nuevos» —**14** con el catálogo de `main`, medido en el mismo entorno—, y la **segunda** corrida dice «**0** accesos nuevos» sin mover una fila: `acceso=16 permiso=16 modulo_sistema=2` antes y después. El arnés de esa medición era temporal y **se borró**; lo permanente es la guarda. <br><br>**Cifras, medidas en el mismo entorno** (PostgreSQL 16.15 + PostGIS 3.4.4 en el puerto 5433, `./gradlew build --rerun-tasks`, sumando los XML de `test` y `pruebaDeArranque`): **1 228 → 1 228**, 0 fallos. `verificarArquitectura` y `verificarAislamiento`, **verdes los dos**. <br><br>**Lo que NO se cierra, con su motivo.** (a) **No se toca `SectorController` ni `ViaController`**: escribir el acceso como constante no es el error —es mejor que repetir un literal nueve veces—, el error era que la guarda sólo supiera leer una de las dos formas. (b) **El arnés `rutas` de `catastro-web` queda con dos comentarios que este PR vuelve falsos**: dice que «`CatalogoDelSistemaTest` busca literales de cadena, asi que no los ve y sigue en verde», y ya no. No pone nada en rojo —su `huerfanos` pasa a estar vacío y el AVISO deja de imprimirse—, pero el texto es de `frontend/` y se corrige allí. (c) **El censo del issue no cuadra al medirlo, y la diferencia importa poco**: dice 57 · 40 · 17 y hoy son **58 · 41 · 17** contando `src/test` —el 17 de las constantes coincide—; sobre `src/main`, que es lo que la guarda mira, son **51 · 36 · 15**. |
| **#55 — las once listas de `src/api/` que no contrastaba nadie, atadas a su enumerado o declaradas con su motivo** (`LISTAS_DERIVADAS_DE_UN_ENUM` y `LISTAS_QUE_NO_SALEN_DE_UN_ENUM` en `catastro.ts` y `cliente.ts`; el **punto 8** de `verificaciones/rutas.mjs`, con un lector de enumerados de Java que no es una expresión regular y que ahora usan también los códigos de error; **10 de 15** listas atadas, **39 valores comprobados en los dos sentidos**, +5 declaradas) | Siete roturas, cada una **sola** y restaurada **por copia comparada con `cmp`**: (1) quitarle `QUINCHA` a `MATERIALES`; (2) añadirle `SILLAR`; (3) una lista nueva sin pareo ni declaración, puesta en un módulo que no declara ninguna de las dos tablas; (4) **vaciar `MaterialEstructural`** en `backend/`; (5) parear `TITULARIDADES` con `CondicionDeTitularidad` —el pareo por parecido de nombre del que el issue avisa—; (6) renombrar la clave del pareo; y (7) un código de error declarado sin `(HttpStatus.`, medido con el lector nuevo **y con el viejo** | **1, 1, 1, 1, 9, 2 y 2 en rojo, y la séptima en VERDE con el lector viejo.** (1) y (2) son las dos mitades del modo de fallo y **hacen falta las dos, porque no se arreglan en el mismo sitio**: «`MaterialEstructural` tiene `QUINCHA` y `MATERIALES` (catastro.ts) no lo ofrece … Es el fallo INVISIBLE de los dos: la opcion no se puede elegir, no hay error, no hay aviso», contra «`MATERIALES` (catastro.ts) ofrece `SILLAR` y `MaterialEstructural` no lo admite … el backend contesta 422 AL ENVIAR, despues de rellenar los seis pasos del asistente». (3) da «`USOS_COMPATIBLES` (urbano.ts) es una lista que no dice de donde sale», que es el AC-1 entero: la que nazca mañana sale en rojo con su nombre en vez de nacer sin nadie que la mire. (4) es **el sujeto vacío**: «dice salir de `MaterialEstructural` y no se pudo leer: «enum MaterialEstructural» se leyo y no tiene ni una constante … Falla diciendo que no midio» — los tres desenlaces que no son «lo leí» se distinguen a propósito (no existe el archivo, existe y no declara ese enumerado, lo declara vacío) y ninguno puede pasar por «cuadra». (6) pone **2**: la lista se queda sin declarar **y** la declaración rancia sale nombrada, que es la mitad que impide que la tabla de pareos envejezca en silencio. <br><br>**(5) mide el aviso del issue y lo corrige al alza.** El pareo por parecido de nombre no da un desajuste falso de seis valores sino de **nueve** —seis «no lo ofrece» más tres «no lo admite», porque la comparación va en los dos sentidos—: una guarda que adivinara el pareo gritaría nueve veces sobre lo único que aquí está bien, y una guarda que grita en lo correcto se acaba apagando. Por eso el pareo se declara: `TITULARIDADES` no es `CondicionDeTitularidad` sino `TitularidadDelPredio`, el filtro del padrón. <br><br>**(7) es el hallazgo de este trabajo, y pasó en VERDE.** El punto 6 leía `CodigoDeError.java` con su propia expresión regular anclada a `(\s*HttpStatus\.`, o sea que **sólo veía los códigos que declaran su estado como literal**. Con un código nuevo escrito `DEMASIADAS_PETICIONES(ESTADO_DE_LA_CUOTA, …)` —Java perfectamente válido— el arnés seguía diciendo «12 codigos de error contra **11**» y **salía en verde**: el cliente no conocería ese código, `esCodigoConocido` lo rechazaría y la pantalla enseñaría el título de OTRO código, que es exactamente el defecto que ese punto existe para impedir. Es la forma de #43 por tercera vez. Se cierra leyendo el enumerado como enumerado: se quitan comentarios y cadenas —vaciándolas, no borrándolas, para que el conteo de paréntesis y llaves siga cuadrando: estos javadoc están llenos de `{@code X}`—, se busca `enum <Nombre>`, que es la única forma que el lenguaje admite, y se toman los trozos separados por comas hasta el primer `;` al nivel de la llave. Con el lector nuevo la misma rotura da **2 en rojo** —el código que falta y el orden—, y `CodigoDeError` se lee igual que antes: **los mismos 11 en el mismo orden**, medido contra el lector viejo. <br><br>**Y el sujeto se censa EJECUTANDO, no leyendo `as const` con una expresión regular** — que es lo que habría hecho pasar en verde a esta guarda con el defecto puesto. Se compila cada módulo de `src/api/` y se recorren sus exportaciones, así que un array es un sujeto lo declare `as const` o no. **Medido: son 15 y no las 14 que se ven en el fuente**; la que no lleva `as const` es `CODIGOS_DEL_BACKEND`, que se deriva con un `filter`. Y el censo del issue tampoco cuadra por el otro lado: de las 15, las que ya contrastaba alguien eran **cuatro** —`CAMPOS_DEL_ALTA`, `CAMPOS_DE_FICHA`, `CODIGOS_DE_ERROR` y `CODIGOS_DEL_BACKEND`, por los puntos 4, 7 y 6—, o sea **once** sin mirar y no nueve: las otras dos de las «cinco» que el arnés ata —`ORDENES` y `COMPOSICION_DEL_CODIGO`— no son listas sino objetos. <br><br>**Las cifras, medidas en el mismo entorno, antes y después.** `tsc`, `eslint` y `vite build` verdes los tres, y **el paquete que se publica no cambia ni un byte**: `index-IsJ85wrR.js`, 357,23 kB, el mismo hash — las dos tablas son declaraciones que ninguna pantalla importa, así que Rollup las quita. Los **doce arneses en verde antes y después**, con las mismas cifras salvo la de `rutas`, que gana su medida: `33 rutas · 17 accesos · 16 opciones · 18 campos de orden · 8 tramos · 20 campos del alta · 22 componentes de la ficha · 12 códigos de error` **· `10 de 15 listas de src/api atadas a su enumerado (39 valores en los dos sentidos, +5 declaradas como no derivadas)`**. <br><br>**Lo que NO cierra, con su motivo medido.** (a) **`CATEGORIAS_CONSTRUCTIVAS` queda declarada y sin contrastar con nada**: sus siete claves son componentes del `record` anidado `ConstruccionDeclarada` —`categoriaMuros`, con el prefijo— y no del `record` `CategoriasConstructivas`, cuyos componentes se llaman `muros`, `techos`…; compararlas exige una traducción que la guarda tendría que inventarse, que es justo lo que una guarda no debe hacer. Con eso, 14 de las 15 listas las contrasta alguien y **una no**. (b) **Los cuerpos anidados del alta siguen sin comparar campo a campo**: el punto 4 mira los 20 de primer nivel, y `ConstruccionDeclarada`, `TierraDeclarada` y las demás viajan dentro sin que nadie las mire — es el mismo hueco que este issue acaba de cerrar por el eje de los enumerados, por el eje de los `record` anidados. (c) **`LISTAS_QUE_NO_SALEN_DE_UN_ENUM` es una salida**, y se declara como tal: quien quiera callar una lista puede escribirle un motivo. Es el mismo trato que `.dockerignore` da a sus excepciones y que `CAMPOS_DE_FICHA_QUE_NO_SE_LEEN` da a los suyos — un motivo escrito se discute en la revisión; un silencio no. (d) **Tres textos de `frontend/` que #43 dejó falsos** —dos comentarios de `rutas.mjs` y una sección entera del `README`, que decían que `CatalogoDelSistemaTest` no ve `sectores` ni `calles` «y sigue en verde» y que `Catastro · Territorio` contestará 403— se corrigen aquí porque la fila de #43 los dejó dichos para `frontend/`. Medido antes de escribirlos: el catálogo declara **16** opciones y `yarn rutas` no nombra ningún huérfano. No cambian ninguna comprobación. |
| **#21 y #22 — el `JOIN` llano de `grd`, y las dos zonas del mismo plan que `LIMIT 1` elegia en silencio** (#21: `LOTE_Y_SU_MARCO` pasa a `CROSS JOIN LATERAL … OFFSET 0` como metodo compartido por las dos capas, con `zonasQueCruzanElLoteSql()`/`fajasQueCruzanElLoteSql()` y `ElMarcoDelRiesgoLlegaAlIndiceTest`. #22: **`V11__zonas_de_un_plan_no_se_pisan.sql`** —un disparador de restriccion diferido, no una exclusion—, `ZONA_QUE_CONTIENE` con `ST_Covers` y `LIMIT 2`, `zonasQueContienenAlPredio` devolviendo **lista**, `ZonificacionDelPredio.ZonaAmbigua` → **409 `CONFLICTO`**, y `ElMarcoDeLaZonificacionLlegaAlIndiceTest`; **1 228 → 1 238 pruebas**) | **Siete roturas, cada una aplicada SOLA sobre `src/main` y restaurada por copia comparada con `cmp`** —«RESTAURADO IGUAL» las siete—: (R1) devolver `LOTE_Y_SU_MARCO` al `JOIN` llano; (R2) quitarle **solo** el `OFFSET 0`; (R3) `V11` sin su disparador; (R4) devolver `LIMIT 2` a `LIMIT 1`; (R5) devolver `ST_Covers` a `ST_Contains`; (R6) quitar el `LIMIT` **del todo**; y (R7) que el caso de uso se quede con la primera zona | **VERDE, 2, 3, 1, 2, VERDE→1 y 2 en rojo. Las dos que pasaron en VERDE son el trabajo entero, y las dos se arreglaron en la verificacion.** <br><br>**(R1) paso en VERDE la primera vez, y eso corrigio la premisa del issue.** #21 da por hecho que el `JOIN` llano de `grd` esta mal *siempre*; medido, no lo esta: con el defecto puesto y un solo tamano de tabla —3 000 filas en cada una, dos municipalidades— el planificador **elige el buen plan por su cuenta** y la prueba pasaba. Que el marco llegue al indice es una decision de **coste**, asi que un solo tamano mide la preferencia del planificador ese dia y no la propiedad. Se cambio la unidad de lo que mide —barrido de nueve tamanos sobre el SQL de produccion, y dos de ellos, `capa=20/predios=3000` y `3000/3000`, fijados en la prueba— y entonces (R1) da **2 en rojo** nombrando el sintoma exacto: «`Join Filter: ((c.marco_oeste <= p.marco_este) AND … AND st_intersects(…))`» con «Rows Removed by Join Filter: 20», y `loops` sobre `predio` en 20 y 3 000 donde tiene que ser 1. <br><br>**(R2) es el hallazgo del que cuelga el arreglo, y el issue no lo contemplaba: `CROSS JOIN LATERAL` NO basta.** PostgreSQL **aplana** (*subquery pull-up*) un `LATERAL` sin barrera, asi que las cuatro comparaciones vuelven a ser condiciones de UNION y caen al `Join Filter` **exactamente igual** que con el `JOIN` llano — medido en los nueve tamanos, «marco en el `Index Cond`» sale **identico** entre las dos formas. Lo que salva a `ZONA_QUE_CONTIENE` de `urbano` no es ser `LATERAL`: es su **`LIMIT 1`**, que es una barrera. `grd` no puede llevar `LIMIT` —devuelve todas las zonas y todas las fajas— asi que lleva **`OFFSET 0`**, el cortafuegos que PostgreSQL documenta. (R2) da **2 en rojo** por la aserción del `Index Cond`, con `loops=1` intacto: las dos mitades se rompen por separado y hacen falta las dos. <br><br>**Las cifras de #21, medidas como `kamayuk_app` bajo la politica, con `EXPLAIN (ANALYZE, BUFFERS)` y `ANALYZE` hecho.** Con 3 000 zonas y 3 000 predios: `JOIN` llano **9 146 bloques y 6,33 ms** —`Index Scan using predio_pk … loops=3000`, o sea el mismo lote rebuscado una vez por zona—; `LATERAL` sin barrera **143 bloques y 1,36 ms**; `LATERAL` + `OFFSET 0` **14 bloques y 0,18 ms**, con las cuatro columnas y la politica juntas en el `Index Cond`. Con 20 zonas y 3 000 predios, **62 → 7**. **Y lo que la medida NO dice queda escrito**: con 200 zonas contra 3 000 predios el planificador sigue prefiriendo `zona_riesgo_codigo_uq` aun con la barrera, y hace bien —50 bloques, 0,15 ms—; lo que la barrera compra es que **pueda** entrar, y que entre cuando la capa crece. <br><br>**(R6) tambien paso en VERDE, y es el mismo hallazgo por el otro extremo.** Quitarle el `LIMIT` del todo a `ZONA_QUE_CONTIENE` —que es lo que «pedir todas las zonas» sugiere hacer— **no cambia ninguna respuesta** y deshace lo que #4 midio, y las **41** pruebas de `urbano` seguian verdes, la de plan de `ZonificacionFronteraTest` incluida: al tamano que aquella siembra el planificador vuelve a elegir bien solo. Medido sobre el esquema de verdad con su politica, en cinco tamanos: **sin `LIMIT` el marco no llega al indice en 5 de 5**; con `LIMIT 2` llega en los tres de capa grande, **igual que con `LIMIT 1`** — que es lo que hace segura la eleccion de #22. Con la guarda nueva puesta, (R6) da **1 en rojo** con «`Join Filter: ((zz.marco_oeste <= p.marco_este) AND … AND st_covers(…))`» y ni una mencion de `zonificacion_marco_ix`. <br><br>**La forma de `V11` la decidio la medida, no el enunciado.** El AC-1 pide una segunda restriccion de exclusion —`plan WITH =`, `codigo WITH <>`, `geometria WITH &&`—. **No sirve, y es el hallazgo de `V7` por el otro eje**: medido el 2026-09-07 sobre la tabla de verdad, esa candidata **rechaza las dos cosas** —el solape que hay que impedir y la adyacencia que hay que dejar pasar—, «ERROR: conflicting key value violates exclusion constraint "candidata_con_cajas"» sobre dos rectangulos que solo comparten su arista. Y no hay tercera forma: el operador que hace falta no es indexable por GiST. Medido sobre las tres relaciones que importan — `&&` y `ST_Intersects` valen `t` en las tres (adyacente, solapada, contenida) y **ninguno distingue «son vecinas» de «se pisan»**; el patron DE-9IM **`ST_Relate(a, b, 'T********')`** da `f, t, t`. Asi que la restriccion se apoya en el operador exacto dentro de un **`CONSTRAINT TRIGGER … DEFERRABLE INITIALLY DEFERRED`** —sigue impidiendolo el motor, que es lo que importa cuando el dato entra por un cargador—, con **el marco delante** (ADR-0034) y sin `SECURITY DEFINER`, para que la politica se le aplique. (R3) da **3 en rojo**, los tres «Expecting code to raise a throwable»: las dos zonas solapadas entran. Los **cinco** contrastes de #4 siguen verdes —adyacentes del mismo plan, plan sucesor cerrado la vispera, relevo diferido dentro de una transaccion, segundo plan rechazado e indice unico—. <br><br>**Y el AC-3 se midio en vez de razonarse.** `ST_Contains` es **falso en la frontera**: un lote a caballo de la arista que separa dos zonas tiene su punto representativo justo encima, `POINT(-80.6 -5.2795)`, y `ST_Contains` da **`f` en las dos** — o sea un 404 «ningun plan vigente cubre el predio» sobre un suelo que cubren dos, que manda a aprobar una zonificacion que ya existe. `ST_Covers` da `t` en las dos. Se toma `ST_Covers`, **y solo es admisible porque el `LIMIT 1` se fue**: con el puesto habria hecho MAS probable que la consulta eligiera en silencio. (R5) da **2 en rojo**, y el primero es el AC-3 literal: «Expecting actual: 404 not to be equal to: 404». (R4) da **1**, «expected: 409 but was: 200», y (R7) **2** —la funcion pura y la frontera—, que es lo que separa «la consulta trae dos» de «quien decide es el caso de uso». <br><br>**Y el estado a caballo de la arista es el UNICO alcanzable despues de `V11`**, lo que hace que la prueba de frontera mida datos legitimos: dos zonas VECINAS del mismo plan son como se dibuja un plan, y las dos cubren el punto de la arista. No hay que corromper nada para llegar ahi. <br><br>**El 409 reusa `CONFLICTO` y no estrena codigo, por una razon medida**: `frontend/verificaciones/rutas.mjs` ata `CODIGOS_DE_ERROR` a `CodigoDeError.java` —los once, **y en su orden**— y `titulosDeError` es un `Record<CodigoDeError, string>` completo, asi que un codigo nuevo convierte un arreglo del backend en un cambio del frontend. Y el significado cuadra: «el estado actual no admite esta operacion». <br><br>**Cifras, medidas en el mismo entorno** (PostgreSQL 16.13 + PostGIS 3.4.2 en el puerto 5434, `./gradlew build --rerun-tasks`, sumando los XML de `test` y `pruebaDeArranque`): **1 228 → 1 238**. Las 10 nuevas son 2 de la prueba de plan de `grd`, 1 de la de `urbano`, 3 de `PlanesDeZonificacionQueNoSePisanTest`, 2 de `ConsultaDeZonificacionTest` y 2 de `ZonificacionFronteraTest`. `V1..V11` aplican **limpias sobre una base de cero** y el recuento de tablas no se mueve —**48 → 48**, `V11` no crea ninguna—. `verificarArquitectura` y `verificarAislamiento`, **verdes los dos**. <br><br>**Lo que NO se cierra, con su motivo medido.** (a) **La linea base traia 1 rojo ajeno y sigue trayendolo**: `ElPadronDeDemostracionSeValorizaTest` muere con «`normativa` no publica ninguna fila «PORCENTAJE_DE_ACTUALIZACION» en …/parametros-2026.csv», porque el clon hermano de ESTE carril esta en una rama con trabajo sin confirmar que retira esa fila; su `origin/main` si la trae. No es de estos dos issues y no se toca el arbol de otro agente. (b) **`docs/50-api/eventos/lote-de-eventos.json` se regenera solo** al correr el build sobre `origin/main` sin tocar nada —medido antes de escribir una linea—, asi que su diff no viaja en este PR. (c) **La prueba de plan de `ZonificacionFronteraTest` (#4) se queda como esta**: mide un solo tamano y por eso no caza (R6); en vez de reescribirla se le anade al lado la guarda que si lo caza, porque lo que aquella afirma —que a SU tamano el plan es el bueno— sigue siendo cierto y util. (d) **`OFFSET 0` es una barrera documentada pero obscura**: queda escrita en el javadoc con su medida, y la prueba de plan es lo que impide que alguien la quite por parecerle ruido. |
| **#24, #25 y #23 — `fiscalizacion`: la observacion llega a la fila, la campania congela con que detecto, y el ciclo del hallazgo se cierra** (**#24**: `FiscalizacionRepository` recibe la `Observacion` en las **cinco** escrituras y en los **dos** `UPDATE`, `AbrirCampania` captura `DuplicateKeyException` y el `if` queda como atajo, e `instanteDe` lanza en vez de inventar 1970. **#25**: `campania.tope` en `V12`, `tolerancia` **fuera** de la peticion y `Tolerancia` retirada, el cruce mira **las cuatro clases de ficha** con una fila por predio, y la deteccion devuelve su **censo**. **#23**: `V12` con las tres columnas del acto de anulacion y sus dos `CHECK` cruzados, la geometria del hallazgo **retirada** con sus cuatro columnas de marco y su GiST, `HALLAZGO_DEJADO_SIN_EFECTO` en el buzon, `DejarSinEfectoElHallazgo` con `POST /hallazgos/{id}/anulacion` y `POST /campanias/{id}/cierre` — **1 228 → 1 253 pruebas** —las dos cifras medidas contra `d6dec0f`, la base de esta rama: tras el rebase sobre #57 y #58 la de partida se volvio a medir y sigue en 1 228, porque las guardas de #58 son arneses `.mjs` de `frontend/` que `./gradlew build` no cuenta y #57 REESCRIBIO `CatalogoDelSistemaTest` sin cambiar su numero de casos—, 0 fallos) | **Once roturas, cada una aplicada SOLA sobre `src/main` y restaurada por copia comparada con `cmp`**: (R24-1) devolver el literal `"deteccion"` al `INSERT` de `candidato`; (R24-2) quitarle a `AbrirCampania` la captura de `DuplicateKeyException`; (R24-3) devolver `instanteDe` a `Instant.EPOCH`; (R25-1) pedir el cruce con una cifra que no es la de la campania; (R25-2) devolver `AND f.tipo = 'UNICA'` al cruce; (R25-3) contar los que superan el umbral **sobre la lista ya recortada** por el tope; (R23-1) quitar de `V12` el `CHECK` que ata el estado al acto; (R23-2) que `hallazgosDejadosSinEfecto()` devuelva vacio; (R23-3) que `cerrar` deje la campania `ABIERTA`; (R23-4) quitar el tipo nuevo del `CASE` de `catastro_evento_predio_ck`; y (R23-5) derivar la identidad de la retractacion **del contenido**. Mas una que paso en **VERDE**, y un rojo del motor que nadie provoco | **1, 1, 1, 1, 1, 1, 1, 2, 2, 2 y 1 en rojo, y cada uno nombra lo suyo.** (R24-1) es el defecto tal cual estaba y **hoy pasaba en VERDE**, porque ninguna prueba leia la columna: «expected: "corrida del 7 de setiembre sobre el sector 04" but was: "deteccion"». (R24-2) da «Expecting actual throwable to be an instance of: `AbrirCampania.CampaniaYaAbierta` but was: `org.springframework.dao.DuplicateKeyException` … SQL [INSERT INTO campania …]» **y el contraste —el atajo del `if`— sigue VERDE**, que es lo que mide que las dos vias contesten lo mismo. (R25-2) da «Expected size: 1 but was: 0 in: []», que es el AC-5.2 de #25 literal: el predio cuya unica ficha vigente es de otra clase **desaparecia**, y la salida era «0 candidatos». (R23-2) es el hallazgo entero de AC-8.2 y da **2**: «expected: 1 but was: 0» con su motivo dentro —«sin esto, el hallazgo anulado DEJA DE APARECER en la proyeccion —que filtra por FIRME— y nada se lo dice a `rentas`»—. (R23-4) da «ERROR: new row for relation "catastro_evento" violates check constraint "catastro_evento_predio_ck"», que es el `ELSE false` de `V10` haciendo exactamente lo que existe para hacer: un tipo nuevo sin forma decidida **no entra**. <br><br>**La que paso en VERDE es un hallazgo del mismo eje que R-N y #7, por tercera vez**: borrar `Tolerancia.java` y **dejar su nombre** en `envoltoriosDeDecimal()` deja `verificarArquitectura` en `BUILD SUCCESSFUL`. Una exencion que nombra un tipo que ya no existe no protege nada y nadie se entera — se limpia y queda escrito en su javadoc; la guarda que lo cazaria vive en `comun-verificaciones` y es de otro repositorio: queda **abierta como [`hneyra/infrastructure#27`](https://github.com/hneyra/infrastructure/issues/27)**, con la medida dentro y el censo de las **siete** listas de `ConfiguracionDeLasVerificaciones` que tienen esa misma forma. <br><br>**Y un rojo que no hubo que provocar, del motor.** El relleno del tope se escribio como `UPDATE campania SET tope = 500` y murio: «ERROR: unrecognized configuration parameter "app.municipalidad_id", Location: V12__…sql, Line: 52». `campania` tiene `FORCE ROW LEVEL SECURITY`, asi que la politica alcanza **tambien al dueno** —que es quien corre las migraciones— y su `USING` evalua un ajuste que fuera de una peticion no existe: **ningun `UPDATE` de datos sobre una tabla de tenant se puede escribir en una migracion** sin recorrer las municipalidades. Se hace con `ADD COLUMN … NOT NULL DEFAULT` y `DROP DEFAULT`, que es DDL y no pasa por la politica. Es la quinta vez que `FORCE RLS` sobre el dueno muerde en este proyecto, y la primera por el eje de las migraciones. <br><br>**Y el borde que el issue no nombra, medido: `>` frente a `>=`.** El `WHERE` filtraba `diferencia > :tolerancia` (estricto) y Java usaba `Score.alcanza`, que es `>=`; con una sola cifra las dos comparaciones **discrepan justo en el borde**. Se toma `>=` —alcanzar el umbral basta, que es lo que la palabra significa y lo que `alcanza` define—, la comparacion queda en **un solo sitio** y el segundo filtro de Java se retira. Lo fija `ContrasteDeAreasJdbcTest` por los dos lados: con umbral `0.5000` el predio que difiere exactamente eso **entra**, y con `0.5001` no. Con `>` estricto, el predio que difiere justo lo que la campania declaro sospechoso se caia y nada lo decia. <br><br>**Y el AC-3 de #23 daba por cierta UNA puerta y hay DOS.** El issue atribuye el remedio imposible del acta a `hallazgo_candidato_uq`. Medido, hay una segunda y es de dominio: `EstadoDelCandidato.esTerminal()` incluye `VERIFICADO_EN_CAMPO` y `Candidato.verificadoEnCampo()` solo admite venir de gabinete, asi que **el indice parcial no basta** — haria falta ademas devolver el candidato a la cola, y eso contradice un motivo ya escrito en ese enumerado (ADR-0035 punto 5: reutilizar un candidato borra uno de los dos descartes de la tasa). Se toma la opcion **(b)** y se corrige el javadoc **con la medida dentro**. <br><br>**Las cuatro patas de AC-5, medidas antes de escribir la migracion**: `TODA_GEOMETRIA_ENTRA_POR_BATCH` sigue puesta y cierra la via HTTP; no hay cargador batch que escriba `hallazgo.geometria`; el unico camino de produccion —`enCampo`— pasaba `null`, asi que la columna es NULA en toda fila que la aplicacion haya escrito; y **ningun `*Resource` la publica** —la unica mencion en los diez es una frase de `CandidatoResource` diciendo que no sale— ni el contrato que `rentas` declara de sus **dos** lecturas de hallazgos, que tiene once campos y ninguno es ella. Se retira con sus cuatro columnas de marco y su GiST, y de paso sale el `ST_AsText(h.geometria)` que hoy es `ST_AsText(NULL)` en cada fila (#30). Lo que cuesta esta en la cabecera de `V12`: vuelve el dia que haya quien la llene, en otra migracion. <br><br>**Dos defectos de orden que destapo ejecutar, los dos del mismo tipo.** `ContrasteDeAreasJdbcTest` y `PublicacionDelTerritorioJdbcTest` comparten tablas entre casos, y las pruebas nuevas las mueven —cierran vigencias, anaden un predio, dejan el hallazgo anulado—: la de «un hallazgo firme viaja con su inspector» encontraba **cero** segun el orden («expected: 1 but was: 0»). Se cierra restaurando el estado en el `@BeforeEach`, y **por `UPDATE` y no borrando**, porque `kamayuk_app` no tiene `DELETE` sobre `predio` ni sobre `ficha_catastral` — que es el privilegio bien puesto. <br><br>**Y una desincronia preexistente de `main` que este PR solo hace visible**: `docs/50-api/eventos/lote-de-eventos.json` **NO viaja en este commit**, y por que no es una opinion sino una medida tomada por los DOS lados: (a) tras compilar la rama el archivo vuelve **sin modificar**, o sea que el commit ya contenia exactamente lo que un build limpio emite; y (b) sobre `d6dec0f` **limpio**, un `build --rerun-tasks` lo deja modificado, y ese diff es **identico linea a linea** al que el commit llevaba (los dos comparados con `diff`). O sea que las seis lineas son **salida del build y no autoria**, y el desactualizado es `main` y no la rama. Ademas lo que cambian es del PADRON —el motivo de D-11 dando paso a un predio con sus cuatro cifras, mas tres `huella` recalculadas— y no del TERRITORIO, que es lo que este trabajo toca. El archivo se restauro a la version de `d6dec0f` y el commit pasa de **45 a 44** archivos. Arreglarlo es **#28**, y esta medida es la que le ahorra volver a tomarla: la prueba **escribe** en vez de afirmar, asi que CI sigue en verde con el archivo atrasado. <br><br>**Lo que NO se cierra, con su motivo.** (1) **#20 no ha aterrizado** y los asientos nuevos componen JSON a mano como el codigo que los rodea; cuando #20 mezcle, esos llamadores **no compilaran** hasta adoptar su firma nueva, que es el modo de fallo ruidoso. (2) **`rentas` tiene que atender `HALLAZGO_DEJADO_SIN_EFECTO`** en su ingestion; aqui se publica. (3) **El lote de ejemplo no ensena la retractacion**: lo escribe la corrida del PADRON y esto es del TERRITORIO (#28). (4) **`V11` sigue reservada** por otro trabajo de esta ola; el hueco es inocuo y esta medido —`Migrador` declara `.outOfOrder(true)`—. `verificarArquitectura` y `verificarAislamiento`, **verdes los dos** contra PostgreSQL 16 + PostGIS real en el puerto 5435 |
| **#20 — la bitacora escribia texto que no es JSON en una columna `jsonb`, y dos caminos reventaban sin que nada lo viera** (el tipo `DatosDeAuditoria` en `kamayuk.catastro.auditoria` y el paquete nuevo `kamayuk.catastro.json` —`ObjetosDeValorEnJson` y `SerializadorDeJson`—; `RegistroDeAuditoria.con(...)` deja de aceptar `String`; **los 21 archivos** de `src/main` que asientan, reescritos; los **tres** `escapar()` duplicados, borrados; `LaBitacoraSoloAceptaJsonTest` (6), `ElFrenteSeAsientaEnLaBitacoraJdbcTest` (2), `DatosDeAuditoriaTest` (6) y los dos casos nuevos de `ElAtajoNoExisteTest`; el censo de `componenElAreaAManoConMotivo()` pasa de **cinco a dos** — **1 263 -> 1 281** pruebas, 0 fallos) | Siete roturas. Las dos primeras son el issue entero y no hubo que inventarlas: se pusieron **las pruebas nuevas sobre el `src/main` de `main`**, en un *worktree* aparte, sin tocar una linea de produccion. Las otras cinco, cada una **sola** y restaurada **por copia comparada con `cmp`**: (R3) una fabrica `DatosDeAuditoria.crudo(String)`; (R4) una sobrecarga `con(String, String)` usada por `ConfirmarElFrente` con su prosa de antes; (R5) componer `{\"longitud\":…}` a mano y pasarlo como **valor** de un campo; (R6) el paquete raiz de la configuracion cambiado a uno que no existe; y (R7) el parametro `area` de `ComponedorDeHechos.textoDelArea` renombrado a `medida` | **2, 2, 1, 1, 1, 1 y 1 en rojo.** (R1) `ElFrenteSeAsientaEnLaBitacoraJdbcTest` sobre `main` da las dos en rojo con el error del motor: «ERROR: invalid input syntax for type json / Detail: Token "Frente" is invalid», y **ese mismo `main` daba `BUILD SUCCESSFUL` con 1 228 pruebas y 0 fallos** — que es el issue: el defecto llevaba vivo desde #7 y ninguna prueba pasaba por el `cast(:datos AS jsonb)`. (R2) los dos casos nuevos de `ElAtajoNoExisteTest` sobre `main` dan «Token "El" is invalid» —la comilla del inspector `Juan "El Tuerto" Perez`— y «Character with value 0x0a must be escaped» —el salto de linea del motivo de un descarte, que es el defecto exacto del `escapar()` que **si** escapaba la comilla y **no** los caracteres de control—. (R3) «Expecting empty but was: ["public static … DatosDeAuditoria.crudo(java.lang.String)"]». (R4) «Expecting empty but was: ["ConfirmarElFrente.confirmar -> …RegistroDeAuditoria.con(java.lang.String, java.lang.String)"]», y **se lee del descriptor de la LLAMADA y no de la declaracion**, que es lo que hace que una sobrecarga no se cuele. (R5) «Expecting empty but was: ["ProponerLosFrentesDeUnPredio"]». (R6) **es el contraste, y ensena por que hace falta**: la guarda dice «no se encontro ni un llamador de RegistroDeAuditoria.con(...): esta guarda NO MIDIO NADA», y en la misma corrida `ningunLlamadorPasaUnaCadena` **pasa en VERDE sobre el vacio**. (R7) «Expecting empty but was: ["ComponedorDeHechos"]». <br><br>**Por que un tipo y no una validacion en el constructor (AC 2).** Porque la validacion contesta tarde: una cadena que no es JSON compila, se despliega y falla al ejecutar. `DatosDeAuditoria` solo se construye con `campos()...datos()`, que serializa; componer prosa **ya no compila**. Es la regla 2 aplicada a la bitacora. **Y donde vive esta medido, no elegido**: Jackson se nombra en **un solo paquete**, `kamayuk.catastro.json`, que depende de `kamayuk.catastro.dominio` y de nadie mas; `DatosDeAuditoria` vive junto al registro del que es parametro y **no nombra Jackson**, asi que los casos de uso —que se prueban sin contexto de Spring— siguen sin verlo y la regla 7 sigue en pie. Meter la definicion en `kamayuk.catastro.web`, donde ya estaba, habria obligado a la bitacora a depender de la capa de PRESENTACION para escribir una columna, que es literalmente el motivo que los javadoc de los casos de uso daban para componer el JSON a mano. <br><br>**Lo que la medicion destapo sin buscarlo (AC 3).** Se corrio `RevisorDeCodigoFuente.revisarAreas` sobre las **cinco** entradas de `componenElAreaAManoConMotivo()` en el arbol de antes del issue: `ModeloDeLaFichaDelContribuyente` **1**, `ComponedorDeHechos` **1**, `ActualizarFichaCatastral` **1**, `DetectarSubvaluadores` **0** y `VerificarEnCampo` **0**. O sea que **dos de las cinco llevaban desde #6 sin eximir nada**: el escaner busca `area....toString()` o `area....valor().toPlainString()` y las dos escribian `.valor()` a secas dejando que la concatenacion llamara a `BigDecimal.toString()`. El area **si** se componia a mano —por eso #6 las declaro— pero no en la forma que el escaner reconoce, y una exencion que no exime nada es invisible: sigue ahi, eximiendo a la clase que venga con ese nombre. La tercera, `ActualizarFichaCatastral`, **si** eximia algo y es la que este issue quita —el area va tipada y la escribe el serializador—. Cinco menos tres: **dos**. Y con ello se escribio la guarda que faltaba, `ningunaEntradaDeLaListaEstaMuerta`, que es la que R7 muerde. <br><br>**Dos cambios de forma, medidos y deliberados, que son «como se serializa» y no «que se asienta».** (1) Todo decimal de la bitacora sale **entrecomillado** —`"tolerancia":"0.10"`, `"areaDeLaFicha":"120.00"`—, porque `auditoria.datos_nuevos` se republica **verbatim** por `GET /seguridad/auditoria`, donde ningun esquema declara el tipo de nada, y un `0.9100` escrito como numero lo lee el navegador como `0.91`. Esa regla se instala **solo** en el mapeador de la bitacora y no en el de Spring: en el de Spring tocaria el cuerpo de los eventos del buzon, que consume `rentas`, y esa forma no se cambia sin que alguien lo decida. Medido: `lote-de-eventos.json` no cambia por este PR. (2) `Titularidad.porcentaje` pasa de `"50.00 %"` —que es `Porcentaje.toString()`, la tercera convencion de #607 colandose por la concatenacion— a `"50.00"`, que es lo que el serializador ya publica por HTTP. <br><br>**Lo que NO cierra, con su motivo.** (a) **`ComponedorDeHechos` no se toca**: ahi el area se compone a mano **solo para la huella** del hecho sellado, que es un resumen criptografico y no pasa por ningun serializador; cambiarlo moveria la huella de eventos **ya publicados** (C-8). (b) **`RespuestaDeError` sigue componiendo su JSON a mano**, y esta declarada con su motivo en la guarda nueva: escribe el cuerpo `problem+json` de los errores que ocurren **antes** de que haya controlador —la cadena de seguridad y el filtro de tenant—, donde todavia no hay convertidores de mensaje; no escribe ninguna columna `jsonb` y sus cuatro campos son un enumerado y un mensaje del catalogo. (c) **Ninguna guarda de aqui puede ver que la fila entre de verdad en la tabla**: despues de #20 «lo que pasan es JSON valido» es una propiedad de TIPOS, y lo que la guarda del AC 4 comprueba es que hay llamadores, que ninguno pasa una cadena y que no hay puerta trasera. Quien va hasta el motor son las dos pruebas del AC 5, y cubren **dos** de los 21 llamadores. (d) **`candidato.insumos` tambien es `jsonb`** y tambien se compone ahora con el serializador, pero devuelve `String`: el dominio lo guarda como texto y meterle un tipo de la bitacora dentro seria peor. <br><br>**Cifras, medidas en el mismo entorno** (PostgreSQL 16.15 + PostGIS 3.4.4 en el puerto 5433, `./gradlew build --rerun-tasks`, sumando los XML de `test` y `pruebaDeArranque`): **1 263 -> 1 281**, 0 fallos. `verificarArquitectura` y `verificarAislamiento`, **verdes los dos**. |
| **#28 — el lote de ejemplo pasa de TRES tipos a los SIETE, y el frente PROPUESTA deja de cruzar la frontera sin guarda** (`docs/50-api/eventos/lote-de-eventos.json` **regenerado**: de 5 eventos y 3 tipos a **10 y 7**, con el `FRENTE_PUBLICADO` ensenando `"longitudEstado": "PROPUESTA"`, **dos** `HALLAZGO_FIRME` —`SUBVALUADOR` con predio y `OMISO_CATASTRAL` sin el— y el `HALLAZGO_DEJADO_SIN_EFECTO` que llego con `V12` mientras esto estaba en curso; `PublicacionDelPadronJdbcTest` gana el publicador del TERRITORIO y una guarda `@Order(0)` sobre el archivo **comprometido**, mas la asercion del generador derivada del enumerado; `DatosDePrueba.sembrarLosHallazgosQueElLoteEnsena`; el lote como **entrada declarada** de `:kamayuk-catastro-nucleo:test`; y `ContratoConRentasTest` exige que el consumidor **lea** `longitudEstado` — **1 228 → 1 230 pruebas** sobre la base de la que salio, y **1 281 → 1 283** con `#59`, `#61` y `#64` dentro, 0 fallos) | Siete roturas, cada una aplicada **sola** y restaurada **por copia comparada con `cmp`** —«RESTAURADO IGUAL» las siete—: (R1) quitar `MANZANA_PUBLICADA` del lote comprometido; (R1b) la misma **sin la entrada declarada de Gradle**; (R2) un **septimo** tipo en `TipoDeEventoDeCatastro`; (R3) la guarda en `@Order(9)` —detras del generador— con el defecto de R1 puesto; (R4) quitar `longitudEstado` del contrato comprometido de `rentas`; (R5) que el estado del frente deje de leerse de la fila (`EstadoDeLaLongitud.CONFIRMADA` constante); y (R6) publicar **solo** las CONFIRMADAS, que es el recorte que el issue prohibe | **1, VERDE, 2, VERDE, 1, 2 y 3 en rojo. Y la primera no hubo que provocarla: el estado de partida era el defecto.** Con la guarda recien escrita y el archivo tal como esta en `main`, la primera corrida da «**«FRENTE_PUBLICADO» no tiene un solo ejemplo en el lote que `rentas` lee**… but could not find the following element(s): `["FRENTE_PUBLICADO", "HALLAZGO_FIRME", "MANZANA_PUBLICADA"]`». Y la linea base lo dice por el otro lado: sobre `main` **limpio**, `./gradlew build --rerun-tasks` deja `lote-de-eventos.json` **MODIFICADO** —6 lineas, todas del padron: el motivo de `PORCENTAJE_DE_ACTUALIZACION` da paso al de RT-004 en un predio, el otro pasa a tener sus cuatro cifras (`240.00000000`), y tres huellas—, o sea que el archivo llevaba desactualizado desde la firma de D-11 y nada lo decia. <br><br>**(R3) es el hallazgo, y pasa en VERDE.** La guarda nueva y el generador viven en la MISMA clase, y el generador **reescribe el archivo**: con `@Order(9)` en vez de `@Order(0)`, quitarle `MANZANA_PUBLICADA` al lote comprometido da **BUILD SUCCESSFUL, 8 PASSED** — la guarda mide lo que el generador acaba de escribir y no lo que hay en git. El orden no puede quedar a merced de JUnit ni de Gradle (dos clases distintas, o peor, dos tareas, se ordenan como quieran), asi que las dos cosas van en una sola clase ordenada. **Y hacen falta las dos**, porque se rompen en direcciones opuestas: un archivo regenerado y **no comprometido** —que es exactamente lo que paso en #38— solo lo ve la guarda; una fixture que deja de producir un tipo solo la ve la asercion del generador. (R2) lo mide: **2 en rojo**, uno por cada direccion, las dos nombrando «`RETRACTACION_PUBLICADA`». <br><br>**(R1b) es la leccion de #192 punto 2 / C-1 reproducida, y tambien pasa en VERDE.** Sin declarar `docs/50-api/eventos/lote-de-eventos.json` como entrada de `:kamayuk-catastro-nucleo:test`, quitarle un tipo al archivo deja la tarea **`UP-TO-DATE`** y el build sale «**BUILD SUCCESSFUL in 1s**» — la guarda no llega a correr y el archivo **se queda mutado** (`grep -c MANZANA_PUBLICADA` → 0). Medido con la tarea ya asentada, para que el rojo no viniera de haber tocado el `build.gradle.kts`. <br><br>**(R4) es el hallazgo de la frontera, y su mitad verde es lo que decide donde va la guarda.** Quitar `longitudEstado` del contrato comprometido de `rentas` deja «este backend cumple lo que su consumidor espera de el» en **VERDE**: esa comprobacion compara por **contencion** y en una sola direccion —el proveedor no puede publicar MENOS de lo que el consumidor lee—, y que el consumidor deje de leer un campo no es un desajuste para ella. Lo unico que muerde es la guarda nueva, con «`catastro` publica cada frente con el estado de su longitud y `rentas` no lo lee… sin ese campo las dos llegan iguales, y quien determine un arbitrio sobre metros que nadie confirmo no tiene como saberlo». **Va en el PROVEEDOR y no en el consumidor**, por ADR-0030 §4 y por lo que P5E §6.3 se nego a escribir: del otro lado mediria dos archivos del mismo repositorio y el rojo le llegaria a quien no rompio nada. Y la tarea **si** volvio a correr al mutar el archivo del clon hermano, que es lo que AC-5 manda comprobar antes de dar por buena una rotura. <br><br>**El AC-2 pregunta por que la regeneracion de #38 no los trajo, y la respuesta se midio: NO era la siembra.** `sembrarTenant` deja desde #7 una manzana, un frente PROPUESTA y un hallazgo FIRME en **toda** municipalidad; lo que faltaba es que el generador llamaba solo a `PublicacionDelPadron`, y los tres tipos de `V10` los emite `PublicacionDelTerritorio`, al que nadie llamaba desde ahi. **De la siembra era una sola cosa**, y es el par del hallazgo: solo habia un `SUBVALUADOR`, y el `OMISO_CATASTRAL` —el unico caso del buzon donde `predio_id` viaja NULO, que `catastro_evento_predio_ck` admite a proposito— no lo sembraba nadie. Entra como `DatosDePrueba.sembrarOmisoCatastral` y **solo en la municipalidad del lote**: las demas pruebas cuentan hallazgos, y una fila mas cambiaria lo que miden sin que midan nada nuevo. Con un solo ejemplo, el consumidor escribe un `NOT NULL` sobre esa columna y revienta con el primer omiso que llegue. <br><br>**Y lo que el issue prohibe hacer —recortar las propuestas— ya estaba guardado, medido y no supuesto**: (R6) publicar solo las CONFIRMADAS da **3 en rojo**, incluido «Expected size: 9 but was: 8» en el lote y «Expected size: 2 but was: 1» en el contraste de #7. (R5) —el estado leido como constante en vez de la fila— da 2, y el rojo ensena el cuerpo entero con `"longitudEstado": "CONFIRMADA"` donde la fila dice PROPUESTA. <br><br>**Y la guarda mordio sobre codigo real antes de que este PR se abriera, que es la mejor prueba de que muerde.** Al traer `main` —#59 y #61— llego `V12`, que anade un **septimo** tipo al buzon: `HALLAZGO_DEJADO_SIN_EFECTO` (#23). El lote comprometido no tenia su ejemplo y **la guarda salio roja sola, nombrandolo**: «`HALLAZGO_DEJADO_SIN_EFECTO` no tiene un solo ejemplo en el lote que `rentas` lee». Es exactamente el caso que AC-2 describe —«si anades un septimo tipo sin ejemplo, tiene que salir roja nombrandolo»— ocurrido **sin provocarlo**, cinco horas despues de escribirla. El ejemplo entra en la misma fixture, con los tres campos del acto (`motivo`, `anuladoPor`, `anuladoEn`), y el lote queda en **10 eventos y 7 tipos**. **Sin la guarda, `V12` habria aterrizado dejando el septimo tipo sin ejemplo exactamente igual que `V10` dejo los tres suyos** — que es todo lo que este issue existe para impedir. De paso, traer `main` obligo a corregir la fixture nueva en dos sitios y los dos los dijo el motor: `campania.tope` es `NOT NULL` sin `DEFAULT` desde `V12` («null value in column «tope» … violates not-null constraint») y `hallazgo.geometria` **ya no existe** («column «geometria» of relation «hallazgo» does not exist»), porque `V12` la retiro: «una geometria que nadie puede llenar se retira».<br><br>**Cifras, medidas en el mismo entorno** (PostgreSQL 16.13 + PostGIS en el 5433, `./gradlew build --rerun-tasks`, sumando los XML de `test` y `pruebaDeArranque`): sobre `origin/main` (`d6dec0f`), arbol limpio, **1 228** con 0 fallos, y esta rama **1 230** — el delta de este trabajo son exactamente **dos** metodos de prueba, la guarda del lote y la del contrato—. Traido `main` dos veces —`93e6994` con #59 y #61, y `c5a820c` con #64— la rama entera da **1 265** y luego **1 283**, siempre con 0 fallos. **Y el +2 queda confirmado tres veces y por dos manos**: el PR de #64 midio `main` (`93e6994`) en **1 263** y `main` (`c5a820c`) deja **1 281**, o sea exactamente dos menos que esta rama en los dos casos. `verificarArquitectura` y `verificarAislamiento`, **verdes los dos**. El lote es **estable**: la corrida completa lo reproduce byte a byte (`cmp` vacio), que es lo que hace que su diff vuelva a significar algo. **Ninguna migracion**: `V11` no existe y no hacia falta ninguna — lo que faltaba era publicar, no esquema. <br><br>**Lo que NO cierra, con su medida.** (a) **`rentas#15`, la mitad del consumidor**: alli `FrenteInscrito.longitudEstado` se **transporta** y **ningun caso de uso lo lee** —medido con `grep` sobre `src/main` de `rentas`: `FrentesDelPredio` aparece en **dos** archivos, el puerto y su adaptador HTTP, y en ningun llamador—. Esta guarda exige que el consumidor **declare** que lo lee; que lo **use** para decidir es de aquel issue, y sus AC-6 y AC-7 —la fila en el `CLAUDE.md` de `rentas` y su PR— van con el. (b) La guarda mide que el consumidor **declare** el campo, no que lo **use** para decidir: eso es `rentas#15`. (c) **La colision de `git stash` entre carriles**: `w1/catastro` y `w3/catastro` son *worktrees* del mismo `.git` y **comparten la pila de stash**, asi que un `pop` puede devolver el trabajo de otro agente —paso, se recupero entero por el SHA del commit de stash y quedo avisado—; no se arregla desde aqui, se declara |
| **#51 — el backend deja de decir solo si UN ejercicio esta sellado y pasa a decir CUALES lo estan** (`GET /catastro/api/v1/seguridad/parametros/ejercicios`, con `SESION_PROPIA` como la lectura vecina; `CacheDeSnapshots.conjuntosCacheados()` y su `record ConjuntoCacheado`; el `DISTINCT ON (ejercicio)` de `CacheDeSnapshotsJdbc` con el desempate escrito **una sola vez** y compartido con `conjuntoCacheadoDe`; el caso de uso `EjerciciosSellados` con su `@Transactional(readOnly = true)`; `EjerciciosSelladosJdbcTest` —7 pruebas contra PostgreSQL real y tres municipalidades— y `EjerciciosSelladosFronteraTest` —3 de capa web—. **Ninguna migracion: no hace falta ni una columna nueva**, y esa es la respuesta medida al «de donde sale el dato» — **1 283 → 1 293 pruebas**, 0 fallos) | Nueve roturas, cada una aplicada **sola** y restaurada **por copia comparada con `cmp`** —«RESTAURADO IGUAL» las nueve—: **(R1)** devolverle a `normativa_conjunto` una politica RLS `USING (true)`, que es la unica forma de que esta lectura devuelva los conjuntos de **todas** las municipalidades; **(R2)** quitarle el `@Transactional` al caso de uso; **(R3)** contestar la lista vacia con **404**; **(R4)** invertir el orden a `ejercicio ASC`; **(R5)** quitar el `DISTINCT ON`; **(R6)** que el desempate de la lista **diverja** del que usa el calculo (`version ASC` escrito a mano, sin la constante); **(R7)** dar los ambitos por supuestos (`'OBLIGACION,VALUACION'` literal); **(R8)** que la respuesta deje de decir a que fecha, devolviendo la lista pelada; y **(R9)**, el contraste, que la siembra deje **cero** conjuntos | **5, 6, 1, 4, 5, 3, 1, 2 y 6 en rojo.** <br><br>**(R1) es el AC-6(a), y es la unica forma de escribirlo: bajo RLS no se puede.** La lectura no lleva ningun `WHERE municipalidad_id` —la regla 2 lo prohibe y `RepositorioJdbc` lo dice con todas las letras— asi que quien acota es la politica de `V2`; como `kamayuk_app`, con `FORCE ROW LEVEL SECURITY`, **no hay mutacion de `src/main` que haga aparecer la fila de la vecina** salvo tocar la politica. Es la rotura 7 de T-0 por el mismo camino. Sale **2 en rojo en `AislamientoMultiTenantTest`** —«normativa_conjunto: fuga de filas de la municipalidad B: expected 0L but was 1L» y «sin contexto la consulta debe fallar … Expecting actual: null to be in: [«22P02», «42704»]»— **y 3 en `EjerciciosSelladosJdbcTest`**, de las que la que mas dice es la de la municipalidad **recien implantada**: «Expecting empty but was: [ConjuntoCacheado[ejercicio=2026, conjuntoId=51002, version=2, ambitos=[OBLIGACION, VALUACION]], …(3)]» — un inquilino sin un solo conjunto propio enseñando los cuatro de los otros dos. <br><br>**Y las dos municipalidades comparten el 2026 a proposito, porque hay una fuga que la lista de anios NO puede ver.** Si sembraran anios disjuntos, cualquier fuga cambiaria la lista y se veria; con las dos teniendo 2026, una lectura sin RLS deja el **ano** en su sitio y cambia el **conjunto** —el `DISTINCT ON` se quedaria con el de A, que es el de version mayor—. Por eso se afirma ademas que el 2026 de la vecina es el suyo y no `51002`. <br><br>**(R2) da 6 en rojo con el mensaje de #486 literal**: «Caused by: org.postgresql.util.PSQLException: ERROR: unrecognized configuration parameter "app.municipalidad_id"», sobre el SQL entero. Que muerda no es gratis: el caso de uso se envuelve en la prueba con el **interceptor transaccional de verdad** (`ProxyFactory` + `TransactionInterceptor`), como hace `SinNormativaFronteraTest`; llamandolo a pelo, quitar la anotacion no habria puesto nada rojo y el defecto habria sido invisible hasta levantar el sistema. <br><br>**(R3) da «expected: 200 but was: 404»**, que es el AC-2. La primera version de esta rotura se escribio con `ResponseStatusException` y salio **500 y no 404**, porque `ManejadorDeErrores` no lo traduce en un `standaloneSetup`: el rojo era rojo pero medía otra cosa, asi que se rehizo con `ResponseEntity.notFound()`, que es como se escribiria el defecto de verdad. <br><br>**(R6) es la que sujeta la decision de diseño.** El desempate —`version DESC, conjunto_id DESC`— lo usan **dos** consultas: la que el calculo consulta (`conjuntoCacheadoDe`) y la que se ofrece para elegir. Escrito dos veces son dos sitios con la misma verdad, que es la forma de defecto que C-17 encontro cinco veces; se escribe una y se comparte. **Pero una constante compartida garantiza que el TEXTO sea el mismo, no que las dos consultas lo usen igual** —una es `DISTINCT ON` y la otra `LIMIT 1`—, asi que hay ademas una prueba que recorre la lista publicada y exige que, ejercicio a ejercicio, `conjuntoCacheadoDe` devuelva el mismo conjunto: «si divergieran, la interfaz ofreceria el ejercicio 2026 y el calculo tomaria otro conjunto, sin que nada lo dijera: expected 1L but was 51002L». <br><br>**(R7) mide una decision que el issue dejaba abierta y la medida cerro: el `ambito` SI se publica.** La copia local tiene una fila **por mitad del snapshot** y los dos cuadros de la valuacion solo viajan en `VALUACION`, asi que un ejercicio con solo `OBLIGACION` descargado esta sellado **y sus cuadros no estan aqui**. Callarlo dejaria la lista prometiendo unos valores unitarios que este sistema no tiene, que es la clase de respuesta plausible y equivocada que no se distingue de la correcta. Con el literal puesto: «Expecting actual: ["OBLIGACION", "VALUACION"] to contain exactly: ["OBLIGACION"]». <br><br>**(R8) es el AC-3, y el caso que lo hace visible es el vacio**: la respuesta pasa de `{"aLaFecha":"2026-09-07","ejercicios":[]}` a `[]`, o sea «no hay ninguno» dejando de distinguirse de «no se pudo leer». **Y la fecha no se puede pedir**, con su motivo escrito: la copia local no guarda vigencia sino `descargado_en`, asi que contestar a «¿que ejercicios habia sellados en marzo?» seria contestar a «¿cuales se habian descargado en marzo?» — otra frase, plausible, e indistinguible de la que se hizo. <br><br>**(R9) es el contraste del AC-6, y encontro un defecto en mis propias pruebas.** Con la siembra vaciada salen 6 en rojo, pero **dos de ellas eran un `NoSuchElementException` pelado** —de un `getFirst()` sobre la lista vacia—, que dice donde revento y no que faltaba el sujeto. Se arreglo la verificacion y no se tapo: toda comprobacion que mire dentro de la lista lo afirma antes, con un motivo compartido, y la misma rotura pasa a decir «la lista de la municipalidad que consulta esta vacia: lo de abajo se cumpliria sobre el vacio, asi que esto no habria medido nada». **Y la premisa se cuenta FUERA de la lectura que se mide** —`SELECT count(*)` con la conexion de superusuario, la unica que omite RLS—: contarla con la misma consulta seria circular, y un `DISTINCT ON` roto pondria rojas la guarda y la respuesta a la vez con un mensaje que acusa a la siembra. <br><br>**Y lo que se midio antes de escribir codigo, que es lo que evito una migracion**: el dato ya esta aqui. `normativa_conjunto` (`V2`) trae `ejercicio`, `version`, `ambito` y su politica, y `CacheDeSnapshotsJdbc` ya la leia. `V11` esta tomada por #59 y **no hizo falta pedir `V14`**: escribir esquema para publicar lo que ya esta guardado habria sido un segundo sitio con la misma verdad. **Tampoco hace falta opcion nueva en `CatalogoDelSistema`**: la ruta usa el centinela `SESION_PROPIA` —basta con estar autenticado—, por el motivo que el javadoc de esa clase ya escribia para la lectura vecina, y `CatalogoDelSistemaTest` lo excluye **por su valor**; medido, la guarda sigue en verde y el catalogo se queda en 16. <br><br>**Cifras, medidas en el mismo entorno** (PostgreSQL 16 en el puerto 5435, `./gradlew build --rerun-tasks`, sumando los XML de `test` y `pruebaDeArranque`): **1 283 → 1 293**, 0 fallos; `verificarArquitectura` y `verificarAislamiento`, **verdes los dos**. **La linea base se midio CINCO veces, y esa es la parte que conviene leer**: 1 228 sobre `d6dec0f`, **1 238** sobre `c9f839a` —`main` con `V11` de #59—, **1 263** sobre `93e6994` —con `V12` de #61—, **1 281** sobre `c5a820c` —#64— y **1 283** sobre `32c3784`, con #28. El delta de este trabajo es el mismo en las cinco, **+10**, que son sus diez pruebas nuevas —7 contra PostgreSQL y 3 de capa web—; lo que se movio debajo fue `main`, cuatro veces, mientras esto estaba en curso — y cada vez se volvio a medir el par, porque una linea base tomada contra otro `main` no dice nada del delta. Y se midio siempre en un **worktree aparte** (`git worktree add`) y no con `git stash`: los cuatro clones de esta maquina son worktrees del MISMO `.git` y comparten la pila de stash, asi que un `push` aqui y un `pop` alli se cruzan — paso, con el carril de #28, y lo que devolvio el `pop` fue el trabajo del otro. Y `yarn rutas` del frontend sigue verde, con **57 → 58 rutas del backend**: la lectura nueva cuenta y no rompe ninguna de las 33 que la interfaz declara. <br><br>**Lo que este issue NO cierra, con su motivo.** (a) **El desplegable de `catastro-web` sigue saliendo del reloj y no de esta lectura**, y es deliberado: el issue lo dice —«no decide que hace el frontend con la lista», eso es #48— y consumirla desde aqui obligaria a tocar `servidas.ts`, que **afirma** que el backend contesta esa ruta donde corre la aplicacion; encenderla sin los dos procesos levantados convierte la barra global en un fallo de conexion. Lo que cambia hoy es que **la suposicion ya tiene a quien preguntarle**. (b) **Un conjunto sellado en `normativa` y no descargado aqui todavia no aparece**, y esa asimetria esta escrita en el puerto: es la misma que `conjuntoCacheadoDe` declara, y es lo que hace que esta lectura conteste con `normativa` caido. (c) **`CacheDelEscenario` —el escenario de `normativa` de los fixtures— no sustituye esta lectura: lanza**. Su tabla no tiene `ambito` y contestar obligaria a inventarlo; devolver una lista vacia habria dejado a cualquier prueba que la usara midiendo sobre el vacio, en verde. |
| **#27 — higiene del esquema: un indice que se retira, uno que NO se retira porque el encargo lo leyo al reves, dos comentarios falsos y un `CHECK` que ataba una sola direccion** (`V13__higiene_del_esquema.sql`: `DROP INDEX frente_predio_ix`, `candidato_omiso_sin_predio_ck` y cuatro `COMMENT ON` que reemplazan los de `V6`, `V7`, `V9` y `V10`; `FRENTES_DEL_PREDIO` extraida a constante para que el plan se le pida a produccion; tres clases nuevas —`CandidatoOmisoSinPredioTest`, `ComentariosQueDicenLaVerdadTest` y `FrentesEnElIndiceTest`— y `PrefijoDeLaHabilitacionEnElIndiceTest`; **1 238 → 1 252 pruebas**, 0 fallos) | Nueve roturas, cada una aplicada **sola** y restaurada **por copia comparada con `cmp`** —«RESTAURADO IGUAL» las nueve—: (R1) quitar el `DROP INDEX frente_predio_ix`; (R2) reordenar `frente_predio_via_uq` a `(municipalidad_id, via_id, predio_id)`, misma unicidad y otro prefijo; (R3) retirar `habilitacion_urbana_codigo_prefijo_ix`, que es la decision contraria de AC-3; (R4) devolver `RangoDePrefijo` a `>=` / `<`; (R5) y (R6) quitar los dos `COMMENT ON COLUMN` de `frente_predio`; (R7) quitar el `CHECK` nuevo, que es el estado exacto anterior a `V13`; (R8) declararlo `NOT VALID`; y (R9) quitar el `COMMENT ON COLUMN candidato.predio_id` | **1, —, 3, 2, 1, 1, 2, 1 y 1 en rojo.** (R7) es el AC-8 literal: «Expecting code to raise a throwable», o sea el `OMISO_CATASTRAL` con predio entrando —medido antes de escribir nada contra el esquema de `V11`, la fila entra y devuelve `4001 \| OMISO_CATASTRAL \| 1`—. (R5) enseña el comentario que un DBA lee hoy: «La que midio el tecnico. NO se deriva de la geometria», que `V10` volvio medio falso al ponerle un derivador. (R8) da «Expecting value to be true but was false» sobre `convalidated`. <br><br>**(R2) PASO EN VERDE, y es el hallazgo.** La primera version de la prueba exigia el nombre del indice y las dos columnas en el `Index Cond` —que es el liston de #17 y de #4—, y con el indice reordenado **el plan sale byte a byte igual**: `Index Scan using frente_predio_via_uq` con `Index Cond: ((municipalidad_id = current_setting(...)) AND (predio_id = 1500))`. Con `predio_id` detras de `via_id` deja de ser condicion de BUSQUEDA y pasa a ser un filtro DENTRO del indice, que PostgreSQL sigue imprimiendo en el `Index Cond` — o sea **el quinto hallazgo de RLS por un tercer eje**: el plan dice «Index», nombra el indice bueno **y** nombra la columna, y aun asi recorre el rango entero del inquilino. Lo unico que los separa son los bloques, y por eso la prueba los cuenta: `EXPLAIN (ANALYZE, BUFFERS)` da **6** con el orden bueno y **57** con el roto sobre 9 000 frentes. La verificacion se arreglo y no se tapo. <br><br>**Y una premisa del encargo salio FALSA al medirla, asi que una de las cinco correcciones se hace al reves de lo que pedia.** #27 daba por muerto `habilitacion_urbana_codigo_prefijo_ix` porque «una comparacion de rango (`>=` / `<`) usa la clase por omision». **En este repositorio la busqueda por prefijo no se escribe asi**: `RangoDePrefijo.condicion` emite `~>=~` y `~<~`, que son los operadores de `varchar_pattern_ops`, y DAT-01 §0 hallazgo 3 escribe la mitigacion con todas las letras —«un rango con los operadores de `text_pattern_ops` …, **sobre un indice declarado con esa clase de operadores**»—. Medido como `kamayuk_app` sobre 12 000 habilitaciones en dos municipalidades: el rango de la convencion llega a `habilitacion_urbana_codigo_prefijo_ix` (102 bloques); el rango con `>=` / `<` llega a `habilitacion_urbana_codigo_uq`; y la igualdad cuesta **3 bloques** con y sin el, o sea que **no compite** —que era la otra objecion posible, la que le costo el puesto a `zonificacion_vigencia_ix` en `V7`—. **Y retirarlo reproduce el quinto hallazgo de RLS literal**, que es lo que (R3) imprime: `Bitmap Index Scan on habilitacion_urbana_marco_ix` con solo la politica en el `Index Cond`, `Filter: (codigo ~>=~ 'HU-0007' AND codigo ~<~ 'HU-0008')` y `Rows Removed by Filter: 5900` — 133 bloques contra 102. Se **conserva**, y lo que se corrige es el comentario, que decia «la clase de operadores que el rango usa» sin decir cuales y de ahi salio la lectura equivocada. (R4) es lo que ata las dos piezas: con `RangoDePrefijo` devuelto a `>=` / `<`, la prueba pasa a ver `habilitacion_urbana_codigo_uq` y se pone roja. <br><br>**Lo que si se retira, con la medida delante.** `frente_predio_ix (municipalidad_id, predio_id)` es prefijo estricto de `frente_predio_via_uq (municipalidad_id, predio_id, via_id)`, y con los dos puestos el planificador **ya elegia el compuesto**: `pg_stat_user_indexes` daba **0** recorridos del primero y todos del segundo. El plan de `FrentesDelPredioJdbc.deUnPredio` es **identico bloque a bloque antes y despues** —`Index Scan using frente_predio_via_uq`, `shared hit=23`—, asi que la lectura no pierde nada; lo que cambia es la escritura, medida en **WAL** y no con reloj porque el reloj de una maquina compartida no distingue 200 ms de 300: 3 000 altas de frente cuestan **21 230 registros / 2,45 MB** con el indice y **18 220 / 2,22 MB** sin el, o sea ~3 010 de diferencia, uno por fila. Es lo que `V7` escribio al retirar `zonificacion_vigencia_ix`. <br><br>**El `CHECK` va VALIDO y no `NOT VALID`, y se midio porque el encargo lo preguntaba.** El cuarto hallazgo de RLS es de las FORANEAS —validar una foranea ejecuta una CONSULTA sujeta a la politica, y el migrador corre sin `app.municipalidad_id`—; validar un `CHECK` es un recorrido del monton que hace el motor. Comprobado sobre 4 000 candidatos de dos municipalidades y **sin** contexto de tenant: `convalidated` sale `t`; y el contraste que impide que eso sea una validacion en vacio —una fila que lo viola escrita en la municipalidad 2, con el contexto ya reseteado— hace morir el mismo `ALTER TABLE` con «check constraint "candidato_omiso_sin_predio_ck" of relation "candidato" is violated by some row». El censo previo dio **cero** filas que lo violen. <br><br>**Y el numero es el TRECE, y ninguna de las dos que van delante choca — medido, no supuesto.** Esta rama sale de la de #22, que se lleva `V11`, y detras de ella el PR de #23/#24/#25 se lleva `V12`; el encargo decia `V12` porque no se habia mirado ese tercer carril, y dos migraciones con el mismo numero son un Flyway que se niega a migrar en cuanto las dos ramas coinciden en `main`. `V11` toca `zonificacion` y solo `zonificacion`; la `V12` ajena toca `campania`, `hallazgo` y los dos `CHECK` cruzados de `catastro_evento` —y le quita a `hallazgo` su geometria y su marco—; esta toca `frente_predio`, `habilitacion_urbana` y `candidato`. **Ni un objeto en comun**, y las trece aplican limpias EN ORDEN sobre una base de cero: se trajo la `V12` ajena con `git show` para poder medirlo y **no se commitea**, porque es de su PR. El orden de mezcla es, entonces, **despues de #59 y despues de #61**; que `outOfOrder(true)` este puesto desde #722 hace que un numero saltado no rompa una base ya migrada, pero lo que `outOfOrder` **no** arregla —y lo dice su propio javadoc— es que dos ramas reclamen el mismo numero, que es justo lo que este renumerado evita. Ninguna migracion aplicada se edita: Flyway guarda el checksum y `validateOnMigrate` esta activo, asi que los cuatro comentarios falsos se corrigen **donde si se puede**, en el `COMMENT ON` que la migracion nueva reemplaza, y se fijan con una prueba que lee `col_description` del **catalogo** y no el archivo de la migracion. <br><br>**Cifras, medidas en el mismo entorno** (PostgreSQL 16 + PostGIS en el 5432, `./gradlew build --rerun-tasks`, sumando los XML de `test` y `pruebaDeArranque`): **1 238 → 1 252, 0 fallos**. **Y la linea base se midio DOS veces, porque la rama padre se movio debajo**: la primera contra `c39ceb1` y la segunda contra `9c95d56`, que ya trae #57 —que reescribio `CatalogoDelSistemaTest` para leer bytecode en vez del fuente y lo movio de modulo— y #58. Las dos dan **1 238**, asi que el delta se sostiene; pero se volvio a medir en vez de darlo por bueno, porque una cifra medida contra una base que ya no existe es exactamente lo que este registro existe para evitar. La de en medio, ademas, **salio contaminada y se descarto**: se corrio mientras yo cambiaba desde `psql` las claves de `kamayuk_owner` y `kamayuk_app`, que son roles del CLUSTER y no de la base, asi que el build murio con «password authentication failed» en tres modulos — se repitio con el arbol en `git stash`; `verificarArquitectura` y `verificarAislamiento`, **verdes los dos**; `V1..V13` aplican limpias sobre una base de cero —con la `V12` de #61 en medio— y las tablas siguen siendo **47** (48 contando `spatial_ref_sys`, que la trae PostGIS): `V13` no crea ninguna, asi que el reparto de la regla 11 y `RevisorDeEsquema` no se mueven —AC-6 comprobado ejecutando, no razonado—. <br><br>**Lo que NO se cierra, con su motivo.** (a) `habilitacion_urbana` **sigue sin que nadie la consulte** —ningun `.java` de `src/main` la nombra, medido—: si esa tabla debe tener codigo es **#31**, y este issue no lo decide; lo que hace es impedir que la respuesta a «no la consulta nadie» sea retirar el indice y dejar el defecto puesto. (b) **Las filas anteriores a `V10` no se reetiquetan**: `frente_predio` no guarda cuando se escribio `longitud_m` sino cuando se creo la fila, asi que una anterior a `V10` que el derivador haya tocado despues no se distingue de una que no, y reetiquetar por fecha convertiria una suposicion en una cifra oficial (ADR-0021). Hoy no cuesta nada —cero filas en cualquier instalacion— y el comentario existe para el dia que las haya. (c) **No se toca `frente_predio_via_uq`**, que es lo que hace idempotente al derivador, ni `frente_principal_uq`, que es PARCIAL y no lo cubre nadie. |
| **#29 — las dos guardas que no podian fallar, y el defecto que las dos dejaban pasar en VERDE** (el contraste de `CatastroNoNombraUnArbitrioTest` pasa a correr **el recorrido de verdad** sobre `muestras/nombra-un-arbitrio/`, un archivo de disco que viola ADR-0024 a proposito; el recorrido se extrae a `fuentesBajoMain(raiz)` y el escaner a `hallazgosEn(archivos)`, para que las dos pruebas ejerzan **la misma cadena**; `MargenDelMarco` gana `cubre(MarcoGeografico)` y `avisoSiNoCubre(MarcoGeografico)` —funciones puras—, `FrentesDelPredio` gana `marcoDelPadron()` y la derivacion lo pregunta **una vez por corrida** y lo publica en su `Informe`, que `DerivarFrentes` saca por `log.warn`; la muestra entra como **entrada declarada de Gradle**; **1 228 → 1 235 pruebas**, 0 fallos) | **Los dos VERDES DE PARTIDA, medidos primero sobre el arbol intacto, y seis roturas despues, cada una aplicada SOLA y restaurada por copia comparada con `cmp` —«RESTAURADO IGUAL» las seis—**: (R1) `/src/main/` → `/src/principal/` en el filtro de rutas; (R2) `LATITUD_MAXIMA` de `26.0` a `26.2`; (R3) que `avisoSiNoCubre` devuelva **siempre** `null`; (R4) que la latitud que decide sea **solo el sur** y no la mas lejana del ecuador; (R5) apartar la muestra del disco; y (R6) que la derivacion **deje de preguntar** | **1 → 2, 1, 4, 4, 1 y 1 en rojo, y los dos verdes de partida son la mitad del entregable.** <br><br>**(a) El contraste del arbitrio no ejercia nada, y esta medido**: con `/src/main/` roto sobre el arbol de `main`, `laPruebaPuedeFallar` sale **PASSED** —y con ella `ningunArchivoNombraUnServicioDeArbitrio`, que da «no hay hallazgos» **sobre el conjunto vacio**—; el unico rojo es `seLeyoUnArbolDeVerdad`, «Expecting size of: [] to be greater than 200 but was 0». O sea que de las tres, la que dice «esta prueba puede fallar» era la unica que no podia. Comparaba tres literales contra otro literal escrito dos lineas mas arriba. Con la muestra, la misma rotura da **2 de 3** y la segunda nombra el sujeto que falta: «sin la muestra en «muestras/nombra-un-arbitrio» este contraste se cumpliria solo, que es el defecto que existe para atrapar: Expected size: 1 but was: 0». <br><br>**(b) La tautologia de `LATITUD_MAXIMA` se demuestra en dos pasos, y el segundo es el que duele.** Mutando **solo** la constante a `26.2`, el arbol de `main` da **1 en rojo** y el rojo no habla de geodesia sino de una copia: «[y el limite esta escrito, no supuesto] expected: 26.0 but was: 26.2». Y lo que cualquiera hace con ese rojo es editar el literal del test — hecho, y el resultado es **`BUILD SUCCESSFUL`** con `LATITUD_MAXIMA = 26,2`, que es **exactamente el valor que #7 midio y descarto** («a 26,2° un grado de longitud mide 99 883 m, o sea MENOS de 100 000»). La prueba no protegia el supuesto: protegia que alguien hubiera tecleado 26 dos veces. Con la constante pinzada contra la geodesia, la misma rotura da «en el limite que MargenDelMarco declara (26.2 grados) el margen TIENE que cubrir la tolerancia, o el javadoc promete una banda que no vale: **Expecting actual: 7.990624136971701 to be greater than or equal to: 8.0**», y **ya no hay ningun literal del test que editar** — el 26,0 no aparece en la prueba: se LEE de la constante y se contrasta contra `111 320 × cos(lat)`, por los dos lados (al limite cubre, una decima mas alla no). Con 26,2, con 26,5 o con 40 cae el primer lado; con 25,9 o con 10, el segundo. <br><br>**Y el supuesto pasa de estar escrito a DECIDIR algo, que es la otra mitad del AC-2.** Hasta #29 `LATITUD_MAXIMA` no la consultaba **nadie**: `git grep LATITUD_MAXIMA` sobre `backend/*/src/main` de `main` devuelve **una sola** aparicion de la suya, su propia declaracion (las otras tres son la constante privada y homonima de `MarcoGeografico`, que significa otra cosa: los ±90 del rango de coordenadas), asi que una instalacion fuera de la banda habria derivado frentes de menos **sin ningun sintoma**: el marco ensanchado descarta la via antes de que el `ST_DWithin` la vea, y un frente que no se propone es indistinguible de un predio que no da a la calle. Ahora la derivacion lee el marco del padron —un agregado sobre las cuatro columnas de marco, **sin un solo operador espacial** (ADR-0034)— una vez por corrida y lo dice. (R3) lo mide: con el aviso mudo salen **4 en rojo en tres clases y en las tres capas** —la funcion pura, el recorrido con su doble y el corte contra PostGIS de verdad—, todas con «Expecting actual not to be null». (R6), la mitad simetrica —el aviso existe y nadie lo pregunta—, da 1. **Y (R4) es la mitad que ninguna de las otras caza**: mirando solo el sur, un padron que empieza a 25° y llega a 40° se declara **cubierto**, y el unico rojo que lo dice es «decide la latitud mas lejana del ecuador, no la del sur: Expecting value to be false but was true». <br><br>**(R5) es el hallazgo del metodo, y la primera version lo dejaba pasar.** Apartar la muestra del disco daba **`BUILD SUCCESSFUL` en 1 s** con `:kamayuk-catastro-aplicacion:test` en **UP-TO-DATE**: el arbol de muestras no esta en ningun conjunto de fuentes de Gradle, asi que borrarlo no era un cambio para la tarea. Es la leccion de #192 punto 2 por tercera vez en este repositorio —la misma que C-1 cerro para el contrato del consumidor que vive en otro clon—, y se cierra declarando `../muestras` como entrada de la tarea; con ella, la misma rotura da 1 en rojo. **La verificacion se arreglo y no se tapo.** <br><br>**Y la muestra vive en la RAIZ del repositorio y no bajo `backend/`, por una razon medida**: el filtro que hay que ejercer es `/src/main/`, asi que la muestra tiene que estar bajo un `src/main/` suyo — y cualquier sitio bajo `backend/` la meteria tambien en el recorrido de PRODUCCION de esa misma prueba **y** en el de los cinco escaneres de `comun-verificaciones`, que recorren `backend/` buscando exactamente eso. La muestra pondria roja la guarda que existe para demostrar. Fuera de todo conjunto de fuentes, ademas, ni Checkstyle ni Spotless la miran (`spotless { java { target("src/**/*.java") } }`, relativo a cada subproyecto) y el `Dockerfile` no la copia (`COPY backend/ ./`). <br><br>**AC-3, y el metodo importa mas que el resultado**: se barrieron **todas** las pruebas del backend con un guion, no a ojo — para cada metodo `@Test`/`@ParameterizedTest` se comprobo si su cuerpo nombra alguna clase de `src/main`, y aparte si el SUJETO de algun `assertThat` es una constante declarada en el propio archivo de prueba. La segunda pasada devuelve **exactamente dos** casos: `CatastroNoNombraUnArbitrioTest` —el de este issue— y `assertThat(TABLAS_EXENTAS)` de `AislamientoMultiTenantTest`, que **no** es tautologico: contrasta la lista contra `pg_class` de la base viva y su rojo dice «una exencion que ya no exime nada es peor que ninguna». **Lo que se encontro y NO se arregla, con su motivo**: tres guardas leen un archivo por una ruta escrita en la prueba (`NingunMotivoDesbordaAlConsumidorTest` → `ValorizacionDelPredio.java`, `GuionDeCargaDeRiesgoTest` → `cargar-riesgo.sh`, `ProhibicionesEnElCodigoFuenteTest` → el directorio de migraciones), y una ruta que deje de resolver las dejaria midiendo el conjunto vacio; **las tres ya llevan su centinela** —«si esto sale vacio, el recorrido dejo de encontrar los motivos y no vigila nada», `hasSizeGreaterThan(40)`—, asi que el hueco esta cubierto por dentro y lo que les falta es la **entrada de Gradle**, que es el mismo defecto que (R5) destapo y que aqui se cierra solo para `muestras/`. Se dice y no se hace: declararlas es otro trabajo y toca tres `build.gradle.kts`. <br><br>**Cifras, medidas en el mismo entorno** (PostgreSQL 16 + PostGIS en el puerto 5434, `./gradlew build --rerun-tasks`, sumando los XML de `test` y `pruebaDeArranque`): **1 228 → 1 235**, 0 fallos. Las **+7** se reparten asi, contadas clase a clase: `MargenDelMarcoTest` **9 → 11** —tres casos nuevos (el marco del Peru sin aviso, el de fuera de la banda con el, y la latitud mas lejana) menos el `26.0` que sale del `@ValueSource` porque ahora lo lleva el borde derivado de la constante—, `DerivacionDeLosFrentesTest` **5 → 8** y `DerivacionDeFrentesJdbcTest` **8 → 10**. `CatastroNoNombraUnArbitrioTest` se queda en **3**: su contraste no se anade, **se reescribe** — el trabajo ahi no es un caso mas sino que el que habia pase de no poder fallar a poder. `verificarArquitectura` y `verificarAislamiento`, **verdes los dos**. <br><br>**Lo que NO cierra, con su motivo.** (1) **No se recalcula `METROS_POR_GRADO`**: el issue lo dice y esta bien elegido —es cota inferior de los dos grados en toda la banda, y ensanchar de mas no produce ningun frente equivocado—. (2) **El aviso sale por `log.warn` y por el `Informe`, y no queda en ninguna tabla**: `frente_derivacion` es por predio y esto es del padron entero, asi que meterlo ahi seria repetir la misma frase catorce mil veces; el `CronJob` que corre el derivador **sigue sin desplegarse** (#7), de modo que hoy no lo lee nadie porque nadie corre el proceso. (3) **Ninguna instalacion esta fuera de la banda hoy**, y por eso el aviso no puede haberse disparado nunca: `catastro` siembra Sullana y Catacaos, entre 4,9° y 5,3° sur, y no hay **ni un poligono** cargado en ninguna instalacion (P5C). Lo que #29 compra no es un aviso que sale: es que el dia que salga, salga |
| **#26 — el derivador deja de pararse en `tope`, y confirmar dos veces deja de pisar lo confirmado** (`DerivacionDeLosFrentes.derivar` con bucle de cursor y `tope` renombrado a `tamanoDelLote`; `Informe` con `prediosEnElPadron`, `lotes` y `agotoElPadron()`; `cuantosPrediosPorDerivar()` y `unFrente(frenteId)` en el puerto; `LOS_QUE_RECORRE_EL_DERIVADOR`, el fragmento de SQL escrito UNA vez que usan la pagina y el censo; `AND longitud_estado = 'PROPUESTA'` en el `UPDATE` de `confirmar` y la excepcion `LongitudYaConfirmada` -> **409**; el «antes» de la bitacora leido de la fila; `frente_predio` en `TablasDelSgtm.PROTEGIDAS` y **no** en `INMUTABLES`; y la clase nueva `PrediosPorDerivarJdbcTest`, con 3 000 predios en cada una de dos municipalidades — **1 314 -> 1 326 pruebas**, 0 fallos) | **Ocho roturas, cada una aplicada SOLA sobre `src/main` y restaurada por copia comparada con `cmp`** —«RESTAURADO IGUAL» las ocho—: **(R1)** devolver `derivar` a su cuerpo anterior, un solo lote desde cero; **(R2)** el `0L` DENTRO del bucle; **(R3)** que `agotoElPadron()` devuelva siempre `true`; **(R4)** escribir el filtro solo en el censo y no en la pagina, o sea dos predicados aparte; **(R5)** quitar el `AND longitud_estado = 'PROPUESTA'` y confirmar dos veces; **(R6)** colapsar las dos causas de «cero filas» en `LongitudYaConfirmada`; **(R7)** devolver el «antes» de la bitacora a la constante; y **(R8)** un `DELETE FROM frente_predio` de verdad en `src/main`, medido **con** la tabla en `PROTEGIDAS` y **sin** ella | **3, 2, 1, 1, 1, 2, 1 en rojo, y la octava — su segunda mitad — en VERDE.** (R1) nombra los 700 que no se derivaban: «but could not find the following elements: [501L, 502L, … 1200L]», mas «Expecting value to be true but was false» de `agotoElPadron()` y el del cursor. **(R2) es la pareja de (R1) y hace falta**: con el bucle puesto y el `0L` dentro, la corrida no sale corta —**no termina**—, y lo que la para es la guarda nueva: «El recorrido pidio los predios posteriores al 500 y el mayor que volvio es el 500: el cursor no avanza y la corrida no puede terminar. Lo que esta mal es `prediosPorDerivar`, que tiene que respetar su `desde`». (R3) es la asercion que no puede fallar, cazada por su contraste: «[es exactamente el estado de #26 (a): 500 de 1 200 y nada que lo dijera] Expecting value to be false but was true» — sin ese caso, `agotoElPadron()` lo cumpliria un metodo que devolviera `true` siempre. (R4) da «Expected size: 3000 but was: 3001», que es el denominador contando una cosa y el recorrido otra. **(R5) es el defecto central del issue**: «expected: 111.00 but was: 222.00», la cifra que una persona firmo sustituida por la de la segunda confirmacion —y la respuesta habria sido un 200 indistinguible de la primera vez—. (R6) pone **2**: el frente inexistente pasa a decir «El frente 999999 ya tiene su longitud CONFIRMADA…», o sea manda a discutir una confirmacion que nunca hubo. (R7) da «Expecting actual: [null] to contain exactly: ["210.08 ML"]» — la longitud anterior no quedaba en ningun sitio, ni en la tabla, que la pisa, ni en la bitacora, que no la nombraba. <br><br>**(R8) es la leccion de R-N por el eje de las tablas, medida en las dos direcciones, y su segunda mitad es el trabajo entero**: el MISMO `DELETE FROM frente_predio`, en el MISMO archivo, da «Expecting empty but was: [FrentesDelPredioJdbc.java — no se borra deuda, pagos, recibos, valores, papeletas, asientos ni auditoria… (RNF-051): DELETE FROM frente_predio]» con la tabla en `PROTEGIDAS`, y **`BUILD SUCCESSFUL`** sin ella. Una tabla que falta en el reparto no da un cruce: **deja de revisarse, en verde**. <br><br>**Y el hallazgo del metodo esta en el DOBLE de `DerivacionDeLosFrentesTest`, no en una rotura**: `FrentesDeMentira.prediosPorDerivar` contestaba `List.copyOf(predios)` **ignorando sus dos argumentos**, asi que la corrida que solo pedia el primer lote desde cero —el defecto (a) del issue— recorria el padron entero en la prueba y salia en VERDE. Un doble que no respeta el contrato del puerto no dobla el puerto: dobla lo que a la prueba le conviene, y por eso este defecto pudo vivir con nueve pruebas encima. Con el doble honesto, (R1) muerde. <br><br>**AC-3 se cumple A MEDIAS, y la mitad que no se hace esta medida y escrita donde vive.** Se filtra por `estado = 'ACTIVO'` —un predio dado de baja no tiene nada que explicar— y **NO** por `geometria IS NOT NULL`, que AC-3 tambien pedia: un lote sin poligono SI tiene algo que explicar, y explicarlo es para lo que existe `frente_derivacion`. Filtrandolo, ese predio deja de tener fila ahi y `GET …/frentes` pasa de «no hay cartografia» a **«nunca se ha derivado»**, que manda a mirar si el proceso corre. Y no es un caso de borde: **hoy no hay ni un poligono cargado en ninguna instalacion**, asi que con ese filtro el derivador no recorreria NI UN PREDIO y no dejaria NI UNA constancia —el proceso que sale con codigo 0 sin hacer nada que C-6 midio—. Lo que AC-3 queria evitar, «el tope se gasta en predios que no pueden dar frente», **desaparece con AC-1**: ya no hay tope. La decision la sujeta una prueba que afirma que el predio sin poligono SIGUE dentro del recorrido. <br><br>**El plan se MIDIO, con 3 000 predios en cada una de dos municipalidades y `ANALYZE` hecho, y no empeora**: sin filtro `Index Only Scan using predio_pk` con «Index Cond: ((municipalidad_id = (current_setting('app.municipalidad_id'::text))::bigint) AND (id > 0))» y **shared hit=25**; con filtro `Index Scan using predio_pk`, el mismo `Index Cond`, «Filter: ((geometria IS NOT NULL) AND ((estado)::text = 'ACTIVO'::text))», «Rows Removed by Filter: 1» y **shared hit=25** — mismo indice, mismas paginas. Sin `enable_seqscan = off`, a proposito: forzarlo mediria si el indice es alcanzable y lo que hay que medir es que el planificador lo elija. Y **no hace falta ninguna migracion**: `predio_pk (municipalidad_id, id)` ya sirve al cursor, y escribir un indice nuevo le habria robado el plan al que sirve (la leccion de `zonificacion_vigencia_ix` en #4). <br><br>**Dos decisiones con su motivo.** (1) **Rectificar una longitud confirmada es OTRO acto y hoy no existe**: se decide y se escribe en `ConfirmarElFrente`. Una rectificacion lleva su propio motivo —por que la cifra que alguien firmo estaba mal— y deja la anterior recuperable, y eso no es «confirmar otra vez». (2) **La guarda vive en el `WHERE` del `UPDATE` y no en un `if` del caso de uso**: dos confirmaciones simultaneas leerian las dos «esta propuesta» y las dos escribirian. El caso de uso lee el frente igualmente, pero **para el «antes» de la bitacora** y no para decidir —una sola compuerta, y la pone el motor—. <br><br>**Y `tope` cambia de nombre porque cambio de significado**: la propiedad pasa de `kamayuk.derivacion-de-frentes.tope` a `…tamano-del-lote`, y no rompe a nadie —medido con `grep` en los cinco repositorios: ningun `CronJob`, ningun descriptor y ningun guion de carga la nombran; ese `CronJob` sigue sin desplegarse desde #7—. <br><br>**Cifras, medidas en el mismo entorno** (PostgreSQL 16 + PostGIS real, `./gradlew build --rerun-tasks`, sumando los XML de `test` y `pruebaDeArranque`): **1 314 -> 1 326**, 0 fallos, sobre `origin/main` = `37cc08b`. La linea base **no se hereda, y se midio CINCO veces**, porque `main` se movio cuatro veces mientras se medía —#68 (#28), #70 (#51), #63 (#27) y #62 (#29)—: 1 281 -> 1 293 sobre `c5a820c`, 1 283 -> 1 295 sobre `32c3784`, 1 293 -> 1 305 sobre `1888dc1`, 1 307 -> 1 319 sobre `bc46741` y 1 314 -> 1 326 sobre `37cc08b`. **La cifra que dice algo es el delta, y es el mismo +12 en las cinco**: un total contra un `main` que ya no existe no dice nada, y el 1 250 -> 1 262 que este trabajo traia medido sobre la rama del PR #60 no se hereda porque esa base ya no esta en la historia. **Y el ultimo rebase, el de #29 (#62), es el que su propio cuerpo predijo**: los dos cambian la firma de `derivar` y la aridad de `Informe` en las mismas lineas, asi que el `record` queda con SEIS componentes —`prediosEnElPadron`, `prediosRecorridos`, `lotes`, `prediosConFrenteNuevo`, `frentesPropuestos`, `avisoDeLatitud`—; el `avisoDeLatitud()` de #29 se sigue calculando UNA vez antes del bucle, y el bucle es el de #26. Y el doble de `DerivacionDeLosFrentesTest` se queda con la version HONESTA de #26 y no con el `List.copyOf(predios)` que #29 arrastraba: ese doble es el hallazgo del metodo de este issue, y perderlo habria dejado (R1) sin morder. `verificarArquitectura` y `verificarAislamiento`, **verdes los dos**. <br><br>**Este trabajo se REBASO sobre `main` y se volvio a medir, y una decision cambio al hacerlo.** Salia apilado sobre el PR #60, que implementaba #20; el que aterrizo en `main` fue el otro, **#64**, y con el una API distinta para el mismo tipo: `DatosDeAuditoria.campos()…mas(…)…datos()` en vez de `objeto()…campo(…)…componer()`. El «antes» de #26 partia la longitud en `magnitud` y `unidad` **porque la API de #60 no sabia asentar una `Medida`** —su lista blanca no la tenia y pasarla lanzaba—; la de #64 si, y lo hace con su motivo escrito («una `Medida` lleva la unidad DENTRO, porque ahi la unidad ES parte del dato»). Asi que la longitud entra **entera** en el antes y en el despues: no era una decision de #26 sino una limitacion de su base, y partirla dejaria el antes y el despues con formas distintas justo donde la bitacora se lee comparando uno con otro. La afirmacion de AC-5 no se mueve —se sigue midiendo que el «antes» trae la cifra que habia y no una frase fija—, y **se volvio a demostrar sobre el arbol rebasado**: (R7) y (R5) se repusieron una a una y dieron el mismo rojo. La prueba de AC-5 se mudo a `ElFrenteSeAsientaEnLaBitacoraJdbcTest`, que es la clase que #64 creo para la bitacora y la unica que monta la `AuditoriaJdbc` de verdad hasta el `cast(… AS jsonb)`. <br><br>**Lo que NO se cierra, con su motivo.** (a) **La mitad de `geometria` de AC-3**, arriba, con su medida. (b) **No hay acto de rectificacion**: quien crea que una cifra confirmada esta mal no tiene hoy por donde corregirla —el 409 lo dice con esas palabras—, y crearlo es decidir quien puede y con que sustento. (c) **Ningun `CronJob` despliega el derivador**, igual que desde #7, asi que el recorrido por lotes no corre en ninguna instalacion todavia. (d) **`frente_derivacion` no entra en `PROTEGIDAS`** y es deliberado: es un `UPSERT` que dice *cuando fue la ultima vez*, no un acto administrativo; borrarla pierde una respuesta, no una constancia de que alguien decidio algo. |
| **#30 — el WKT que nadie lee, y la deteccion que esperaba dentro de un `POST`** (`ZonaQueRige` y `CandidatoEnLaCola` —lo que se LEE, sin poligono, frente a `Zona` y `Candidato`, que es lo que se ESCRIBE—; `zonaPorCodigo` pasa a `idDeLaZona` y devuelve el identificador; `COLUMNAS_DE_LA_COLA` sin `ST_AsText`; **el endpoint `POST /fiscalizacion/campanias/{id}/deteccion` se retira** y la corrida pasa a `DetectarEnCampania`, del perfil `batch`; tres guardas nuevas —dos que **ejecutan la sentencia de produccion contra PostGIS con un poligono de PDU de 5 001 vertices** y una de ArchUnit sobre el borde HTTP—; **+10 pruebas**: **1 281 → 1 291** medido contra la base `c5a820c` en un *worktree* aparte, y **1 293** despues de traer `main` (#68) a la rama, las dos con 0 fallos) | **Ocho roturas, cada una aplicada SOLA sobre `src/main` —o sobre la propia guarda— y restaurada por copia comparada con `cmp`** («RESTAURADO IGUAL» las ocho): (R1) devolver `ST_AsText(zz.geometria)` a `ZONA_QUE_CONTIENE`; (R2) devolverlo a `COLUMNAS_DE_LA_COLA`; (R4′) devolverlo a `HALLAZGOS_DEL_PREDIO`; (R5) que el controlador vuelva a inyectar `DetectarSubvaluadores`; (R6) **el contraste**, apuntar la guarda de ArchUnit a un paquete que no existe; (R7) **el otro contraste**, sembrar un cuadrado de cuatro vertices en vez del poligono de PDU; (R8) que el runner se trague `SinCartografia` y termine bien; y (R9) que deje de limpiar los dos contextos | **1 en rojo cada una, y en pruebas DISTINTAS, que es lo que se queria medir.** (R1): «el poligono de la zona son **188 595** bytes de texto que ninguna componente de `ZonaVigente` lee y que `ZonaResource` no publica», con el `MULTIPOLYGON(((-80.35000000000001 -5.25,…` dentro de la respuesta. (R2): «la cola de candidatos traeria 188 595 bytes de poligono por fila, y ningun recurso de este contrato lo publica» — y deja las otras dos comprobaciones en VERDE: son consultas distintas y se rompen por separado. (R5) nombra la arista: «Expecting empty but was: [«FiscalizacionCatastralController -> kamayuk.catastro.fiscalizacion.aplicacion.DetectarSubvaluadores»]». (R8): «Expecting code to raise a throwable». (R9): «un proceso de vida corta limpia lo que fijo… Expecting an empty Optional but was containing value: 200105». <br><br>**(R6) y (R7) son los dos contrastes, y dicen «no midio» en vez de aprobar.** (R6): «no se leyo ni una clase de «.infraestructura.paquete-que-no-existe.» del bytecode de este sistema: esto no es «esta bien» sino «no se pudo comprobar»» — sin esa asercion, una guarda que dejara de ver la capa web afirmaria «ninguna depende» sobre el conjunto vacio. (R7): «sin un poligono grande esta prueba se cumpliria sola: cualquier respuesta seria «pequena», con el `ST_AsText` puesto o quitado — Expecting actual: **106** to be greater than: 100000». Un cuadrado de cuatro vertices pesa 106 bytes y una zona de PDU 188 595: **la fixture es el sujeto**, y sin ella la mitad de las aserciones se cumplen solas. <br><br>**(R4′) no la caza una prueba sino el MOTOR, y eso es mejor**: «ERROR: column h.geometria does not exist. Hint: Perhaps you meant to reference the column «c.geometria»». Es el punto (c) del issue —el `ST_AsText(NULL)` de cada fila de cada respuesta de hallazgos— y **lo cerro #23 mientras este trabajo estaba en curso**, retirando la columna en `V12`, que es exactamente lo que #30 anticipo por escrito («si #23 decide retirar `hallazgo.geometria`, el punto (c) se resuelve solo»). La comprobacion se queda como guarda de regresion y **se dice que su sujeto ya no se puede reintroducir sin una migracion**, en vez de dejarla pareciendo que muerde. <br><br>**Las guardas EJECUTAN la sentencia de produccion y pesan lo que vuelve, y no leen la constante.** Una guarda de texto sobre `ST_AsText` se cumple sola el dia que la conversion se escriba de otra forma —`geometria::text`, `ST_AsEWKT`, una vista— y ademas no dice cuanto cuesta; estas piden el SQL a las constantes del repositorio —no a copias escritas en la prueba, que seguirian verdes el dia que alguien cambiara la de produccion— y lo corren como `kamayuk_app` bajo la politica. <br><br>**AC-4, medido antes y despues en el mismo entorno** (PostgreSQL 16.13 + PostGIS 3.4.2 en el puerto 5434, como `kamayuk_app`, con un poligono de PDU de **5 001 vertices** cuyo WKT pesa **188 595 bytes**, 3 000 predios y `ANALYZE` hecho). **En bytes de respuesta y en bloques, nunca en milisegundos**, que en esta maquina no significan nada: la zonificacion **188 636 → 41 bytes** y **32 → 20 bloques**; la pagina de 20 candidatos **3 772 822 → 922 bytes** y **285 → 2 bloques**; la pagina de 20 hallazgos **3 772 964 → 1 064** y los hallazgos de un predio **188 651 → 56**, estas dos ya por #23. Y la respuesta HTTP **no cambia ni un byte**: `CandidatoResource` gana una segunda fabrica que publica exactamente los mismos campos desde el registro de lectura. <br><br>**AC-3: se elige `batch` y no un 202, y el motivo esta medido.** Una respuesta asincrona mueve a donde nadie mira las tres respuestas que hoy son visibles —«no hay cartografia», «esa campania no existe», «esa campania esta cerrada»— y este sistema no tiene ni tabla de trabajos ni ruta para preguntarlas: habria que inventar las dos. En `batch` siguen visibles donde ya se miran los otros nueve procesos de este perfil: el registro y el **codigo de salida**. `DetectarEnCampania` **deja salir** las tres excepciones despues de registrarlas, asi que sin cartografia el `Job` queda en `Failed` y no en `Complete` — que hoy sea rojo en toda instalacion es correcto: es lo que pasa, y es el defecto de C-6 —arrancar, no hacer nada y salir con 0— por su otra cara. **Y el censo de #25 no se pierde: cambia de superficie.** `DeteccionResource` publicaba los candidatos y de cuantos predios salian, porque «0 candidatos» sin denominador no es una respuesta; ese recurso se retira con la ruta —una respuesta sin endpoint que la devuelva es una promesa que nadie cumple, y `LaDeteccionNoEstaEnElCaminoCalienteTest` lo destapo sola al quedarse huerfano— y las cuatro cifras de `Cobertura` pasan al registro de la corrida, **tambien —y sobre todo— cuando no deja ningun candidato**. `DetectarSubvaluadores` decia de si mismo desde #6 «corre en el perfil `batch` … y no en la web: recorre el padron», **que era falso**; ahora lo es. <br><br>**Y el «de paso» del AC-3 —«`ST_Area` se calcula dos veces por fila; una sola basta»— resulto FALSO al medirlo, y NO se aplica.** Se midieron seis formas contando las llamadas **de verdad** —con una envoltura `IMMUTABLE` que lleva la cuenta, porque el plan `VERBOSE` repite la expresion por nodo y decir «sale 10 veces» mide otra cosa—, sobre 3 000 predios y 3 000 fichas: escrita **dos veces** **285 bloques · 6 500 llamadas**; escrita una vez en una subconsulta **285 · 6 500**; en un `LATERAL` **285 · 6 500**; subconsulta con la valla `OFFSET 0` **9 156 bloques · 3 000 llamadas**; `LATERAL` con `OFFSET 0` **9 156 · 3 000**; CTE `AS MATERIALIZED` **9 156 · 3 000**. O sea que **las tres formas que consiguen una sola evaluacion cuestan 32 veces mas bloques**, y por el mismo mecanismo las tres: la valla impide que el filtro entre en el `Join Filter`, el planificador cambia el `Hash Join` por un `Nested Loop` y `predio` pasa a rebuscarse por su clave primaria **una vez por ficha** —`Index Scan using predio_pk … loops=3000`, 9 000 bloques—. Es el defecto que #21 midio en `grd` con el `JOIN` llano, por la puerta de atras. Escribirla una vez **sin** valla no cambia nada medible (285 y 6 500 las dos), asi que **el `CRUCE` se deja tal como #25 lo dejo**: reescribir a ciegas una consulta que otro issue acababa de medir, para no mover ninguna cifra, es peor que no tocarla. <br><br>**Dos registros y no reusar los que hay, con su motivo.** Reusar `Zona`/`Candidato` con la geometria en `null` era el atajo, y no vale: ahi el nulo **ya significa algo** —«el insumo no traia poligono»— y una pagina que lo dejara nulo lo afirmaria de cada fila. Es la misma asimetria, con el mismo motivo, que `ZonaDeRiesgo` en `grd` («el poligono no viaja en este registro: se queda en la base») y que `HallazgoDelPredio` desde #17. Y `Zona` conserva su invariante —«una zona sin poligono no cubre ningun suelo y no puede decidir nada»— porque es cierta **al cargar**; al leer, el poligono ya decidio dentro del motor, en el `ST_Covers` de la propia sentencia. <br><br>**El trabajo se rehizo entero sobre `main` a mitad de camino, y conviene decirlo.** La primera version salio sobre `c9f839a`; entre medias aterrizaron **#61** (#23, #24 y #25) y **#64** (#20), y con ellos **`V12` retiro `hallazgo.geometria`** —los puntos (b2) y (c) del issue— y `DetectarSubvaluadores` cambio de firma (umbral y tope los congela la campania). En vez de resolver el conflicto a mano se volvio a `origin/main` y se reaplico solo lo que seguia haciendo falta: `HallazgoInformado` **se retira entero** porque su trabajo ya estaba hecho, y `DatosDeDeteccionDeSubvaluadores` **no ofrece umbral ni tope**, con una prueba que lo afirma sobre sus componentes — que es lo que `PeticionDeDeteccion` protegia en el borde hasta que esta ruta se fue. <br><br>**Lo que NO se cierra, con su motivo.** (a) **`candidatoPorId` sigue trayendo el poligono** y esta bien: AC-2 dice que la geometria de UNO es una lectura por identificador. Hoy no lo lee nadie —ningun recurso lo publica—, asi que es la unica conversion a texto que queda sin consumidor; retirarla seria decidir sobre el visor del plano (ADR-0022) y no es de este issue. (b) **El recorrido sigue sin cota**: el `ORDER BY` obliga a calcular el padron entero antes del `LIMIT`, y cambiar eso es **#25**. Lo que este issue consigue es que lo pague un proceso de vida corta. (c) **Ningun `CronJob` despliega el runner nuevo**, igual que `DerivarFrentes` y `PublicarElPadron` desde C-8; y **no se anade guion a `infra/carga-de-datos/`** porque no carga ningun archivo y un guion sin `..._ARCHIVO` pone en rojo el censo de `infrastructure` (la leccion de #4, #5 y #7). (d) **`yarn rutas` del frontend pasa a contar una ruta menos del backend**: no rompe nada —ni `src/api/fiscalizacion.ts` ni el contrato que `rentas` publica declaran esa ruta, comprobado— pero la cifra de la fila de #55 se queda vieja. (e) **`docs/50-api/eventos/lote-de-eventos.json` se regenera solo** al correr el build sobre `main` en este carril, por el estado del clon hermano de `normativa`; su diff **no viaja** en este PR. |
| **#73 — nueve entradas de `ConfiguracionDeCatastro` que no eximian a nadie, y el dia que alguien creara una clase con ese nombre la habrian eximido sin que nadie lo decidiera** (cinco de `tiposAjenosQueFiscalizacionSoloLee()` y cuatro de `escriturasSinUsuarioQueObserve()`, con sus comentarios; **ni una linea de `src/main`**) | **No hubo que provocar el defecto: el estado de partida ERA el defecto.** La guarda que lo caza vive en `comun-verificaciones` y llega con [`infrastructure#27`](https://github.com/hneyra/infrastructure/pull/35); corrida contra `catastro@main` con la libreria de esa rama, `ArquitecturaTest` pasa de **3 a 5** pruebas y **1 sale roja sin tocar nada**. Y antes de borrar una linea se comprobo la premisa **por separado y desde el arbol**, en vez de creerse el mensaje: de las **nueve** clases y metodos que las entradas nombran, `find` sobre `src/main` devuelve **0** para las nueve, y los paquetes `kamayuk.catastro.rentas` y `kamayuk.catastro.cuentacorriente` **no existen** — los de produccion son `auditoria, autorizacion, carga, compartido, contribuyentes, documentos, dominio, esquema, fiscalizacion, grd, json, nucleo, parametros, persistencia, plataforma, seguridad, urbano, web` | **1 en rojo de 5, con las nueve nombradas una a una y su motivo dentro**, y los dos motivos son distintos porque las dos listas cuestan cosas distintas: «`escriturasSinUsuarioQueObserve()` → «.cuentacorriente.aplicacion.ReconstruirSaldo.deContribuyente(long)»: ningun metodo del arbol tiene esa firma, asi que **no exime a nadie de la regla 10**; el dia que aparezca, **escribira sin observacion sin que nadie lo haya decidido**» y «`tiposAjenosQueFiscalizacionSoloLee()` → «.rentas.DeclaracionesDelEjercicio»: no clasifica a ningun tipo, asi que **la frontera de fiscalizacion corre hoy sin esta excepcion**; el dia que exista, `fiscalizacion` podra depender de ella sin que nadie lo haya decidido». Retiradas las nueve: **5 de 5 en verde**, y `verificarArquitectura` + `spotlessCheck` **verdes los dos**. <br><br>**De donde salieron, medido y no supuesto**: son herencia del monolito. El `sed` de P5C cambio `rentas` por `catastro` en los nombres de paquete y dejo las entradas apuntando a paquetes que este sistema **no se llevo** — `cuentacorriente` esta ademas declarado en `ambitosAusentes()`, o sea que la propia configuracion se contradecia consigo misma. **Es la forma de defecto de R-N por un cuarto eje**: R-N la midio en los MODULOS (`getOrDefault` con una clave vieja deja de revisar, en verde), `#7` en las TABLAS, `#25` en los TIPOS —borrar `Tolerancia.java` dejando su nombre en `envoltoriosDeDecimal()` da `BUILD SUCCESSFUL`— y esto en las EXENCIONES. **Y `#20` ya habia medido la variante silenciosa**: dos de las cinco entradas de `componenElAreaAManoConMotivo()` llevaban desde `#6` sin eximir nada porque escribian una forma que el escaner no reconoce. <br><br>**Lo que NO cierra, con su motivo.** (a) **El censo de 16 que la guarda imprime y NO pone en rojo** se queda: son 8 claves de `modulosDelReparto()` sin modulo en el disco y 8 de `ambitosAusentes()`. No bloquea porque esas dos listas **reparten y declaran**, no eximen —la direccion que cuesta la cierra R-N—, y podarlas es el otro trabajo que el javadoc de `modulosDelReparto()` ya declara. (b) **Dentro de ese censo hay una afirmacion FALSA que queda dicha y no arreglada**: `ambitosAusentes()` declara ausente `seguridad`, y este sistema tiene `kamayuk.catastro.seguridad` desde C-7. Es inerte hoy y se corrige con la poda. (c) **Este PR no toca `src/main`**, asi que `RUTAS_DE_CODIGO` no le exigia fila; se escribe igual, porque lo que se midio aqui no lo puede volver a medir nadie leyendo el diff |
| **#66 — el job de navegador consumia el 88 % de su tope, y el 90 % de eso era Playwright esperando a clics que ya no podian ocurrir — con el arnes afirmando en verde sobre 33 pantallas que no habia abierto** (`loQueEsconden()` en `sin-red.mjs` —clic DENTRO de la pagina, espera al atributo y no a un plazo, de uno en uno y cerrando detras—, la guarda de las **pantallas a ciegas**, `verificaciones/presupuesto.mjs` con sus dos umbrales y sus cuatro autocomprobaciones, y `timeout-minutes` de 15 a 10 en el job `navegador`) | Seis roturas, cada una **sola** y restaurada **por copia comparada con `cmp`**: (R1b) devolver `loQueEsconden` al `desplegarlo` anterior —clic de Playwright por indice con 900 ms **y sin cerrar detras**—; (R1) solo la primera mitad de eso, el clic de Playwright pero **cerrando**; (R2) que `abrir` deje de ser el primer paso que ejecuta algo; (R3) un paso que ejecuta algo **detras** de `cerrar`; (R4) quitarle al job su `timeout-minutes`; y (R5) borrar la marca de apertura | **2, VERDE, 2, 2, 2 y 2.** (R1b) da «**33 de 34 pantalla(s) se miraron A CIEGAS**», las nombra una a una con sus «6 plegado(s) que no se abrieron», y el resumen queda en «1 seccion abierta y leida · 204 que se pincharon y no se abrieron». **(R1) es el hallazgo del metodo, y paso en VERDE**: con el clic viejo pero cerrando detras, se abren **245 de 261** y la guarda calla — o sea que **la ceguera necesitaba las DOS mitades**, y una rotura que solo pusiera el clic viejo habria dado por demostrado algo que no lo estaba. Que la segunda mitad importe no es teoria: el velo que el menu de sesion tiende al abrirse es lo que tapa los clics siguientes, y sin cerrar ese menu el velo **sobrevive a los cambios de ruta**, porque el armazon navega por el `#` y no recarga. <br><br>**AC-1 — donde se va el tiempo, medido en CI y no en local** (`gh api repos/hneyra/catastro/actions/jobs/<id>`, seis jobs verdes: 101647351053, 101635295105, 101615956145, 101599375830, 101589290622 y 101768582448 de `main`): el job dura **682-790 s** y `sin-red` es **526-615 s de ellos, el 78 %**; lo demas es arranque 31 s (`Set up job` 1 + checkout 2 + setup-node 5 + install 2 + Chromium 21), `Compilar y servir` 11 s, `errores` 63 s, `mirar` 29 s, `impedimentos` 22 s, `ficha` 7 s, `paleta` 5 s y `ejercicios` 3 s. Y la tendencia estaba dentro de `sin-red`: **526 -> 615 en un solo dia**, al entrar las vistas de ficha. **Y por dentro de los 615 s** —medido en local, donde el mismo recorrido da 609 s porque lo que manda no es la CPU sino plazos fijos—: los **dos `vite build` cuestan 1 s** (0,16 %), y el recorrido 608, repartidos en `goto` 134 ms, esperas fijas de 900 ms 30 665 ms, **`desplegarlo()` 575 396 ms** y leer el texto 161 ms. De ese `desplegarlo`, **551 739 ms en clics: 1 bueno y 612 fallidos**, todos con el mismo motivo en el registro de Playwright: «`<div></div>` intercepts pointer events». <br><br>**AC-2 — se descartan los tres caminos del issue, con su medida, y se arregla el que la medida senala.** (1) *Reutilizar el `yarn build` para no compilar dos veces*: las dos compilaciones son **1 s de 609** en local y como mucho 8 s de 615 en CI —el paso `Compilar y servir` entero son 11 s—, o sea **~1 %**; y ataria el contraste «con el proxy encendido las marcas SI se ven» a un `dist/` que construyo otro paso, de modo que un `dist/` viejo lo pasaria contra el paquete equivocado. Un 1 % a cambio de un modo nuevo de estar en verde. (2) *Partir el job en dos*: `sin-red` **sola** son 615 s, y con su propio arranque (31 s) y su `Compilar y servir` (11 s) la mitad lenta queda en **657 s de los 785** — un 16 % de reloj a cambio de duplicar checkout, node, install y Chromium en un segundo runner, y **sigue al 73 % del tope**. No arregla la intermitencia: la renombra. (3) *Compartir navegador y vista previa*: los seis arneses que usan la vista previa **ya la comparten** —los seis apuntan al 5210—, lo unico sin compartir es el `chromium.launch()`, medido en decimas; y `sin-red` **no puede** compartirla, porque el proxy se resuelve al compilar. Menos de 2 s de 785. <br><br>**AC-4 — el antes y el despues de LO QUE SE MIDE, y ninguna cifra baja**: `mirar` 34 pantallas -> 34; `impedimentos` 34 pantallas y 29 controles impedidos -> iguales; `errores` 36 renders sobre 6 superficies x 6 escenarios -> iguales; `ficha` 6 vistas sobre 5 predios y las 4 clases, con sus 2 obras, 2 bienes comunes, 2 participaciones, 2 tierras, 4 colindantes, 2 versiones y 4 construcciones -> iguales; `paleta` e `imagen`, iguales. `sin-red`: 34 pantallas -> 34, y **0 secciones plegadas abiertas -> 263**, o sea **34 textos rastreados por las seis expresiones de `CIFRAS` -> 297**. La unica cifra que se mueve, sube. **El reloj**: `sin-red` **609 s -> 41,4 s** en local (41,56 y 41,20 en dos corridas mas, con las mismas 263), los siete arneses de navegador **734,8 s -> 167,2 s**, y el job de CI **785 s -> ~230 s estimados** —la estimacion es del recorrido nuevo, que es 95 % espera fija (34 x 1 100 ms = 37,4 s) y por tanto casi identico en cualquier maquina, mas las dos compilaciones, que en CI cuestan lo que cuesta el paso de al lado—. **No se ha corrido en CI: este PR no se ha empujado**, asi que el «despues» de CI es una estimacion con su aritmetica escrita, y el «antes» no. <br><br>**AC-3 — el presupuesto deja de ser un numero escrito a mano.** El primer paso del job abre un cronometro y el ultimo lo cierra contra el **`timeout-minutes` de ese mismo archivo**, que es el unico limite que de verdad manda: no hay una segunda tabla de plazos —esa se queda vieja sola— ni un sistema de medicion nuevo, porque **el reparto paso a paso ya lo publica Actions** en `steps[]`, que es de donde sale el AC-1. Avisa con `::warning::` al 60 % y **falla al 85 %**, o sea que no anade una cancelacion: la SUSTITUYE por algo que se puede leer. Contrastado con las cifras reales de las cinco corridas que el issue cita, con el tope de 900 s que habia: **682 s -> aviso al 76 %**, **785 s -> ROJO al 87 %**, **790 s -> ROJO al 88 %**. Habria hablado en las cinco, y la primera vez tres semanas antes del `cancelled` de #64. Las cuatro autocomprobaciones son lo que impide que el cronometro se convierta en un adorno —«arranca tarde o para pronto y el numero sale mas bajo, en verde», que es la regla 11 por el mismo eje—: R2 da «`abrir` es el paso 4 y el primero que ejecuta algo es el 3», R3 «hay 1 paso(s) detras de `cerrar` que no son de `if: failure()`», R4 «el job «navegador» no declara `timeout-minutes` … que no es un limite sino la ausencia de uno» y R5 «No hay marca de apertura …». Las cuatro salen con **2**, no con 1: no es que el job vaya mal, es que **no se pudo comprobar**. <br><br>**Y `timeout-minutes` baja de 15 a 10, que es parte del arreglo y no cosmetica**: con el job en ~230 s, un tope de 900 s pone el aviso en 540 s, o sea despues de una regresion del 135 %. Con 600 s el aviso llega a los 360 y el rojo a los 510. <br><br>**Lo que NO se cierra, con su motivo medido.** (a) `errores` son 63 s y no se toca: es el segundo paso mas caro, pero su coste son 36 renders con dos esperas de 1 100 ms cada uno —o sea la comprobacion misma— y recortarlo cambiaria lo que afirma. (b) `mirar` e `impedimentos` recorren las mismas 34 pantallas que `sin-red` con `networkidle` y esperas fijas de 700 y 600 ms: 51 s entre las dos, y compartir un recorrido entre tres arneses que miden cosas distintas es otro trabajo. (c) Los 16 `rebeldes` que R1 destapo —secciones que el clic de Playwright no abria aun cerrando— **no existen con el clic de dentro de la pagina**: 263 de 263. (d) La ceguera se arregla en `sin-red` y **no se ha buscado en `mirar` ni en `impedimentos`**, que no despliegan nada; si manana un modulo esconde una cifra tras un desplegable, esos dos seguirian sin verla, y eso es un issue y no una nota. |
| **#71 — la fiscalizacion se podia mirar y no se podia hacer: las nueve operaciones que ninguna pantalla ofrecia** (`src/api/fiscalizacion.ts` con las **ocho rutas** nuevas, los siete cuerpos de escritura y las nueve listas; `src/datos/fiscalizacion.ts`; el modulo `Fiscalizacion.tsx` reescrito con el componente `Acto` —observacion obligatoria, primario apagado con su `title`, confirmacion aparte para lo irreversible—; la pestana **Hallazgos** del predio en `Catastro.tsx` (AC-4); `src/simulado/{respuestas,ciclo}.ts` con las trece operaciones simuladas; el **punto 9** de `rutas.mjs` —los cuerpos de escritura contra su `record`—, **dos superficies nuevas** en `errores.mjs`, **diez vistas** nuevas y el **arnes numero trece**, `transiciones.mjs`) | **Trece roturas, cada una aplicada SOLA y restaurada por copia comparada con `cmp`** —«RESTAURADO IGUAL» las trece—: (R1) un campo de mas en `PeticionDeAnulacion` y en su lista; (R2) quitar `dispositivo` de `PeticionDeEvidencia`; (R3) apuntar un cuerpo a un `record` que el backend no declara; (R4) quitar un campo de la lista y no del tipo; (R5) que `actosDe` devuelva **todos** los actos; (R6) **al reves**, que devuelva ninguno; (R7) que el proxy **deje de recordar** la transicion; (R8) sembrar la cola sin los dos estados terminales; (R9) que el error de una escritura **no pase por `Fallo`**; (R10) quitarle el `motivo` al primario impedido; (R11) **el contraste**, con R10 puesta, quitar las nueve vistas de fiscalizacion; (R12) quitar la confirmacion aparte del acto irreversible; y (R13) la ruta nueva con otro nombre de parametro | **1, AVISO, 1, 1, 9, 5, 1, salida 2, 16, 6, VERDE, salida 2 y 1.** (R1) da «`motivoDeLaAnulacion` no es un componente de «record PeticionDeAnulacion» … El cuerpo es una lista blanca: lo que no esta se descarta aunque llegue en el JSON, y el servidor contesta que la operacion se hizo. El dato no acaba en ningun sitio y nada lo dice». (R4) la caza `tsc` con la asercion de compilacion: «Type 'boolean' is not assignable to type '["falta en CUERPOS_DE_ESCRITURA", "detalle"]'» — sin ella, la lista que el arnes compara se quedaria vieja en silencio. (R5) pone **9** en rojo, uno por fila, y el que mas dice es el terminal: ««Candidato 3», en «VERIFICADO_EN_CAMPO», OFRECE «Admitir en gabinete», «Descartar en gabinete», «Verificar en campo», «Descartar en campo» y ese estado no lo admite. Lo que admite: (ninguno: es terminal)». (R6), la mitad simetrica, da **5**: «en «DETECTADO», NO ofrece «Admitir en gabinete», «Descartar en gabinete» … Es el fallo invisible de los dos: el acto no se puede hacer, no hay error y no hay aviso». (R9) da **16 sobre 6 renders** —los 6 titulos mas los **10** pares byte a byte, que es `C(5,2)` porque el sexto escenario trae el mensaje que pone `cliente.ts` y no el cable—, con «ERROR_INTERNO es reintentable y esta superficie ofrece 0 «Reintentar», no 1» y «pierde la incidencia «c0ffee11-…»». (R13) da «existe en el backend con OTRO nombre de parametro». <br><br>**(R7) es la decision del proxy medida, y no razonada.** ADR-0010 decision 4 dice que este proxy **no persiste**, y para el alta de ficha eso es correcto y esta escrito. Aqui no vale: `fiscalizacion` es una maquina de estados y lo ofrecible sale del estado de la fila, asi que sin memoria el proxy produce un par que **el backend no puede producir** —un `200` que admite al candidato y el `GET` siguiente devolviendolo `DETECTADO`—. Medido con la memoria quitada: «tras «Admitir en gabinete», el candidato 1 sigue en «DETECTADO» … y sigue ofreciendo «Admitir en gabinete», «Descartar en gabinete». La lectura siguiente contradice a la escritura anterior: la pantalla vuelve a ofrecer lo que se acaba de hacer, que es el defecto que el AC-2 existe para impedir». O sea que **la alternativa fabrica el defecto que el issue existe para cerrar**. Se toma la memoria, dura lo que la pagina —una recarga la devuelve al principio, asi que las capturas siguen siendo repetibles— y queda declarada en el javadoc de `ciclo.ts` con lo que rompe. <br><br>**(R11) es el contraste que esta serie lleva trece veces necesitando, y sale VERDE.** Con el defecto de R10 **puesto** sobre `src/`, quitar las nueve vistas de fiscalizacion deja `impedimentos` en «35 pantallas recorridas · 34 control(es) impedido(s) · todos dicen por que lo estan», **codigo de salida 0**. Los seis primarios que este issue anade **solo existen dentro de un acto abierto**, y el acto se abre desde la ruta: sin esas vistas el arnes informa en verde sobre los seis controles nuevos. Con ellas, R10 da **6 en rojo** nombrando las seis rutas una a una. Y (R8) es la misma exigencia por el eje del sujeto: sembrar la cola sin los dos terminales deja el arnes en «2 de 6 estado(s) declarados no aparecieron en ninguna fila … los dos terminales son los que dan sentido a «no ofrece de mas»», con salida **2**. <br><br>**(R12) mide la mitad del AC-3 que no se ve mirando**: un panel de confirmacion que ademas manda la peticion cumple cualquier guarda que solo mire la pantalla. Por eso el arnes mira `window.__peticiones` —envolviendo `fetch` con un `get`/`set`, porque el proxy lo SUSTITUYE y `page.route` no ve ninguna peticion, que es la leccion de `errores.mjs`— y con la advertencia quitada dice «««Dejar sin efecto» ESCRIBIO a la primera pulsacion: salio «POST /catastro/api/v1/fiscalizacion/hallazgos/1/anulacion» sin que nadie confirmara»» y sale con **2**. <br><br>**Y una rotura paso en VERDE por un motivo que hay que anotar: el arnes midio un `dist/` rancio.** (R5) escrita como `Object.values(tabla).flat()` dejaba `estado` sin leer, `tsc -b` fallaba dentro de `yarn build`, **`dist/` se quedaba con el paquete anterior** y `transiciones` informaba «cada fila ofrece exactamente lo que su estado admite», exit 0. Es el verde rancio de #192 punto 2 por el eje del artefacto: un arnes de navegador mide lo que hay en `dist/`, no lo que hay en `src/`. Se rehizo la rotura para que compilara y entonces dio los 9 rojos. <br><br>**Lo que la medida corrigio del enunciado, y lo que destapo.** (a) El issue habla de **catorce** endpoints y diez sin ofrecer; son **trece** y **nueve** —#30 retiro el `POST` de deteccion—, y por eso esta pantalla **no lleva boton de «lanzar deteccion»**: ponerlo reintroduciria por la interfaz lo que aquel issue saco del backend, y hoy no fallaria —no hay ni un poligono cargado— sino el dia de la primera carga cartografica. (b) El javadoc de `src/api/fiscalizacion.ts` decia «once operaciones» y «cuatro lecturas»: las dos cifras estaban viejas desde #17 y #23. (c) `CampaniaResource.tope` y los tres campos del acto de la anulacion —`motivoAnulacion`, `anuladoPor`, `anuladoEn`— viajaban desde #23 y #25 y **el tipo del frontend los descartaba**. (d) **Y la siembra del proxy contestaba seis valores que ningun enumerado del backend admite** —`EN_CURSO` y `PASO_GABINETE` como estado, `GABINETE` y `CRUCE` como origen, `OMISO` como clase y `FOTOGRAFIA` como tipo de evidencia—, pintados tal cual en pantalla desde #32: no lo cazaba nada porque este modulo **no declaraba ni una lista** y el punto 8 de `rutas.mjs` solo mira las declaradas. Hoy declara las nueve. <br><br>**Cifras, medidas en el mismo entorno** (Node v26.7.0, yarn 1.22.22, Chromium de `playwright-core`, Docker 29.x): `tsc` y `eslint` **verdes antes y despues**; `vite build` **59 → 62 modulos**, `index` **357,23 → 379,73 kB**, el trozo del proxy **43,83 → 52,10 kB** —que no viaja en el paquete de produccion, y lo sigue comprobando `sin-red`— y el CSS **3,01 kB** sin moverse. Los arneses: `rutas` **33 → 41 rutas declaradas** contra las 59 del backend, **10 de 15 → 17 de 24 listas atadas** (39 → **61** valores en los dos sentidos, +5 → **+7** declaradas) y **7 cuerpos de escritura contra su «record» (24 campos)**, que antes no se median; `datos` **2 → 3 archivos** y **455 → 577 cadenas**, ni una cifra; `motor` **17 → 18 guiones**; `mirar` **34 → 44 pantallas**; `impedimentos` **34 → 44 pantallas** y **29 → 41 controles impedidos**; `sin-red` **34 → 44 pantallas** y **263 → 341 secciones plegadas abiertas**; `errores` **36 → 48 renders** (6 → **8** superficies × 6 escenarios); `reglas`, `paleta`, `ejercicios`, `ficha` e `imagen`, **iguales**; y el nuevo `transiciones`, **8 filas en 2 tableros · 6 estados cubiertos · 2 actos ejecutados**. <br><br>**El reloj, que es lo que #66 dejo vigilado**: los arneses de navegador pasan de **168,7 s a 234,7 s** en local (`paleta` 5,4 · `ejercicios` 3,2 · `ficha` 6,6 · `transiciones` 6,3 · `mirar` 35,0 · `impedimentos` 28,0 · `errores` 94,4 · `sin-red` 54,2). Llevado a CI paso a paso con las cifras que #66 midio —arranque 31 s, compilar y servir 11 s, y cada arnes escalado por su recorrido— el job queda en **~309 s de 600**, o sea el **52 %**: por debajo del aviso del 60 % que `presupuesto.mjs` emite, y lejos del 85 % que le hace fallar. **Es una estimacion y se dice**: este PR no se ha empujado, asi que el «despues» de CI no esta medido. <br><br>**Lo que NO se cierra, con su motivo.** (a) **`TRANSICIONES_DEL_*` no se contrasta con `Candidato.java`**: `transiciones.mjs` mide que la pantalla obedezca a la tabla, y la tabla es una declaracion. Derivarla del fuente Java —«`exigirEstado(ADMITIDO_EN_GABINETE, "verificarlo en campo")`»— es leer logica y no una lista, y una guarda que adivina es peor que una que declara. Lo que si esta atado es que la tabla sea un `Record` COMPLETO sobre `ESTADOS_DE_CANDIDATO`, que el punto 8 saca del enumerado: un estado nuevo no compila hasta que alguien decida que admite. (b) **La hoja de Actas ofrece sus dos actos sin saber el estado del hallazgo**, porque **no hay lectura de UN hallazgo**: se dice en pantalla y el servidor contesta con su `409` y su motivo, que es una respuesta y no un camino inventado. (c) **No se sube ningun archivo de evidencia**: el cuerpo lleva huella, ruta e instante, y subirlo es otra frontera —lo dice el propio issue—. (d) **La tabla de hallazgos se sale de 1 440 px** y se desplaza dentro de su `overflow-x`, que es lo que el sistema de diseno hace con el contenido ancho; con diez columnas y la de acciones al final, la ultima queda cortada hasta que se desplaza. (e) **`sectores`, `calles` y los cuatro `ficha_*` siguen sin token real**: la pestana de hallazgos del predio exige `fiscalizacion_catastral` ademas de `actualizacion_catastro`, y quien tenga uno y no el otro vera un 403 dibujado como el rechazo que es — **no se ha medido con un token de verdad** porque este entorno no tiene backend levantado. |
